# Handover 2026-09-23 (viper, overnight session 09-22 20:00 -> 09-23 06:00, Opus 5.5 medium)

Read this first on a new session, then the memory index (MEMORY.md) and the notes it points to.
Model setup: session and all agents on Opus 5.5 medium; agents use `subagent_type: worker`
(.claude/agents/worker.md). Standing rules added tonight: all timing on GPU; every GPU job exports
HSA_XNACK=1 and HSA_NO_SCRATCH_RECLAIM=1; dhj tests start from prod4 / hyd4 restarts, not prod3;
agents short and single-deliverable (chain them through README files); no replies to interim agent
notifications.

## Productions (running; do not relaunch/chain without the user)
| run | dir | jobs | notes |
|---|---|---|---|
| prod4 (MHD) | bench/cs_mhd_prod4 | 11941995 (link 1, running since 01:31) + 11944873-5 | cs_wellbalanced_src OFF (set before start); links 2-4 have HSA_NO_SCRATCH_RECLAIM=1 (1.32x, bitwise) |
| hyd4 (hydro twin) | bench/cs_hyd4_prod | 11943719 (link 1) + 11944876-8 | prod4 minus B; same env change |
Rot-1.2 check of prod4 (03:25): tracks prod3's cold start (ME 8.2e32, dt 3.8 s), eos_fail 0, efloor
falling. Pace ~0.66 rot/h early: 4 links will NOT reach 283 rot at this dt; re-assess at rot ~20
(dt should rise to ~16 s). hyd4 at rot ~11.5 by 05:00, dt ~20 s.
Both run the LEGACY semi-implicit RT step (dhj pgen never reads rt_semi_lin; rt_newton tested and
rejected: 3-7x worse day side). The 10/14 rank split is state-dependent (faster at rot 283, slower
cold): re-time from a developed prod4 restart before any swap.

## Committed tonight (fork rt-integration, 886bd677..HEAD; see git log)
- 886bd677 per-rank restart read fix.
- M1: 1715190b PCR line solve; 055ca153 fused BiCGStab; d14f96de bvals functors < 32 kB (all
  modules, bitwise); a6202226 fewer Picard passes; 66d5c59a speed-up DEFAULTS ON (pcr, bcg_sync 1
  for all closures; lres_test false / conv_est / EW 1e-2 for eddington, vet_sc, tau -- NOT m1
  Levermore; predictor still off). 3-D He box 1 GPU: ~450 -> ~116 ms/cycle (two-stream ~88).
- M1 closures: 1d22a118 vet_sc (SC formal solution, full tensor); dccdf506 multi-block exact SC
  sweep; d837c355 closure = tau (multi-block).
- Implicit ck on tm + sph + beam: c02ddd34 (T1-T3), 95595afc (T4): 1.70-1.77x the semi-implicit
  step, gap ~1e-6 vs semi -10 % (hyd4 rot 11.5). Needs ck_impl_arat = 1e30 and the T4 switches;
  all default off.
- 1e9c0a04 <hydro> reads cs_wellbalanced_src; eb060b1b prod inputs with it off.

## Open / next (ranked)
1. M1 3-D CFL test (tests_m1/runs_3o_vet3d_cfl/RESULTS.txt): mean T dt-independent but HORIZONTAL
   KE strongly dt-dependent with vet_sc full (x15 at one turnover, cfl 0.3 vs 0.15). Isolation arms
   E30/E15 (eddington) U30/U15 (uniaxial) jobs 11945504-7, bench/m1_vet3dcfl_0923: analyse with
   `python3 tests_m1/runs_3o_vet3d_cfl/gate3d.py E30 E15 U30 U15` (run in that dir). Suspect: the
   once-per-step lagged SC tensor.
2. Predictor restart fix COMMITTED (M1PRED01 restart block; implicit_predictor = step now default
   for eddington / vet_sc / tau; bitwise restarts CPU+GPU; README_PREDRST.md).
3. SC sweep: COMMITTED dd4df74c (ray-parallel kernel: 18.2 -> 4.0 ms per sweep on 1 GPU; 2 GPUs 23.5
   -> 8.2 ms, 5.6 ms with vet_mb_halo = 3). TO-DO in tests_m1/runs_3m_vetmb/README_SCALING.md (angles
   within a node + space across nodes; one message per neighbour; fuse moments; halo 3 default).
   M1 FAST PATH COMMITTED 24eb220a (default off: implicit_halo_direct, implicit_od_cache,
   implicit_krylov_fuse = 3, implicit_op_stencil, implicit_precond = rbgs_fwd): 3-D box 1 GPU
   Eddington 113.6 -> 48.3 ms/cycle (radiation ~28 ms = 1.2x hydro's 22.8), vet_sc full 119.9 -> 67.3
   (measured before dd4df74c; with the 4 ms sweep expect ~53). NEXT: gate these for default-on (as
   66d5c59a did); time vet_sc with fast path + dd4df74c together; README_FAST.md ranked TO-DO
   (inert conduction 6.3 ms in the M1 input, reduction kernels ~3.7 ms).
4. SC heating experiment (tests_m1/runs_3n_scheat, patch not committed): dt-dependent (Lie/Strang);
   needs IMEX to be accurate -- user decision.
5. dhj: odd-even / ragged-front columns exist in every restart (hyd4 1337, prod4 1359 flagged columns);
   prod4 MeshBlock 2 top (1e-9..1e-8 bar) T swings 200-1000 K -- look at it.
6. Small, uncommitted: bench/rtnewton_0923/pgen.patch (dhj reads rt_semi_lin etc., bitwise default);
   eos_cache |dq|/q diagnostic fix (divide by gross exchange); FATAL guard for radially split columns
   under SMR (two-stream / rad_implicit_x1).
7. Planning docs (uncommitted): docs/dev/dhj_resolution_plan.md, cs_dhj_smr_equator_scope.md (SMR not
   worth it; first step = time uniform hyd4 at 128^2 and 256^2 per panel on 1-16 nodes),
   cs_dhj_switch_audit.md.
8. User decisions pending: grav_point_mass / rot_potential on for curved grids; resolution test pair
   (hyd4 128 vs 192 radial); IMEX for SC heating.

## Housekeeping
u2 inode quota is tight: agents put large trees on /viper/ptmp2/jinma (SCRATCH: not backed up, may be purged -- keep patches, READMEs and results on u2 / in the repo); delete build dirs of finished
work (49 deleted 09-23). Worktree group B removal still waits for the user (command in the 09-22
session). Branch-wide cpplint: 2168 pre-existing errors (not from tonight).
