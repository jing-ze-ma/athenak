---
name: rt-cost-profile-0916
description: Kokkos kernel profile of the B-star production box (09-16, 2 apudev GPUs, binary 1ce37b6f): the mode-3 PCR column solve is 78-82 % of ALL kernel time in every scheme; Strang = 1.03x default, ImEx imex2 = 1.42x (one extra column solve per cycle); transverse ADI 2.4 %, halos 0.8 %, hydro 7 %
metadata:
  type: project
---
bench/prof_0916 (jobs 11722084-6, restart from prod_w4 rst 00019, 81 timed + 40 profiled cycles). ms/cycle D 822 / S 846 / I 1168 on 2 GPUs (production on 8 GPUs ~220-280). Shares D: column (rt_col3_p PCR partition kernel, 0.31 s per launch, 6 launches/cycle) 78.3 %, transverse/ADI 2.4 % (50 launches), bvals 0.8 %, sweep+weights 5.4 %, radial implicit 5.3 %, hydro 7.1 %, other 0.6 %. Strang: identical column work, +halo launches (16 -> 24). ImEx: column 6 -> 9 launches (2 -> 3 implicit stage solves) = the whole 1.42x. ANY speed-up must come from the column solve (nseg, Newton tolerance, warm start, fewer solves/cycle). Tool: bench/kokkos-tools kp_kernel_timer.so via KOKKOS_TOOLS_LIBS, prints to stdout.
NEWTON PROFILE 09-16 04:00 (bench/newton_0916, job 11722194): one rt_col3_p launch = one whole solve (Newton loop inside the team kernel), 2/cycle under rk2, 311 ms each = 76 % of the 822 ms cycle. it_mean 3.85 (it_max 4) at tol 1e-8 = ~2.9 factorised passes + 1 rescheck-only confirming pass; no column converges on rescheck alone. ONE full Newton pass = 99 ms (block assembly + PCR elimination ~85, sweep + residual ~14); fixed per-call overhead 12.7 ms. rt_impl_warm = 1: it_mean 3.20, 249 ms/launch (-20 % column, -12 % cycle), Ftop within 4e-10 and E within 4e-13 of the default = FREE -> TURN ON (add problem/rt_impl_warm = 1 to the inputs; rt_report_every is not read by box_convection.cpp, prints only every 100 cycles). NEXT: reuse the block factorisation across Newton passes 2..n (frozen Jacobian, hook rt_impl_cvfreeze) -> attacks ~160 of 311 ms; shortening the column / mixed precision is the second lever.
FACTORISATION REUSE 09-16 06:30 (branch rt-fastcol, bench/wt_fast, bfa74258+1ecdd4bf, pushed; notes bench/fast_0916): implemented (rt_impl_reuse 1/2 + rt_impl_reuse_rho; the Jacobian is constant in the iterate except dedb, the RHS is one number per cell, so a reuse pass = 42 ms vs 114 for a factorising one) and EXACT to the tolerance (Ftop 4e-10, E 2e-13 vs baseline on GPU) - but only 1.06x per cycle at best (rho 0.05, +1.7 GB/rank): with warm start the solve already takes ~2.2 factorising passes and the frozen dedb (swings 10x across the ionisation zones) costs 1.3-1.7 extra passes. NOT worth switching on. Remaining lever = the cost of the FIRST pass itself (assembly + 5x5 inverses, 114 ms) and the sweep/residual: shorten the two-stream column (hybrid deep diffusion), mixed precision in the block inverse, occupancy. Also: box_convection.cpp now reads rt_report_every on this branch.
HYBRID DEEP DIFFUSION 09-16 06:00 (bench/hyb_0916, rt-fastcol 917687aa+10293d3c pushed): the existing rt_col3_hybrid_tau was simply NEVER SWITCHED ON in production. B-star GPU (prod_w4 rst 19, warm 1): off 293 ms/call 791 ms/cycle; tau_hyb 100 -> 222 ms/call, 644 ms/cycle = 1.32x call, 1.23x cycle, dFtop 1e-8, dE 6e-11 (1-D accuracy: tau 30 shallowest passing dT/T < 1e-6, tau 100 chosen = same cost + a decade of margin). SHIP: problem/rt_col3_hybrid_tau = 100 in the B-star inputs (+ rt_impl_warm = 1): combined ~1.4x per cycle for free. NOT for the He box (its column only reaches tau ~few hundred at the cut; hybrid leaves 1e-4 errors). nseg stays 32; the work-balanced-segment variant (rt_col3_split_deep) is a measured pessimisation, committed default-off. NEXT LEVER: mixed precision in RTCol3Inv5 (float factor, double residual) ~1.3-1.5x on the thin rows; slot-layout trim ~1.1x.
HYBRID DYNAMICAL GATE PASSED 09-16 07:10 (bench/hybgate_0916, ARMS 11 H0/H100/H30): 1-D 8000 s: no new spectral line, column mode damping within 0.4 %, dFtop 1e-9; 3-D f-mode arms: gamma identical (0.0 sigma), Ftop within 5e-11, no interface-localised structure; 1.06x on the small box whole cycle. SAFE FOR THE B-STAR PRODUCTION at rt_col3_hybrid_tau = 100 (keep off for He). Both switches (rt_col3_hybrid_tau, rt_impl_warm) predate the production binary 4e5007b9, so the B-star link 2 needs ONLY the two input lines in prod_w4_p/fecz_rt_p.athinput (input is read at job start; the sbatch snapshot covers only the script) after a restart smoke with the production binary. NEXT: mixed precision in RTCol3Inv5 (launched).
MIXED PRECISION 09-16 07:30 (rt-fastcol 1878d91c pushed; bench/mp_0916): problem/rt_impl_mixed 1 = float 5x5 inverses + elimination products with row equilibration + pivot fallback to double (nmixfb counter), 2 = also float storage of the factors (compact WkF View, +37 % memory). Double kept for assembly, residual, EOS, reduced system, update = iterative refinement. CPU: bitwise off; on: dFtop 1e-10, dE 1e-13, it_mean unchanged, no fallbacks. GPU (warm + hybrid 100): mixed 0 233 ms/call 669 ms/cycle; mixed 1 208 / 616 (1.12x/1.09x); mixed 2 195 / 612 (1.19x/1.09x), dFtop 4e-12. ADOPT mixed 2 (or 1 if memory binds). STACK on the B-star production box: cold default 311 ms/call, 822 ms/cycle -> warm + hybrid 100 + mixed 2: 195 ms/call, 612 ms/cycle = 1.34x per cycle, answer unchanged to 1e-10. Caveat: HIP compile of box_convection.cpp ~12 min (three template instantiations). Remaining: memory-bound kernel; slot-layout trim ~1.1x.
