# Handover 2026-09-25 ~05:00 (viper; session 09-25 early morning; Opus 5.5 medium) -- START HERE

Read this block, then MEMORY.md (copy: docs/handover/claude-memory-2026-09-25/). rt-integration = dbfbe0d6, PUSHED to
fork. Session 09-24 night CRASHED at 00:44: an analysis agent ran multiprocessing Pool(24) x 6-8.6 GB on the login node
(108 GB user cgroup) -> OOM killed everything. GUARD NOW INSTALLED: ~/.claude/hooks/memcap.sh (PreToolUse Bash hook in
~/.claude/settings.json, viper only) runs every Bash command in systemd user slice claude-work.slice (MemoryMax=70G,
~/.config/systemd/user/claude-work.slice). Side effect: `cd` does not persist between Bash calls. After a crash: kill
orphaned Pool parents (they respawn workers) and old sessions' watcher shells.

## Merged today (all gated, pushed)
| merge | what |
|---|---|
| 587accf0 dhj-grid256 | team scratch falls back to level 1 only when level 0 cannot launch (nx1 256/320 run); radial poly stretch 8 coefficients (4-coef bitwise) |
| 6375568c dhj-sponge-switch | problem/sponge_top, problem/sponge_bottom (default true, bitwise) |
| 091d1422 m1-precond | <rad_m1>/implicit_precond = mg (box -7 %, wedge ~0; boxes only, implicit_mg_levels 3) |
| 3e7659fd m1-fast3 | inert-conduction skip + RosselandTable bisection (on, bitwise, box -8 %); implicit_op_team_red |
| dbfbe0d6 | implicit_op_team_red DEFAULT ON (user; box -12 %, wedge -13 %, round-off) |
| d3a04acc ck-nq2 | ck_nquad = 2 on the production ck path (thick limit exact), dead-cell fix CkDeadE, speed steps, ROUTE B |
| db8b4764 ck-hitemp | ck tables to 10100 K (data files gitignored, in data/exo_fms_ck/*/..._hiT.txt on viper; regenerate with data/exo_fms_ck/tools/gen_hitemp.py, needs pip --user pyfastchem); key problem/ck_ce_table |
| d38ace15 dhj-cksph-guard + 9de6060b | FATAL: ck_spherical=false + curvilinear + tau blend (non-conservative); dhj_ck_spherical/implicit test inputs flipped to true; red_giant_cs.athinput now stops by design |

## User decisions 09-25
- Production ck = ROUTE B: ck_nquad = 2, isotropic_conduction = none, problem/ck_pcut_bar = 1e8 (ck to the inner wall,
  no Rosseland handover), T4 + c2 + ck_impl_every = 4, hiT tables (problem/ck_table + ck_ce_table IN the input file).
  C32 2 GPUs: nx1 128 B 31.5 ms/cycle (n1 blend 29.9), nx1 256 B 75.3 (n1 76.8); 0 non-converged; wall flux 1.004.
  (The "7 %/18 % handover mismatch" was a test-formula artifact; kappa_R table = ck mean to ~1 %.)
- Deep convection (/viper/ptmp2/jinma/deepconv_0925): RCB ~3 bar; deep strongly convective; old IC (substellar
  picket fence + 0.9 nabla_ad) -> ~60-rotation transient, deep cooled 74-163 K onto the cooled 3-bar adiabat. No MLT.
- New IC = 1-D ck RCE, GLOBAL MEAN (f = 1/4), nquad 2, route B, exact EOS adiabat below the RCB, hiT tables, via new
  problem/ic_profile (table must reach >= 250 bar). Agent running (branch dhj-ic-profile, /viper/ptmp2/jinma/ckrce_0925).
- Resolution counts ONLY below 1e-6 bar (memory resolution-only-below-1e-6-bar).
- MHD relax (/viper/ptmp2/jinma/mhdrelax_0924): bbot=3 MHD = hydro after rot 6 -> hydro spin-up + 10-20 rot C256 MHD.
- prod4 and hyd4 CANCELLED (superseded physics); restarts kept: prod4 dhj.00179.rst, hyd4 dhj.00217.rst.
- THREAD TRIAGE (user "too many things"): production path only; no new side threads without asking.

## Production path (in order)
1. ck RCE initial profile -> merge dhj-ic-profile.
2. Rebuild the sparc_0925 binary from rt-integration (route B, nq2, hiT, sponge switches, ic_profile); update
   /viper/ptmp2/jinma/sparc_0925/sparc.athinput (grid nx1 66, 8-coef, 3 cells/H below 1e-6 bar; add route-B keys, hiT
   tables, ic_profile, ck_nquad 2); smoke.sub on apudev; then 4 sponge arms (base/nobot/notop/none), 1 GPU each, 100
   rot, two arms per node: `run.sub base nobot`, `run.sub notop none` (+ afterok links). User said: submit (I submit;
   agents' sbatch is often refused by the classifier). Sponge runs + IC were ON HOLD until the IC is ready.
3. Radial A/B a320 (job 11967744, /viper/ptmp2/jinma/radab_0924) start est. 09-26 03:16 -> analyse with ana.py/maps.py
   -> decide the spin-up grid (recommended: 8-coef nx1 256, spin-up from a fresh start with the new IC).

## Running at handover (in-process agents die with the session -- resume from their dirs)
| thread | state / resume from |
|---|---|
| ck RCE IC | /viper/ptmp2/jinma/ckrce_0925 (README, rce.py), branch dhj-ic-profile |
| SC-on-sp prototype (finish, then stop) | /viper/ptmp2/jinma/scsp_0925 (scans running; README when done) |
| m1-mgfuse phase 1 only (kernel fusion) | /viper/ptmp2/jinma/wt_mgfuse (uncommitted work; profile job 11969244 done); phase 2 (global coarse correction) PARKED |
| ck-fast2 (nq2/B speed + scaling) | NOT started; start once the production binary exists |

## Added after 05:00
- m1-mgfuse phase 1 FAILED (fusion exact but slower; mg cost = small slow coarse kernels, not launch overhead);
  docs merged 7aabf6fd; phase 2 parked. SC-on-sp prototype (dhj): NO-GO (/viper/ptmp2/jinma/scsp_0925).
- NEW (user): second-order implicit VET on the sp wedge -- branch m1-sp-order2, worktree /viper/ptmp2/jinma/wt_sporder2,
  runs tests_m1/runs_5o_sporder2 (lift implicit_recon = dc refusal on sp with a validated 2nd-order reconstruction;
  convergence gates thick/thin/lateral with hesdirk2).
