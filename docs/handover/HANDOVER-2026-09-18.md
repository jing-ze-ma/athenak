# HANDOVER 2026-09-18 (viper, 01:00): global He4 model after the evening/night rounds

Read after `HANDOVER-2026-09-17-B.md`. Covers 2026-09-17 21:00 to 2026-09-18 01:00. Branch
`he4-presn-global` in `bench/wt_he4` (HEAD e73c6add). Nothing pushed. The Opus agent quota is
exhausted until 2026-09-19 23:00 (Europe/Brussels); Sonnet/Fable only until then.

## Productions
B star `prod_w7` reached 39/40 turnovers, statistically steady (KE +0.013 +- 0.016 per turnover,
profile drift < 2e-5/turnover in the CZ, F_top = F_imposed to 5 digits, 21 box / 104 CZ thermal
times). He `box_w8` at 27 turnovers, thermally relaxed (1e4 thermal times) but velocity still
rising: 2.3-2.5 v_MLT at the driving layer. Page v7 (version 6, simplified to the table + v_z and
dT/T maps on tau 1 and the opacity peak): https://claude.ai/artifact/SLgYFj9JDBUsNqxj5UYgPJ.
HR diagram of 3-D envelope simulations (v9, light/dark/build-up):
https://claude.ai/artifact/79q5ATcM6xcDy5QLTR39Gz, files in bench/analysis_0916/hrd_3d/.

## The global He4 model: what was fixed tonight (all on he4-presn-global)
| commit | fix | evidence |
| --- | --- | --- |
| 8bca3dfa | spherical two-stream rewritten: geometry only at the faces (S and A D continuous); mode 3 exact (FormalSph), mode 0 lagged O(beta^2) | thick test 1e-4 (was 16-97 %), transparent 1.0002, He t=0 F_2s/F_raddiff 0.98-1.02, box bitwise (tests_r4) |
| e247210d | efloor_as_tfloor; tfloor raised to the table edge; retraction: the "EOS garbage T=5e11" was wtemp in code units = the table clamp | tests_r5 |
| 5b71b144 | dfloor_keep_velocity + vceil 1e8 (null on structure, kills the 212 c cell) | tests_r6 |
| 52d16d6b | optional marked pgen restart block ("PGENST01"); red_giant's MLT closure state in it | chained 1-KE continuity 1e-15 (tests_r7) |
| 65cd5bd1 | the 0.79 R killer = the radiative force handed to a thinned interior cell by the rho-only taper | only rt_rad_force=false removes it (tests_r7) |
| a6d66c3c | taper gated on TEMPERATURE (eos_rad_t_hi/lo at tau 3/0.3), consistent at all EOS sites; RegionIndcs zero-init | 1-D alive > 2.5 turnovers, 3-D > 1.5, Gamma_rad -> 1, Mdot(0.9 R) turned over (tests_r8) |
| e73c6add | tests_r9 bisection of the 0.97 R pile-up; rt_profile T/eint slots fixed | see below |

## Where it stands
The star no longer dies of a single cell. It inflates because the FeCZ is super-Eddington on
radiation alone (Gamma 1.0-1.2 over 0.66-0.94 R) and nothing resolved carries the convective
share yet (F_res = 0, v_r,rms 0.02 v_MLT); the shell-mean MLT closure (mlt_alpha 1.5) holds
Gamma_rad at 1.00-1.05 and the interior settles. REMAINING DEFECT (tests_r9): the emergent
luminosity relaxes to 0.956-0.981 L while the domain thermal time is 0.46 turnover, so the
envelope banks 4-9 % of its energy per turnover (+20 % by 2.5 turnovers), lifts the 0.94-0.99 R
layer and dt falls to 0.08 s. None of six input changes moves it (mlt off, flux via the wall,
top wall, no sponge, taper window at tau 1/0.1, rad_tau 20/300, nx1 144). The shortfall is in the
last five optically thin cells and the top face (F_2s/F_req 1.28 at t=0 at the top): suspect the
spherical sweep's top boundary / thin-layer treatment (rt_top_re off under mode 3, the ghost
mirrors the top cell), NOT the interior form (fixed, 1e-4). Second item: the ic and the solver
disagree by 28 % at the top face at t=0 (build the ic against the solver, as make_ic_he.py does).

## What to do first
1. 1-D grey atmosphere with an analytic answer INCLUDING the top boundary: gate L_out/L = 1.000
   off rt_surface.bin at 0.1 turnover (extend tests_r2/thick to report the top-face flux and the
   emergent L); fix the top-face treatment of the spherical sweep (mode 3 and mode 0).
2. Rebuild the ic against the run's own two-stream (zero-cycle probe + secant on T(r)).
3. Re-run tests_r8 Gnr8 (3-D smoke, vpert 1e-3 then 1e-2) to 3-5 turnovers; gate on L_out/L within
   1 %, dt not eroding, shell drift falling; only then discuss production with the user.
4. Open: walled-star mass loss and the sponge's hold on dt (tests_r9 5.2); bvals_fc seam
   defects (MHD); rt_rad_force transverse fix and the seam fix not yet ported to rt-integration.
