---
name: he4-r11-floor-arms-verdict
description: He4 global model tests_r11 d1/d2 (dfloor 1e-11, 1e-10) verdict 09-18 - floors do not cure; exponential radial expansion identical in all arms
metadata:
  type: project
---

tests_r11 d1 (dfloor 1e-11) and d2 (1e-10) both ran 2 turnovers (bench/wt_he4/tests_r11). End dt: g3d 0.07 s, d1 ~0.5 s, d2 ~1.5 s (0.18 at the last step): the floor only delays the erosion.
The real problem is identical in g3d/d1/d2 to 3 digits: radial KE (hst col 8) grows x2.2-2.3 per 0.25 turnover from 0.5 to 1.5 turnovers (3e36 -> 2.7e38 erg, v_rms ~1.7e6 cm/s), KEr/KEh ~100, i.e. a spherically symmetric exponential envelope expansion (e-fold of v ~0.6 turnover), not convection and not the voids. At 1.6-1.9 turnovers it reaches the top: F_top per column goes negative / x30, L_out/L 2.8-4.1 (d1) or -0.4 (d2), mass -1.2 to -1.8 %, tot-E +20 % (d1) / +65 % (d2). d2's floor exceeds the IC top density 5.5e-11 (mass rises before 1.5 turnovers): invalid arm by construction.
**Why:** hot voids are a symptom of the outer shells being launched; the next step of [[he4-presn-global-plan]] is not a 5-turnover chain but finding what drives the radial runaway (Gamma_rad ~1 layer, 2-3 % flux shortfall, see tests_r11/README.md).
**How to apply:** do not chain d1/d2. CHECKED: the 1-D arm a15_11 has the SAME KEr history (3.04e36, 1.06e37, 2.40e37, 5.10e37, 1.16e38, 2.66e38, 4.74e38, 2.9e39 at 2.0), so the runaway is 1-D physics/numerics of the hydrostatic envelope and can be debugged on the cheap 1-D column (apudev).

**1-D discriminator arms 09-18 evening (tests_r11/{c15,ns,f97,f94}, radial momentum hst col 4 at 0.25 turnover; base a15_11 = +1.02e31):** c15 (cfl 0.15) +4.8e30 = HALF -> the outward push is O(dt) numerical; ns (rt_strang=false) -1.5e30 = nearly balanced, end dt 6.4 s (base 1.85); f97 (bottom flux x0.97) -9.9e30, f94 -3.05e31 -> linear in flux, zero at ~1.5 % reduction, i.e. the Strang split acts like a +1.5 % radiative force at cfl 0.3, fatal at Gamma_rad ~1. Same family as [[fmode-dt-taper-test]] (split lag). ns still grows outward after 1 turnover (KEr 1.5e38 at 1.5): follow-ups ns3 (3 turnovers) and nsc15 (ns + cfl 0.15) submitted.

**CORRECTION 09-18 late (arms c075, nsc15, nsc075, ns3): Strang is NOT the culprit.** Momentum at 0.25 turnover vs cfl 0.3/0.15/0.075: Strang +1.02e31 / +4.76e30 / +4.71e30 (CONVERGED at cfl <= 0.15 to a real +0.7 %-of-flux outward imbalance); rt_strang=false -1.5e30 / -2.36e31 / -3.89e31 (DIVERGES as dt shrinks: the unsplit path is inconsistent, never use it; the ns balance at cfl 0.3 was a coincidence). ns3 blew up at 2.0-2.4 turnovers like everything else (L_out/L 1.06 -> 0.2 -> -3.8). So: keep rt_strang=true, use cfl 0.15 on this star (0.3 doubles the push); the late event at 1.7-2.0 turnovers (top goes opaque, L_out -> 0, shells launched) survives dt convergence and is the open problem; f97 (flux x0.97) was the only arm still healthy at 2 turnovers (dt 11 s).

**LATE EVENT SOLVED 09-18 ~18:00 = THE TOP RT CLOSURE (unresolved-column model) SHUTS when dense gas reaches it.** c15 rt_profile: a shell pulse (2.5e6 cm/s) reaches the top cell at 1.5 turnovers, top rho 2.2e-11 -> 8.4e-10 (x40), the closure's column above the domain turns optically thick, L_out/L 1.00 -> 0.15, the trapped L makes the whole envelope above 0.94 R isothermal at 1.6-1.8e5 K (top T x4), then collapse/launch. Arm c15vac (cfl 0.15 + problem/rt_top_vacuum=true): L_out/L 1.003-1.012 through 2 turnovers, KEr SATURATES at 2.5e37, end dt 5.1 s. vac11 was null only because it stopped at 1 turnover, before anything reached the top. RECIPE for this star: rt_strang=true, cfl_number=0.15, rt_top_vacuum=true. c15vac3 (3 turnovers, job 11812928) submitted; next = 3-D with the recipe (user approval).
c15vac3 result: ALIVE at 3.0 turnovers, L_out/L 0.991-1.004, end dt 3.6 s; BUT KEr not saturated: 2.4e37 (1.9) -> 4.3e37 (2.4) -> 1.1e38 (2.9), momentum still outward and rising = the residual slow expansion of the +0.7 % IC imbalance (1-D has no convection to carry flux). Not a blow-up; whether it saturates is the 3-D question.
3-D RECIPE RUN LAUNCHED 09-18 ~18:45 (user approved): tests_r11/v3d, job 11817568 (apu, 1 node, 3.5 h limit), r11_3d_arm.sh v3d time/cfl_number=0.15 problem/rt_top_vacuum=true time/tlim=23525 (5 turnovers, rst every 0.2 turnover; resubmitting the same command chains from the newest rst). Gates: alive, L_out/L (lout9.py), hst KEr (col 8) vs KEh (9+10), dt, top-cell rho from rt_profile.
v3d STATUS 09-18 ~22:00 (t = 4.6 turnovers, job near its 3.5 h limit): ALIVE, no top-closure event, L_out/L 1.04; BUT the envelope keeps expanding through the open top: domain mass 1.912e26 -> 1.531e26 (-20 %, loss starts at 1.5 turnovers, fastest 3.0-4.0), KEr 2.6e37 (2.0) -> 2.9e39 (4.5), KEh 4e36 -> 3.4e38 (convection growing, ~12 % of KEr), dt 6.2 -> 0.29 s. So the recipe cures the radiative lid, not the secular outflow: the star is inflating / losing its outer envelope, not settling.
v3d PROFILES (shell means): NOT a wind. Outward phase 1.5-3.5 turnovers (v1 2-4e6 cm/s = 4-7 % of v_esc 5.9e7, top rho x13-30, Mdot through the top 9e21 g/s), then FALLBACK at 4.0-4.5 (v1 -4 to -9e6 cm/s at 0.90-0.97 R, rho there x0.2, T -35 %). = a large-amplitude RADIAL PULSATION (period ~4 turnovers ~2e4 s ~3.5 t_dyn, fundamental-mode-like) whose outward half drains bound mass irreversibly through the open top at 1.014 R (-20 % of the domain mass). The top is too close for this star: needs x1max ~1.3-1.5 R (IC column extension in make_ic_sph_d.py) or a top that returns mass; test in 1-D first (the 1-D c15vac3 showed the same outward phase to 3 turnovers).
TOP-BC ARMS 09-18 night (1-D, recipe cfl 0.15 + vacuum): v3d ended COMPLETED at 4.7 turnovers (3 h 20 wall), L_out 1.03-1.08. Domain extension to 1.3-1.5 R is NOT practical: gas scale height above the photosphere 0.002 R, ic_profile ends at 1.0256 R, a pressure-matched hot corona would need rho ~1e-15 < EOS table edge 1e-13. outer_bc=wall (wallv) is NOT a lid: ghosts from the IC column, top cell still +2e6 cm/s, mass -2.1 % at 3 turnovers (open: -2.6 %), dt 0.40. open_outer_noinflow=false (inflv) = open. NEW `problem/open_outer_lid` (mirror v1 at the top face; red_giant.cpp +14 lines UNCOMMITTED in bench/wt_he4, binary tests_r11/athena_v8, param added to he4_presn_cs.athinput because cmd-line overrides of absent params FATAL; default-off bitwise vs c15vac3): lidv holds the mass (-0.33 % at 2.5) but KEr grows the same (2.5e37 at 2.0 -> 6.5e37 at 2.5), dt collapses to 0.14 s, reached only 2.51 turnovers in 15 min. CONCLUSION: the growing radial motion is independent of the top BC; candidates = strange-mode/kappa pulsational instability (L/M = 1.5e4 Lsun/Msun, expected unstable) vs the +0.7 % IC imbalance. Bursts reach the top at ~1.6 and ~2.8 turnovers (period ~1.2 turnovers ~5600 s ~ t_dyn).
RELAXED-START ARMS 09-19 00:30 (new problem/vdamp_all_until + vdamp_all_time: global v1 damp then release; athena_v9, UNCOMMITTED with the lid): damp1 (tau 20 s, 1 turnover) and damp2 (tau 300 s, 2 turnovers): under damping the momentum sits at a TERMINAL value (damp2: 3.7-5.6e30 => force p/tau = 1.9e28 dyn, slowly rising), and after release p rises at 1.75e31 per 0.2 turnover = the SAME 1.9e28 dyn = ~0.6 % of the envelope weight. tot-E rises steadily 3.8e35 erg/s (0.16 % L) even while damped, L_out/L 1.005. So: a persistent secular outward force + slow heating, NOT a noise-seeded oscillation. Caveat: overdamped relaxation time is t_dyn^2/tau ~1e5 s for tau 300, so neither arm could actually relax; damp3 (tau 1500 s ~ critical, 4 turnovers damped, release, to 6; apu 45 min) submitted to test real relaxation.
damp3 (tau 1500 s, damped 4 turnovers, released, to 6.0; apu 23 min): NEVER relaxes. Damped momentum creeps 2.3e31 -> 4.4e31 (force p/tau ~2-3e28 dyn, rising), tot-E +0.2 %/turnover, mass starts leaving the top at 3.3 turnovers while still damped; after release the usual expansion, M -11 % by 6.0, fallback at 6.0, dt 0.93. budget9.py on it: t=0 F_2s/F_req = 1.04 / 1.21 / 1.28 at 0.99 / 1.00 / 1.01 R (top shells lose 0.5-3 % L at t=0) and the 0.96-0.97 R cells gain eint +35-40 %, rho +20-34 % by 3 turnovers = the tests_r9 "0.97 R pile-up" again. VERDICT: not an instability seeded from noise and not curable by damping: a standing thermal/force inconsistency between the IC (grey Eddington column, ideal mu) and the run's own two-stream + tapered table EOS in the top ~5 % of the radius. Next = build the IC in equilibrium with the code's own RT (iterate T(r) until the run's t=0 F_2s = L/4 pi r^2 on every face, then re-solve hydrostatic p with radiation force), before any more 3-D.
tests_r12/relax_ic.py (09-19 ~02:00): code-in-the-loop IC relaxer (1-cycle probe via r12_arm.sh + athena_v9 -> Unsold flux correction of T above 0.935 R -> hydrostatic re-solve with the run's force law -> ic_passNN.txt). Offline EOS/opacity match the run (dT 7e-5, dk 3e-4; force law reproduces the old IC's dpgas/dr to a few %). FINDINGS: (1) the t=0 top flux excess 1.28 ~ the 1.33 a two-stream with diffusivity 1.5 gives on a grey Eddington T(tau): the mismatch is the two-stream SURFACE CONDITION vs the Eddington 2/3, not the EOS T (T_table = T_col to 0.05 %). (2) the code-consistent atmosphere is ~6 % cooler at the top; there kappa_He rises with rho (0.57 -> 0.85), Gamma 0.83 -> >1 just outside R, the outward hydrostatic integration flips to a no-decline branch (rho ~7e-10 to the top). (3) RELAX 0.8 one pass: unusable; RELAX 0.35 five passes: top-face excess +28 % -> +11 % (ic_pass01, best) -> -22 % -> +50 %: oscillates, not converging (tau scale moves with rho). Proposed next: make the two-stream top/diffusivity consistent with the Eddington atmosphere (code), rather than re-relaxing the star to the closure. No pkill -f patterns on the shared login node (matched another user's pid once; harmless).
RETRACTIONS 09-19 ~03:00: (a) the "diffusivity 1.5 surface condition" explanation is WRONG: the run uses ck_nquad = 2 (two-point Gauss-Legendre), which is exact on an Eddington T(tau); my own planar 2-pt formal solution on the run's dumped (T, tau) gives F(0)/F_req = 1.04, not 1.28. (b) the t=0 excess is RESOLUTION-INDEPENDENT (nx1 96 / 192: 1.278 / 1.286; nx1=384 probe wrote no dump, not investigated) and grows exactly across the EOS-taper ramp (0.986 -> 1.005 R, w 1 -> 0). Best reading: mltfaces "first call" F_2s is the flux AFTER the first implicit column solve; the tapered top cells have a thermal time of ~seconds (heat capacity without aT^4), so the excess is their fast cooling to the two-stream's own RE (README: 1.278 at t=0 -> 0.98 by 0.1 turnover). It is a seconds-long transient and is NOT established as the driver of the secular +0.6 % outward force. relax_ic.py therefore chases a transient; do not use ic_passNN. STILL UNEXPLAINED: constant ~2e28 dyn outward force + tot-E rise 0.2 %/turnover with L_out/L 1.004. rt_profile has no true two-stream flux slot, so an offline per-cell force residual cannot reach 0.6 % accuracy; the next diagnostic needs an in-code per-cell force budget (dp/dr, rho g, f_rad, WB residual) dumped from a damped quasi-static state.
FORCE LOCALISED 09-19 ~05:00 (tests_r12/fbud.py on arms with problem/rt_profile_dt=1: d(rho v1)/dt per cell over the first 300 s of the 1-D column = the net force as the code applies it): a persistent DIPOLE in the DENSITY-INVERSION layer under the Fe bump: f/(rho g) = -1.7 % at 0.90 R, +3.2 % at 0.925 R (dln rho/dln p = -1.9 there, Gamma 0.93), present from the first step, zone total ~1.8e27 dyn. UNCHANGED by: wellbalance_dynamic=false (fb1), wellbalance_static + reconst (fbs), mlt_relax_time 1e9 (fbz), and the new problem/mlt_flux_fix (fbx; signed shell flux residual on faces the closure leaves alone, athena_v10, default-off bitwise vs c15vac3). nx1=192 halves the peak (+1.7 %) but not the zone total. wb_option isothermal/adaptive are far worse (-2.6 % g everywhere); mlt_relax_time=20 s is violently unstable (+-60 % g). t=0 flux budget: closure makes SUM = 1.0000 where MLT acts (0.64-0.93 R); purely radiative faces are 0.9985-1.0001 (0.51-0.62 R) and 1.001-1.003 (0.94-0.98 R); T drifts +0.07 % below 0.93 R / -0.07 % above in 300 s. REFUTED: "thermal imbalance is the driver, fix the flux": ffx3 (recipe + mlt_flux_fix, 3 turnovers) expands the same (KEr 1.7e38 at 3.0), L_out/L drops to 0.96-0.98, dt 0.47. OPEN: what momentum term makes the dipole at the inversion if static WB does not remove it (check that static WB was really active in fbs; then the geometric/curvature source and the FOFC/reconstruction at the density extremum 0.945 R).
09-19 ~08:00 (new session): (1) fbs static WB WAS ACTIVE (hst differs from fb0: momentum -1.5e30 vs +3.5e29, KE x7; outside the zone -6.7e27 dyn, so static is worse overall) and the dipole is identical to 3 digits => hydro pressure/gravity discretisation is INNOCENT. (2) rt_profile at dt=1 s (scratch dp.py: slot 5 T, slot 6 eint): within 13 s T shifts +1.8e-4 below 0.915 R and -5e-4..-9e-4 above 0.93 R (to -1.1e-3 by 50 s, then frozen), rho unchanged (1e-5): the first implicit column solves pull T to the code's own discrete radiative equilibrium. With p ~ T^4 (LTE rad pressure in the EOS here, w=1) that 1e-3 T step is a 0.4 % pressure step across 0.905-0.93 R = +0.5 % rho g at 13 s, before any motion; the later 1.7/3.2 % dipole is this plus the rho response. Fits nx1=192 halving (truncation of the IC-vs-discrete conduction operator). Zone = where the closure's F_conv falls 12 % -> 0 (0.88-0.935 R). NEXT: thermal pre-relaxation of the IC with hydro frozen (a few 100 s) then hydrostatic re-solve of rho at the relaxed T; separate open item = tot-E +0.16 % L secular source under damping.
RETRACTION 5 + DIPOLE CAUSE FOUND 09-19 ~08:30: the "T relaxation -> p~T^4 step" reading above is WRONG (fr1 on a T-pre-relaxed IC ic_rx1: dipole and T drift identical; force at full strength at the first dump t=6 s). DISCRIMINATORS (30 s arms, force = d(rho v1)/dt /(rho g) at 0.925 R): mlt_alpha=0 FLIPS the sign (fm0); rt_rad_force off: no change (ff0); cfl 0.3 / 0.15 / 0.0375 = +8.5 / +4.1 / +1.0 % = EXACTLY LINEAR IN dt. MECHANISM: div F_conv (MLT closure, up to 12 % of L across 0.88-0.935 R) was deposited inside the RK stages (RedGiantGravity rg_mlt_div) while the balancing two-stream is Strang-split outside them -> the stages see a pressure error ~ dt div F_conv. FIX = new problem/mlt_split_deposit (default off; red_giant.cpp, UNCOMMITTED on he4-presn-global, binary tests_r11/athena_v11, script tests_r12/r12_arm11.sh): deposit div F_conv in RedGiantRTSplit BEFORE the sweep (then the implicit column solve is backward Euler with the source, balanced state = fixed point). Deposit AFTER the sweep is 1.25x WORSE (fs0/fs3). Result ft0: dipole 4.3 % -> 0.4 % of rho g (cfl 0.15), ft3 0.9 % (cfl 0.3); a one-time -1.6 % first-step kick remains (fconv_ not built before the first half step; mlt_owed_). Same family as [[fmode-dt-taper-test]] (split lag). 3-turnover 1-D test = tests_r12/sd3 (job 11832456). Also: the LAST rt_profile dump of every run carries a +3-5e-4 T step (end-of-run artifact): never use the final dump for thermal diagnostics. default-off bitwise check of v11 still owed (fd0 hst not comparable to fb0: different tlim).
sd3 RESULT (3-turnover 1-D, recipe + mlt_split_deposit): the dipole fix does NOT cure the expansion: momentum +4.98e30 at 0.25 turnover (c15vac3 4.76e30), 4.2e31 at 1.0 (4.5e31); then WORSE: KEr 6.5e37 / 4.8e38 at 1.5 / 2.0 (c15vac3 2.5e37 / 2.6e37), dt 0.04 s at 2.1 turnovers, cancelled; mass held to 1.75 (1.91234e26) where c15vac3 had already lost 1 %. Should have seen it beforehand: dipole zone total 1.8e27 dyn vs the 1.9e28 dyn secular force of damp2. The momentum growth is exponential-like (x3 per 0.5 turnover from t=0), tot-E rises in both. OPEN: (a) energy source ~0.1-0.2 % L with L_out/L >= 1 (non-conservative piece: taper bookkeeping, rad-force work, floors?), (b) genuine linear instability of the Gamma~1 envelope (needs an independent 1-D reference: MESA hydro / GYRE nonad). Keep mlt_split_deposit as a correct fix but it is not the recipe's missing piece.
ENERGY LEDGER 09-19 ~09:30 (new problem/e_ledger=<cycles>, single rank, prints "### e_ledger": rt / mlt / srcs / hydro split of the change of sum u0(IEN) dV; binary tests_r11/athena_v12, script tests_r12/r12_arm12.sh; v11 default-off verified BITWISE vs v10 (fd0 vs fd10), v12 default-off NOT yet checked). Arm el0 (recipe + mlt_split_deposit, 0.5 turnover): the star GAINS 0.45 % L (0-600 s), 0.32 %, 0.22 % L (1200-2350 s) and ALL of it comes through the two-stream sweep (rt = dE to 1e-3); MLT deposit conservative (1e26 of 1e39); srcs (-8.8e38) and hydro (+8.8e38) cancel to 0.2 % (etotgrav bookkeeping). With L_out/L reported 1.003-1.012 the sweep should REMOVE 0.3-1.2 % L, so the RT operator's actual deposit exceeds F_bot - F_top by ~0.5-1.5 % L = a non-conservation inside the sweep. NEXT SESSION: split the sweep's deposit (rad-force work term, taper w bookkeeping of aT^4, r^2 geometry of the deposit vs the reported top flux, LimitRTSource/relax sub-cycle; rt_desum is planar dx1-weighted, not usable as is) and compare sum(deposit dV) with area-weighted F_bot - F_top per sweep. All code UNCOMMITTED in bench/wt_he4 (red_giant.cpp +~110 lines, he4_presn_cs.athinput +2 params).
PER-SHELL LEDGER 09-19 ~10:30 (e_ledger now also writes e_ledger_shell.txt: per radial shell, sweep+split-MLT dE; col 3 "reported flux" is ZERO because pc->rad_f2s is not allocated in this setup -> still needs a face-flux source; arm el1, 1033 s): the +0.41 % L gain is NOT at the top (cells above 0.92 R lose ~0.02 %): bottom wall boundary layer i=0..3 gains 0.094/0.028/0.011/0.005 % L (0.14 % total), base of the MLT zone 0.64-0.72 R ~0.1 %, the rest spread over 0.55-0.91 R. I.e. L(r) carried by the code's operators falls from 1.000 at the bottom to ~0.9956 at 0.91 R: the IC interior is off the DISCRETE thermal steady state at the 1e-3 level and heats; yet lout9 reports L_out/L = 1.003-1.0045 -> 0.8 % inconsistency between the reported top flux (or its 4 pi X1MAX^2 normalisation) and the energy actually removed: CHECK NEXT. Heating 4e39 erg/turnover >> KEr 1e37-1e38, so it can drive the expansion (consistent with f97 = the only healthy arm). WB: no-WB arm sdnw == dynamic-WB sd3 to 1 turnover (mom 4.38e31 vs 4.19e31): WB scheme irrelevant (static too, fbs). User asked 09-19: keep to 1-D, compare dynamic WB / no WB.
sdnw (no WB, recipe + mlt_split_deposit, 1-D) ends like sd3: KEr 4.9e37 / 1.2e38 / 9e38 at 1.5 / 1.75 / 2.0 turnovers, dt 0.12 s at 2.0. lout9 normalisation checked (x1max 2.4057e11 is right), so reported L_out/L 1.003-1.005 vs ledger-implied ~0.996 stands unexplained (either L_in > L by ~0.8 % at the rt_bottom_flux face, or the top removal < reported flux). Reading of the old f97/f94 arms: momentum responds LINEARLY to the bottom flux within 0.25 turnover, which can only act through F_req in the MLT closure (F_conv = F_req - F_2s) -> the push is equivalent to ~0.7 % L (cfl 0.15) of heating in the MLT zone; the ledger finds ~0.25-0.4 % L there. PLAN: (1) get true face fluxes out of the sweep (rad_f2s not allocated; add a face-flux copy under e_ledger) to close L_in / L(r) / L_out; (2) build the IC as the steady state of the code's own discrete flux operator + hydrostatic re-solve; (3) only then judge physical instability.
09-19 midday. FACE LEDGER (e_ledger_shell.txt col 3 = time-integrated face luminosity from rt_face_flux; areas verified exact): L_face/L = 1.0063 at the bottom wall (the rt_bottom_flux BC ends up ~(imposed + local)/2), 1.0046 uniform over the deep radiative faces, 1.012/1.025 on the two faces under the MLT base, ~1.002 above 0.94 R, top 1.0026. The deep 1.0046 TRACKS problem/lstar (el5: lstar x1.00475 -> 1.0092) and is unchanged by a gradient-corrected IC (relax3.py / ic_rd1, el4; in a Prad/Pgas ~50 zone rho g ~ -dPrad/dr locally so rho follows the gradient and F is unchanged; the DOWNWARD hydrostatic march is exponentially unstable there, march upward). Matching lstar + bottom flux to it changes neither the heating pattern nor the momentum (5.1e30): RETRACTION 6, the L mismatch is not the driver. Origin of the 1.0046 factor not found.
USER DIRECTION 09-19: "expansion and pulsation are probably physical; find a way to make the simulation run normally". dt KILLER IN 1-D IDENTIFIED: not the top; a CAVITY under the Fe bump at 0.85-0.87 R evacuates (rho 4e-9 -> 4.8e-10 at 2.0 turnovers -> 1.6e-13 = dfloor at 2.1) at T = 1.5e5 K; the EOS T-gate keeps LTE radiation pressure in a hot cell at any density, so c_s = sqrt(4aT^4/9rho) ~ 4e9 cm/s, dt 17 s -> 0.3 s. NEW problem/cs_max [cm/s] (red_giant.cpp RgCsCeiling: density floor rho >= (10/9) e_int/cs_max^2, velocity kept, mass added tallied; default off) + diagnostic problem/rt_no_heat (split RT keeps its force, deposits no heat). Binaries: athena_v13 (rt_no_heat), athena_v14 (+cs_max); scripts r12_arm13/14.sh. Running: ad3 (adiabatic 3 turnovers), cm_3.0e8 and cm_1.0e8 (recipe + mlt_split_deposit + cs_max). default-off bitwise of v12-v14 NOT yet checked.
RESULTS 09-19 ~13:00 (1-D, 3 turnovers, recipe + mlt_split_deposit): (1) ad3 (rt_no_heat: force kept, no heat deposited) = NO exponential growth: KEr stays 2-4e36 through 2.6 turnovers (sd3: 4.8e38 at 2.0), momentum changes sign, mass constant -> THE EXPANSION IS THERMALLY DRIVEN (RT + MLT heat exchange), the hydro/force balance of the IC is fine. (dt still eroded to 0.8 s in ad3: top cells, not examined.) (2) cs_max: identical to sd3 until the cavity forms at 2.0; cm_1.0e8 COMPLETES 3.0 turnovers (dt 2.5 -> 1.0 s) but the event is violent anyway (KEr 1e39 at 2.25, 2.8e39 at 3.0; M +0.8 % then -4 %; totE -12 %); cm_3.0e8 worse (dt 0.34-0.5, M +3.7 %, totE +40 % = the velocity-kept mass injection adds KE; should add mass at REST: keep momentum, not velocity). The ceiling is a dt band-aid, not a cure for the cavity event itself. 3-D (v3d) never formed the fully evacuated cavity (convection breaks the inversion?), so the 1-D cavity may be a 1-D artefact of a physical instability.
09-19 ~13:40: cs_max now adds the mass AT REST (momentum + total energy kept); athena_v15 = everything (mlt_split_deposit, e_ledger + shell/face ledger, rt_no_heat, cs_max), default-off BITWISE vs v10 (fd15 vs fd10). 1-D rerun cr_1e8 ~ cm_1.0e8 (completes 3.0, dt 1.1 s, mass swings -10 %/+: the 1-D cavity event, accepted as a 1-D artefact). 3-D LAUNCHED with user approval: tests_r11/w3d, job 11847855 (apu 1 node 2 GPUs, 3.5 h limit), script tests_r11/r11_3d_arm15.sh, args time/cfl_number=0.15 problem/rt_top_vacuum=true problem/mlt_split_deposit=true problem/cs_max=1.0e8 time/tlim=23525 (5 turnovers; resubmitting the same command chains from the newest rst). Verified started: dt 6.45 s, 100 cycles per 72 s. GATES vs v3d: dt (v3d fell to 0.29 s), domain mass (v3d -20 %), L_out/L (lout9.py), KEr col 8 vs KEh cols 9+10, rho minimum / cavity in rt_profile. All code still UNCOMMITTED in bench/wt_he4.
COMMITTED 09-19 ~14:00 as 27e0c387 on he4-presn-global (unpushed); two over-90-column lines wrapped first (one pre-existing at the vdamp comment). The running w3d uses the athena_v15 copy made before the wrap (same code).
09-19 evening: SPHERICAL-POLAR WEDGE (user idea). New inputs/hydro/he4_presn_sp.athinput (from the cs input; theta AND phi PERIODIC: user pointed out the two theta faces of an equator-symmetric wedge have the same sin theta; polar boundary deliberately ignored for now). rad_implicit_ang (ADI) was refused on sp: now admitted for an off-axis wedge periodic in x2,x3 (conduction.cpp) and conduction_transverse.cpp splits curv (sp||cs: volume/area) from iscs (seams, open chain, cross term); UNCOMMITTED, binary tests_r11/athena_v16. No other scheme refuses sp. VALIDATION: 1-D wedge column sp1 == cs column sd3 x 0.021567 (the wedge's share of 4 pi) to 3-4 digits in M, mom, KEr through 1 turnover. SIZING from the IC column: MLT zone 0.635-0.967 R = 0.33 R but only 2.9 Hp; Hp = 25 deg (base), 17 deg (kappa peak 0.82 R), 3.2 deg (0.93 R), 0.22 deg (tau 2/3): deep cells are tens of degrees wide -> wedge 90x90 deg (theta 45-135), 256^2 (0.35 deg: 49 cells/Hp at the peak, 9 at 0.93 R, 0.6 at the photosphere), meshblock 96x32x32 because the ADI reduced system allows at most NADIB = 8 MeshBlocks along a line. Smoke (tests_r11/r11_smokesp.sh, 2 GPUs): runs, dt 2.38 s (angular cells at r_in), 8.5e5 zone-cycles/s, 4 GB/node -> 5 turnovers ~ 20 h on 2 GPUs, ~5 h on 8. w3d (global 32^2) at 1.72 turnovers still 1-D-like (KEr 1.7e38 vs KEh 2e36).
WEDGE LAUNCHED 09-19 evening (user approved): tests_r11/wedge1, job 11848971 (apu, 4 nodes / 8 GPUs, 6 h), script tests_r11/r11_wedge.sh (athena_v16, he4_presn_sp.athinput, recipe cfl 0.15 + rt_top_vacuum + mlt_split_deposit + cs_max 1e8, tlim 23525 = 5 turnovers; chains from the newest rst on resubmission; bin dumps every 0.1 turnover are KEPT). Wedge code + input committed 47f80807 (+ script commit) on he4-presn-global, unpushed. GATES: dt (smoke 2.4 s), KEh vs KEr (does convection take over from the radial mode?), mass, L_out/L (lout9.py needs the wedge's area weight, not the gnomonic one), cavity in rt_profile. hst totals are for the wedge = 0.25*sqrt(2)/2... use the solid-angle share (phi 90 deg x (cos45-cos135) = 1.5708*1.41421/(4 pi) = 0.17678) when comparing with global numbers.
09-19 night: user chose 192^2 (0.47 deg, 36 cells/Hp at the kappa peak; meshblock 96x24x24 = 64 blocks; smoke dt 3.18 s, 8.6e5 zc/s, 3.8 GB/node) and OK'd cancelling other jobs. apu was 100 % full: w3d CANCELLED at 2.16 turnovers (1-D-like to the end: KEr 3.2e38 vs KEh 4.2e36 at 2.0, dt 1.0-1.2 s, M +2.4 % from the cs_max ceiling) and the wedge took its node at once. RUNNING: tests_r11/wedge1 job 11849266, 1 node / 2 GPUs, 6 h, args = recipe + mesh/nx2=192 mesh/nx3=192 meshblock/nx2=24 meshblock/nx3=24 (overrides on he4_presn_sp.athinput, whose file default is still 256^2/32^2). Verified: 4.1 s per cycle, dt 3.17 s -> ~3.4 turnovers in this job; chain with the SAME command (r11_wedge.sh picks the newest rst; flexible 1-4 nodes, time-min 1 h).
UPGRADE JOB 11849601 queued (2-4 nodes, OLDJOB=11849266 take-over, log tests_r11/wedge1_up.log); if it starts it cancels 11849266 and continues from the newest rst; otherwise it is the chain job.
BUG FOUND 09-19 ~19:30 (RETRACTION 7: "convection switching on in wedge1" was wrong). wedge1 (192^2, 4 nodes) dropped to dt 0.68 s at 0.93 turnovers with 1.4e7 dfloor/efloor events per log interval. 0.5-turnover bin dump: ALL of KEh sat in ONE shell i=86 (0.988 R, the EOS taper ramp, T ~6e4 K = the temperature gate) at MESHBLOCK EDGES (every 24th theta cell: drho -0.19/-0.01 vs -0.10), v_h rms 8.7e5 max 3.4e6 cm/s, neighbouring shells 1e3. CAUSE: rt_rad_force's transverse Prad grad w term differences T_g(j+-1), T_g(k+-1) but rt_pre_tp filled T_g over ks..ke, js..je only -> unfilled ghost T at every block edge. FIX (two_stream_rt.hpp, committed after 47f80807): fill T_g one angular ghost wide under rt_rad_force; binary athena_v17. The cs runs take the same path: v3d/w3d "KEh growth = convection starting (x30/turnover)" and "hot voids in ~1 % of columns at 0.96-0.99 R" are SUSPECT (16-cell blocks -> 2/16 columns are block edges); 1-D arms had one block per panel with 4x4 cells: affected at panel seams too?? (not checked). wedge1 CANCELLED; RERUN = tests_r11/wedge2, job 11850256 (4 nodes, athena_v17, same args: recipe + 192^2 + 24^2 blocks), verified 100 cycles at 1.06 s/cycle, dt 3.17 s. Early dfloor counts (~3.4e6 per 15 cycles from cycle 0) are GHOST-zone c2p counts, benign. How to check a dump: tests in this session's python (bin_convert.read_binary, mb_logical -> global [phi,theta] map per shell).
09-19 ~19:50: T_g FIX CONFIRMED on wedge2 at 0.5 turnover: shell i=86 rho scatter 6.3 % -> 0.018 %, v_h rms 8.7e5 -> 3.4e2 cm/s, deep shells identical to the last digit; dt stays 3.18 s through 1.0 turnover where wedge1 had collapsed to 0.68 s => the block-edge bug WAS a 3-D dt killer. wedge2 KEh (velocity seed 1e-3, one mode per MeshBlock on sp) only creeps 3.4e32 -> 2.0e33 (0.25 -> 0.75), ratio to KEr falling 0.0026 -> 0.0010: convection will not win the race to the ~2-turnover radial event from this seed. USER agreed to the ENTROPY SEED: new problem/vpert_sp_rand (+ vpert_sp_kmax 8), a61b29a8, athena_v18: 32 random modes periodic over the wedge, with vpert_var=eint a scalar entropy pattern in the tau 20-300 window (verified in tests_r11/smokeseed: CZ-confined, rho untouched, at rest, periodic; vpert=3e-2 gives de/e rms 0.9 %, max 2.9 %). SWAP SCHEDULED: tests_r11/swap_wedge3.sh (background) waits until wedge2 (job 11850256) passes 2.3 turnovers (to see the radial event in clean 3-D), then submits wedge3 with OLDJOB take-over: same recipe + 192^2 + vpert_var=eint vpert=3.0e-2 vpert_sp_rand=true. Open choice NOT made: MLT hand-over (mlt_hold_time/mlt_ramp_down_time) so resolved convection is not held marginal by the closure.
09-19 20:20: user: "just run wedge3". wedge2 CANCELLED by take-over at 2.07 turnovers: clean 3-D, dt 3.22 s CONSTANT through 2.0 (every earlier run had lost dt by then), mass conserved 6e-5, KEr/f 3.9e37 at 1.75 (4x below 1-D sd3), KEh/KEr 1e-4 (no convection from the v1 seed), totE +1.4 %. RUNNING: tests_r11/wedge3, job 11850864 (4 nodes, athena_v18): same recipe + 192^2 + vpert_var=eint vpert=3.0e-2 vpert_sp_rand=true. Lesson: `pgrep -f <script>` also matches the shell running the pgrep command line; it killed my own tool call (exit 144). Use `ps ... | grep '[s]cript'`.
09-19 20:40: wedge4 SUBMITTED job 11851044 (flexible 1-4 nodes, separate arm, no take-over) = wedge3 + problem/mlt_hold_time=2352 (0.5 turnover) + problem/mlt_ramp_down_time=9410 (2 turnovers): the MLT closure is withdrawn linearly between 0.5 and 2.5 turnovers. Why: wedge3's entropy seed (de/e 0.9 % rms) gave KEh only ~3x wedge2 (2e33 at 0.37 turnover, v_h ~1e4 cm/s): the CZ is radiatively leaky (tau ~60 across a 3e10 cm structure, diffusion time ~minutes << buoyancy time), low-Peclet convection, growth ~1 e-fold/turnover while the closure pins the superadiabatic gradient. Note the shell-mean closure already asks only for the deficit F_req - F_cond - F_2s - F_resolved, so it hands over by itself IF the resolved flux grows.
09-19 ~22:30 RESULT OF wedge3 / wedge4 (both still running, user: do NOT cancel): convection's KEh grew exponentially (x~3 per 0.25 turnover, identical in both: the closure does NOT limit the growth; withdrawing it only feeds the radial expansion: wedge4 KEr 2.6-3.7x wedge3). The radial motion IS a pulsation but violent: outward to ~2.75 turnovers, photospheric-density gas reaches the top at 1.014 R (rho_top x26), 17 % (wedge3) / 39 % (wedge4) of the domain mass leaves through the open top, then fallback at 4e6-1.1e7 cm/s (Mach ~0.3) over 0.72-0.97 R; tot-E -33 % / -56 %; dt 3.2 -> 0.66 s; vceil ~2e6 and FOFC ~2e5 events per log interval; KEh/KEr 0.4 by 3.25. Everything after ~2.75 turnovers is contaminated by the mass loss. 1-D BC arms tests_r13 (script r13_arm.sh, athena_v19, sp column): b0 open / b_in open both ways (open_outer_noinflow=false) / b_lid: identical to 2.0; at 2.5 mass -3.4 % / -3.3 % / -2.5 %: open-both-ways changes nothing (the ghost is an extrapolation, not a reservoir) and the lid still leaks. NEW CODE (uncommitted): problem/vdamp_all_mean_only (damp only the shell mean of v1 so convection grows while the radial mode is held; athena_v19). USER IDEA = TALLER TOP with a stretched grid, fine where the photosphere can move: tests_r13/fitgrid.py (4-coeff poly refit: x1max 1.25 R = 2.964625e11, nx1 144, c = +0.992525 -0.404821 -0.372255 -1.499759, dr 3.9e8 at 1.02 R, <= 1.2e9 over 0.97-1.10 R, 2.8e9 at the top) + tests_r13/mk_tall_ic.py -> ic_he4_tall.txt (file IC + FLOOR ATMOSPHERE rho 3e-13 at T 4.1e4 K above 1.0256 R, 2e22 g; hot corona impossible: below the EOS table edge and the T-gate would give it LTE Prad). 1-D arm b_tall submitted (job 11852559).
09-19 22:55: user OK'd cancelling 4-node runs when needed. wedge4 CANCELLED (take-over) at ~3.4 turnovers (M -39 %, totE -56 %). 1-D BC arms to 4.0 turnovers: none conserves the envelope: after the fallback the domain mass GROWS (+18..+24 % at 4.0: inflow from a ghost that copies the dense top cell / the hydrostatic ghost / cs_max), so boundary tricks at 1.014 R are out. RUNNING: wedge3 (job 11850864, violent fallback phase, dt ~0.6 s), NEW tests_r11/wedge5 (job 11852628, 4 nodes, athena_v19, commit 341e6b4b) = wedge3 + problem/vdamp_all_mean_only=true vdamp_all_until=18820 (4 turnovers) vdamp_all_time=500, tlim 6 turnovers: hold the radial mode while convection grows to saturation, then release. 1-D b_tall (tall domain + floor atmosphere) started with dt 0.54 s (free-falling floor atmosphere through the refined cells?) -- check what limits dt. Lesson: a flexible 1-4 node take-over job starts on whatever is free BEFORE it cancels the old job (got 1 node); submit take-overs with --nodes=4 fixed.
