---
name: rt-source-dt-forcing
description: 2026-09-14 THE He 'INSTABILITY' IS A dt-LINEAR NUMERICAL FORCING of the closed box's organ-pipe acoustic modes (P 107/54/36/27 s = 2L/c_s/n) by the OPERATOR-SPLIT SEMI-IMPLICIT two-stream source (a delayed thermostat, lag ~dt): hestar_fecz/conv_fix at 100/300 with the fixed sweep - saturated v_rms 3.0/2.8/1.7/1.6/0.7/0.5e5 cm/s for dt 0.22/0.12/0.10/0.077/0.032/0.028 s, monotone in dt and NOTHING else (dx and nquad sit on the same curve; growth rate changes sign; harmonic selection flips; explicit source Mach 15; saturates by 200 s, not a runaway). NOT physics. Consequences: no He result trustworthy until the RT source is time-accurate (IMEX inside the RK stages, or at least the relaxation applied to the updated state); B STAR CHECKED (bstar_fecz/dtcheck, 336x8x8, no seed): same forcing, P2 = 2693 s organ-pipe mode, v_rms ~ dt^0.7-0.9, at production dt 1.5 s v_rms 1.4e3 cm/s = 0.6 % of v_MLT, deep Mach 2e-4, 100x below the FeCZ convective velocities -> acceptable floor for the interior science (30x more benign than He); nquad 1 is 5x worse (keep 2); DESIGN (scoped 09-14): the He box already hands the sweep the post-RK state (rt_use_cons=true); the forcing comes from (a) the target e_eq taken from ONE sweep of the UN-relaxed column (Jacobi overshoot in the thick interior) and (b) the per-cell semi-implicit damping (1-e^-x)/x applied to a NON-LOCAL exchange (neighbours damped differently, no cancellation; same mechanism the decad388 handover fix removed for the ramp) -> dp ~ dt x compression rate; explicit source = overshoot x stiffness = Mach 15. SUB-CYCLED LOCAL RELAXATION (rt_relax_sub/xcrit/submax, commits 8507a73e/b1c5a0e5/4cfe92d0, bitwise off, conservative, 1.25x cost) DOES NOT HELP: v_rms still linear in dt to the digit -> the local damping factor is NOT the pump; remaining hypothesis = the absorbed field A frozen from ONE sweep of the un-relaxed column (non-local lag) -> OUTER RE-SWEEP rt_outer_iter (86cc1b0b, bitwise off) makes it WORSE (fixed point diverges, ratio 0.9/pass, box transonic) -> absorbed-field lag exonerated. DISCRIMINATING ARMS: two-stream OFF (Newton lid) -> v_rms dt-INDEPENDENT to 0.03 % => the pump is INSIDE the two-stream per-cell relaxation step; handover/wall/radial solve/source assembly all cleared. CLIP EXPERIMENT (hestar_fecz/conv_fix, 11 arms): NO clip/cap/rescue is the pump (all counters zero, rt_de_max off = bitwise baseline); rt_semi_lin / rt_newton off shift 10 %, same dt spread. VERDICT: the pump is the per-cell semi-implicit factor (1-e^-x)/x applied to the NON-LOCAL exchange (neighbours damped differently -> no cancellation; same mechanism as the decad388 handover bug, now cell-to-cell); explicit is unstable, Jacobi re-sweep diverges -> NO CHEAP SWITCH. PROPER FIX = implicit column solve of the radiative balance inside each RK stage (Newton-Krylov / damped iteration with the sweep as the operator), ~1 week; also makes rg/dhj time-accurate. He investigation PARKED 09-14; B star tolerates the floor (0.6 % v_MLT). Earlier plan: sub-cycle the local relaxation re-forming Em from the running energy, A frozen per stage (rt_relax_sub/xcrit/submax, bitwise off); proper fix later = IMEX inside the RK stage (driver has impl_src/a_twid, used only by ion-neutral). NOTE red_giant and dhj use rt_use_cons=false -> they ALSO have a full stage-state lag (sweep sees w0 from the stage start): set rt_use_cons=true there after this settles. He production ON HOLD.
metadata:
  type: project
---
Supersedes the 'physical strange mode' lead ([[he-star-strange-modes]]) and the 'handover mismatch' lead
([[rt-shallow-ramp-missplit]]) as the cause of the He box mode; the sweep/cut/limiter fixes stand on their own
([[two-stream-layer-stagger-fix]]).
