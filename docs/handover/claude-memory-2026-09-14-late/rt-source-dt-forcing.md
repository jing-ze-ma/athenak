---
name: rt-source-dt-forcing
description: 2026-09-14 THE He 'INSTABILITY' IS A dt-LINEAR NUMERICAL FORCING of the closed box's organ-pipe acoustic modes (P 107/54/36/27 s = 2L/c_s/n) by the OPERATOR-SPLIT SEMI-IMPLICIT two-stream source (a delayed thermostat, lag ~dt): hestar_fecz/conv_fix at 100/300 with the fixed sweep - saturated v_rms 3.0/2.8/1.7/1.6/0.7/0.5e5 cm/s for dt 0.22/0.12/0.10/0.077/0.032/0.028 s, monotone in dt and NOTHING else (dx and nquad sit on the same curve; growth rate changes sign; harmonic selection flips; explicit source Mach 15; saturates by 200 s, not a runaway). NOT physics. Consequences: no He result trustworthy until the RT source is time-accurate (IMEX inside the RK stages, or at least the relaxation applied to the updated state); B STAR CHECKED (bstar_fecz/dtcheck, 336x8x8, no seed): same forcing, P2 = 2693 s organ-pipe mode, v_rms ~ dt^0.7-0.9, at production dt 1.5 s v_rms 1.4e3 cm/s = 0.6 % of v_MLT, deep Mach 2e-4, 100x below the FeCZ convective velocities -> acceptable floor for the interior science (30x more benign than He); nquad 1 is 5x worse (keep 2); DESIGN (scoped 09-14): the He box already hands the sweep the post-RK state (rt_use_cons=true); the forcing comes from (a) the target e_eq taken from ONE sweep of the UN-relaxed column (Jacobi overshoot in the thick interior) and (b) the per-cell semi-implicit damping (1-e^-x)/x applied to a NON-LOCAL exchange (neighbours damped differently, no cancellation; same mechanism the decad388 handover fix removed for the ramp) -> dp ~ dt x compression rate; explicit source = overshoot x stiffness = Mach 15. SUB-CYCLED LOCAL RELAXATION (rt_relax_sub/xcrit/submax, commits 8507a73e/b1c5a0e5/4cfe92d0, bitwise off, conservative, 1.25x cost) DOES NOT HELP: v_rms still linear in dt to the digit -> the local damping factor is NOT the pump; remaining hypothesis = the absorbed field A frozen from ONE sweep of the un-relaxed column (non-local lag) -> OUTER RE-SWEEP rt_outer_iter (86cc1b0b, bitwise off) makes it WORSE (fixed point diverges, ratio 0.9/pass, box transonic) -> absorbed-field lag exonerated. DISCRIMINATING ARMS: two-stream OFF (Newton lid) -> v_rms dt-INDEPENDENT to 0.03 % => the pump is INSIDE the two-stream per-cell relaxation step; handover/wall/radial solve/source assembly all cleared. CLIP EXPERIMENT (hestar_fecz/conv_fix, 11 arms): NO clip/cap/rescue is the pump (all counters zero, rt_de_max off = bitwise baseline); rt_semi_lin / rt_newton off shift 10 %, same dt spread. VERDICT: the pump is the per-cell semi-implicit factor (1-e^-x)/x applied to the NON-LOCAL exchange (neighbours damped differently -> no cancellation; same mechanism as the decad388 handover bug, now cell-to-cell); explicit is unstable, Jacobi re-sweep diverges -> NO CHEAP SWITCH. PROPER FIX = implicit column solve of the radiative balance inside each RK stage (Newton-Krylov / damped iteration with the sweep as the operator), ~1 week; also makes rg/dhj time-accurate. SCOPED 09-14 (design in the agent report; key points): the sweep is exactly LINEAR in B at frozen opacity -> the column balance F(T) = C_v (T-T*)/(a dt) - kappa rho (M - I) B(T) has an M-MATRIX Jacobian (diag dominant for any dt, Newton 2-3 iterations); any per-cell/lagged-neighbour splitting contracts at ~1 - escape ~ 0.9 (= the measured rt_outer_iter ratio). PUMPING CELLS = the thick ones (tau 4-321, ~100/134 He cells; x ~ 1e3 with near-total neighbour re-absorption). PLAN = fold the nearest-neighbour linearisation of the two-stream source (from the alp/bet transmission weights in registers, new rt_jac (m,3,k,j,i)) + the 4 kappa rho sigma T^3 sink INTO the existing radial implicit tridiagonal (ImplicitRadialUpdate, T-form; next task after the source; no reordering, no IMEX tableau), src_ex on the same RHS (dissolves rad_blend_use_2s), residual >= 2 cells away explicit, outer loop via rt_outer_iter plumbing now preconditioned (~0.05/pass); inputs rt_implicit_column 0/1/2, rt_impl_tol, rt_impl_maxit; requires rad_implicit_x1. ~250 + 120 lines, 2-3x the RT source cost. Dense Newton rejected (134x cost, memory), banded viable but worse, Newton-Krylov = fallback. TESTS: conv_fix dt scan (6x spread -> <10 %), bitwise off, grey_cons round-off, legacy grey_atm + RG restart + rad suite, bstar dtcheck floor should drop. IMPLEMENTED 09-14 on rg-box-convection (d595be5b census, 0d7e28de merged tridiagonal, 0d950ec6 sweep Jacobian + apply, a48d52e1 wiring, 4199e3e7 thick/thin split + dT cap, 57ec5814 thickness test via kc_g*rho*dx1 since tau_g is unfilled without a beam): switch rt_implicit_column (0 = bitwise off, verified), rt_impl_tol/maxit, rt_impl_tau_min 1.0, rt_impl_dtmax 0.25. M-matrix 0 violations; conservation exact (4e-16) when no cap fires. RESULTS: full-column version diverges in thin cells (E->0, the documented 'why not linearise'); thick/thin split removes the dt-linearity (spread 15 % over 3x dt) BUT v_rms 1.4-7.6x ABOVE baseline, transonic: the cap fires on 22-30 rows with |dT|/T 12-76 even in thick cells -> A/E ~ 50-300 on a RELAXED column at rest, = ASSEMBLY BUG FOUND + FIXED 515cf64d: the Jacobian had been accumulated in rt_chain, but the grey split path runs launch_grey_chain (centre-to-centre layers) -> J_ii = 0, source with no sink. After the fix (cycle 0 on the relaxed column: A/E = 1.0000 in every thick cell, dT/T ~ 1e-3, budget 4e-16, no caps): He v_rms 4.1/5.5/6.4e4 for cfl 0.15/0.30/0.45 (old 5.3e4/1.6e5/2.8e5; spread 57 % vs 6x), dF_top 20x better, all decaying, cost ~1.0x. STILL OPEN: outer passes (oit 2/3) DIVERGE (defect: thin cells likely re-apply their relaxation per pass), B-star narrow column 2.5x NOISIER (3.5e3 vs 1.4e3; suspect the thick/thin seam at tau ~10) -> OUTER LOOP defect fixed 508e5e9e (anchor re-set by the thin cells' own dT) but still NO contraction (thin nonlinear relaxation is outside the Newton system; Gauss-Seidel chase) -> keep rt_outer_iter = 1. SEAM SCAN (rt_impl_tau_min 0.3/1/3/10, blend): tau_min 10 = inert (bitwise the off numbers, criterion validated); He v_rms 6.3/5.5/10/16e4, B-star floor 4.0/3.5/1.8/1.4e3 - MONOTONE in the implicit fraction, so the B-star rise is the thick region under the single-pass linearised solve, NOT a seam; the smooth blend is inconsistent (15x worse). STATE 09-14 end: switch works as a single-pass solve (He 3x lower amplitude, 20x smaller L swing, decaying; residual dt spread 57 %); B-star floor 1.5 % of v_MLT with it on (0.6 % off) -> leave OFF for production. NEXT DESIGN SCOPED 09-14: column-wide NONLINEAR NEWTON IN u = T^4 (rt_implicit_column = 2): at frozen opacity every radiative term is LINEAR in u; only the heat-capacity term is nonlinear (monotone, concave) -> M-matrix Jacobian for any dt; the thin-cell Newton step in u IS the existing exact per-cell scalar solution (the T-form divergence was an artefact) -> no threshold/seam/dtex; inexact Newton: near-field tridiagonal (today's) as preconditioner + full re-sweep per iteration (contraction ~ 2-cell transmission in thick cells); clamp u in [u/4, 4u]; cost cuts: cache e0/alp/beta per stage (opacity frozen), per-column early exit, freeze far field after 2 iterations -> +5-10 % wall; keep the operator split (Strang if needed, no IMEX); ~250+120 lines. FLOOR DIAGNOSTIC DONE (bstar_fecz/dtcheck/impl_diag): with mode 1 the B-star floor is dt-INDEPENDENT (3.97/3.50/3.31e3 over 3x dt; off = 0.79/1.41/2.02e3 dt-linear) -> the dt pump IS removed by an implicit treatment, replaced by a larger dt-independent forcing that outer iterations make WORSE (6.6e3, 5.0e3, 2nd harmonic) -> NOT the far-field lag (b); it is (a) loss of per-cell damping and/or (c) src_ex through the dense inverse (inseparable, no switch; src_ex is inside res_g). Budget closes. => mode 1 is a DEAD END (inconsistent solve); the exact block-tridiagonal column (mode 3) is the path; its B-star GATE: floor <= 1.4e3 (the off value). If mode 3 fails that gate, the suspect is the operator split itself -> Strang. JIANG 2021 REDUCTION SCOPED 09-14 = THE RECOMMENDED DESIGN (rt_implicit_column = 3): intensities as unknowns -> in a column the fully implicit system (I_+1, I_+2, I_-1, I_-2, e_gas per cell) is EXACTLY block-tridiagonal (5x5 blocks) -> block Thomas = exact coupled solve, no Jacobi/GS (Jiang's 1000-iteration pathology is multi-D only); thin cells carry the far field exactly. HYBRID: keep the exact exponential layer coefficients (e0, alp, bet at two_stream_rt.hpp ~2041-2120) as the FV flux function (pure FV upwind is 36 % wrong in transmission at dtau 1 -> tens of % in F_top; Jiang's tau-throttled HLLE is a patch for that); M-matrix; Newton 2-4 iterations on B(T); I/(c dt) diagonal kept (0.15 % regularisation), no RSLA; cost 3-6x the RT source (~+10-20 % wall before cuts); grey thermal channel only (bands explicit); vertical handover no longer needed for stiffness (keep deep for FP saturation, transverse operator everywhere). ~400 new lines (two_stream_column_implicit.hpp) + 150 modified. Supersedes the u-form Newton (option A) and mode 1. MODE 3 BUILT 09-14 (a8ae36c2, 49689cc1, 5025dd32, 3b87eff1, 16ba77d6; src/utils/two_stream_column_implicit.hpp; unknowns D_q at the lower face, U_q at the upper face, b = sigma T^4/pi; 5x5 block Thomas, one thread per column; exact e(rho,T) energy row with table c_v per iterate; e0/c_in/c_out cached; early exit): bitwise off; 3 Newton its, no clamps, M-matrix holds, EOS round trip 1e-15, budget 5e-9, grey_cons unchanged (4e-6 F); cost 1.8x/cycle in 3-D. RESULT: dt-dependence REMOVED COMPLETELY (B-star floor 5 % over 3x dt) BUT the residual dt-INDEPENDENT forcing is LARGER: B-star floor 2.2e4 (mode 0 1.4e3, mode 1 3.5e3); He spread 1.7x, period -> fundamental, Mmax 0.74; at rest the exact solve moves e by 6 % at tau 4 (per-cell vs exact increment differ 20 %), emergent flux 2 % below the sweep. INTERPRETATION: each step of exactness makes the box modes ring harder -> the standard scheme's small floor is largely ITS OWN numerical damping ((1-e^-x)/x >> physical radiative damping at tau 100-300); the driver is SPATIAL (dt-independent): suspects = the two-stream/handover/conduction split (test: two-stream owns the whole column, bottom flux on it, conduction transverse only) or a weakly damped genuine box mode (test: damping rate of a kicked column with RT frozen vs on). NOT done: I/(c dt) term (intensities not evolved). DISCRIMINATING ARMS DONE (bench/rtcol3_disc, 09-14): (E1) WHOLE-COLUMN two-stream (rad_tau_lo/hi 1e5/1e6, new problem/rt_bottom_flux = true imposes rad_flux_inner on the sweep's lower boundary, 5b3c763d + 298d416f, budget-clean Ftop/F_bot 0.9999) collapses the STANDARD scheme's forcing: B-star floor 1.41e3 -> 4.1e2 (3.4x), He v_rms 1.61e5 -> 1.23e4 (13x), dF_top 170x, growth -> decay; and with no ramp the 30 % heating loss has no place to occur. Mode 3 does NOT collapse (B star 2.2e4 -> 3.0e4; He transonic) -> mode 3's ring is not the handover. (E2) kicked-fundamental damping IDENTICAL for mode 0 and mode 3 (0.8/period) -> not a damping deficit; the drive differs (zero-seed floors 1.5e3 vs 9.8e3). Remaining suspect for mode 3 = the RT/hydro operator split (Strang/IMEX untested). NEW PRODUCTION CONFIG = mode 0 + whole-column two-stream + rt_bottom_flux + rad_blend_transverse = false (NEW switch being added: the transverse RKL1 weight must not follow the vertical blend) + RKL1 horizontal; radial conduction inert. WHOLE-COLUMN dt SCAN DONE (rtcol3_disc/dtscan_whole): dt exponent -0.1 (He) / -0.2 (B star) = NONE; residual 8.7e3 cm/s (He, 0.07 % c_s) and 4.1e2 (B star, 0.17 % v_MLT) = dynamically irrelevant; Ftop/F_bot 1.0000. nquad 1 is 21 % worse (keep 2). B-star cfl 0.45 died of a hydro positivity collapse (unrelated; production cfl 0.3). DEEP-FLUX CHECK (bench/bstar_fecz/gate_deep): the sweep's FLUX is within 0.06 % of exact and 0.1 % of the diffusion flux at dtau/cell 20-60, no degradation to 134 (wall face 1.3 % = imposed flux vs the column's own gradient); BUT the per-cell HEATING (A - E = 1e-5 of F per cell in the FeCZ) from the sweep is 8x noisier than the diffusion operator's (17 F_conv/H_p cell-to-cell, 1.8 after 9-cell smoothing) -> the FeCZ heating should be owned by the DIFFUSION operator: preferred config = two-stream down to a handover BELOW the FeCZ (rad_tau 3000/5000), conduction owning the FeCZ + wall, PROVIDED the standard scheme's ramp loss does not recur there (1-D test bench/bstar_fecz/ramp_deep in progress); fallback = whole column (dt-clean, Ftop = F_bot, noisier FeCZ heating). He project UNPARKED (whole column is quiet). USER DIRECTIVE 09-14: IF mode 3 proves better in every aspect (pending: the relaxed-IC test bench/rtcol3_relaxed, then Strang), ACCELERATE IT SIGNIFICANTLY: profile first (Kokkos timers on the B-star narrow box); cheap wins = 2 Newton iterations by residual, freeze c_v within the stage (EOS inversion only at the apply), factorise the transport blocks once per stage (opacity frozen; energy diagonal = rank-1 update), once per cycle instead of per stage; structural win = parallel cyclic reduction across the column with a thread team (336 serial steps -> ~9 parallel) instead of one-thread block-Thomas. Target: 1.8x -> ~1.2x per cycle. Commits d595be5b..ebf5d180 on rg-box-convection. B star tolerates the floor (0.6 % v_MLT). Earlier plan: sub-cycle the local relaxation re-forming Em from the running energy, A frozen per stage (rt_relax_sub/xcrit/submax, bitwise off); proper fix later = IMEX inside the RK stage (driver has impl_src/a_twid, used only by ion-neutral). NOTE red_giant and dhj use rt_use_cons=false -> they ALSO have a full stage-state lag (sweep sees w0 from the stage start): set rt_use_cons=true there after this settles. He production ON HOLD.
metadata:
  type: project
---
Supersedes the 'physical strange mode' lead ([[he-star-strange-modes]]) and the 'handover mismatch' lead
([[rt-shallow-ramp-missplit]]) as the cause of the He box mode; the sweep/cut/limiter fixes stand on their own
([[two-stream-layer-stagger-fix]]).

MODE-3 RELAXED COLUMN (09-14 late; bench/bstar_fecz/relax1d_m3, ic/ic_fecz_rt_relaxed_m3_taper.txt, 1e5 s, 2.0x cost with
uform): gas budget 0.999998 but the sweep's Ftop = 0.9715 F_bot steady (Fcut 0.997), and the THIN REGION OVERHEATS (T x1.96 at
tau 0.01 = 43 kK vs ~24 kK grey RE; rho x2.2) while tau >= 100 moves 1-9 % -> SUSPECTED MODE-3 BUG: its top boundary was
implemented as the 'frozen unresolved-column intensity' and may ignore rt_top_vacuum (the box top is vacuum) -> fictitious
illumination from above (agent checking). Do NOT use that column until resolved.

MODE-3 CLOSE-OUT (09-14 ~23:30, bench/rtcol3_relaxed): top boundary CORRECT (honours rt_top_vacuum; Ttop 23.6 kK - the '43 kK'
was a misread, retracted); relaxed-IC hypothesis FAILS (B-star floor 2.2e4 -> 2.1e4, still 15x mode 0; He worse, does not settle);
cycle-level Strang (8a2336eb, rt_strang, user_split_func + Hydro::RTStrangSplit, bitwise off) makes it WORSE (2.1x at cfl 0.3) and
restores dt dependence -> the RT/hydro split is NOT the driver. REMAINING SUSPECT: mode 3's own steady 2.6 % cut-to-top flux
deficit (Ftop 0.972, Fcut 0.998 on the relaxed_m3 column; gate A2's 2 % / 6 % at tau 4) = a thin-region assembly inconsistency
between its energy row and its transport rows (diagnosis running). Mode 3 stays EXPERIMENTAL; production candidate = standard
scheme + rt_ali_diag (arms pending). build_hip_rtd was relinked with rt_strang (default off, bitwise).
