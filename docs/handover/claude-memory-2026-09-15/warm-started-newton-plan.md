---
name: warm-started-newton-plan
description: WARM-STARTED NEWTON TRIED 09-15 (rt_impl_warm 0/1/2, uncommitted in wt_m3acc, bitwise off): SAFE BUT NO GAIN - the Newton already sits at its floor it_mean 2.0 (one pass to solve, one to confirm); the warm iterate cuts |db/b| 10x but not the pass count; freeze2 impossible without a stage index. Column speed now needs a cheaper pass, not fewer passes
metadata:
  type: project
---

Target: mode-3 column solve (RTCol3::Solve / RTCol3TeamSolve in wt_m3acc src/utils/two_stream_column_*.hpp), currently 2-3 Newton passes per stage at tol 1e-8; the passes are the cost (cvfreeze arm: assembly 1.5 %). Plan: (1) warm start = initial iterate extrapolated from the previous stage's converged column (store the converged b/T per column; at the next call start from it or a linear extrapolation in time) instead of the entry state; (2) frozen Jacobian on RK stage 2 (reuse the stage-1 factorization, one corrector pass); (3) count passes per call (it_mean is printed in the rt_col3 stats) - success = it_mean -> ~1-1.5 with the SAME converged answer (tol unchanged) -> results equal to roundoff, not bitwise; gate with analyze.py G3 numbers (Ftop 1.00000, floor v_rms, Mmax, P_nyq) + He Q1p2 fluxes. Expected 30-40 % off the column = ~20 % of the cycle. Radiation-to-hydro parity is NOT reachable (would need 10x); the only large-factor route is horizontal coarsening of the columns in the diffusive region (physics approximation, separate validation). Do NOT start before the B-star chain (11710002) passes its first turnovers and the He chain is swapped to 76b42ca8. See [[bstar-prod-cost-profile]], [[rkl1-halo-every]].

RESULT 09-15: implemented as shared helpers RTCol3WarmStart/WarmStore (both paths), guard band [0.25,4] b0 with a fallback counter (stat slot 21, nwarmfb); build_hip_warm md5 228a3713; W0 bitwise = G3g; W1/W2 gate numbers within the mode-3 scatter (v_rms 741/851 vs 861), Ftop 1.00000, it_mean 2.22/2.02 vs 2.08, zone-cycles/s unchanged (2.54-2.60e5); He Q1w1/Q1w2 = Q1p2 fluxes, cpu 101/100 vs 101 s. A residual-only exit would not fire either (the warm iterate is ~1e-3 off, tol 1e-8). Frozen Jacobian on stage 2 needs the stage index plumbed into picket_fence_two_stream_RT and 25 slots/cell of factorization storage - the only remaining lever inside the column besides the 5-unknown block arithmetic itself. Leave uncommitted unless wanted as a switch.

FROZEN JACOBIAN STAGE 2 TRIED 09-15 (rt_impl_freeze2, worktree bench/wt_fj detached at 76b42ca8, UNCOMMITTED, 652 lines): stage index via pgen->srcs_stage/srcs_bdt_next set by the 3 calling tasks; factors built at the NEXT stage's bdt (a stage-1-bdt operator contracts only 0.5 per pass under rk2); 20 Reals/cell stored (+313 MB per production block); both solvers; J0 bitwise = T0; J1 correct (Ftop exact, 0 fallbacks) but 3 % SLOWER on the B star (2.96e5 vs 3.05e5 zcs; it_mean 2.4/2.2 vs 2.2/2.1), +4.6 % on the 1-D He column. The per-pass cost is the formal-solution sweep (step 4a), not the factorization -> the column is at its floor for pass-count tricks. NOT worth committing; wt_fj kept as the record (remove with git worktree remove when done).
