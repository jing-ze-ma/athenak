---
name: bstar-photosphere-checkerboard
description: B-star prod_m3 (mode 3) photosphere carries a STATIONARY DIAGONAL PLAID (crossed +-45 deg wave trains, lambda ~10 cells, v_z +-2.9 km/s, T' +-3 %, NOT a Nyquist checkerboard), present from turnover 1, saturated, confined to tau < 3 where the transverse RT is tapered off; mode-0 prod_taper clean; CURE FOUND: rad_tr_tau_lo/hi = 1.0/3.0 removes it entirely at NO cost (bench/bstar_fecz/plaid L1); the plaid also inflates Ftop/Fbot by 0.6 %
metadata:
  type: project
---

Seen 09-15 in the science-page maps (surface_vz.png, tprime_slices.png, vslice.png): (pi,pi)-type diagonal alternation over the whole 216x216 plane at i = 314 (tau 2/3), absent at tau 10. It sits where the transverse RT operator is off (rad_tr_tau 3/10) and inside the EOS taper band (rho 1e-8..1e-9): columns decoupled, per-cell two-stream source + rt_rad_force. The gate could not see it (8 cells wide, 1 turnover; dT_tau1 7.5e-6 there). Suspects: (a) decoupled columns with the horizontal RT off above tau 3, (b) rt_rad_force per cell, (c) the EOS taper handover, (d) an odd-even hydro mode in the low-density lid. Convection below: one dominant downdraft near x = 60 Mm at 8 turnovers, KE still growing 10x; box 4 H_p wide - re-render with per-depth scales running. Check prod_taper (mode 0) for the same. See [[bstar-prelim-science-page]], [[rt-thin-region-lte-prad]], [[cs-vertex-limiter-clipping]].

prod_taper (mode 0, t = 2e5 s = 20 turnovers, analysis/obs_maps.png): NO grid-scale checkerboard at tau 2/3; instead a BOX-SCALE DIAGONAL organisation (dF/F +-0.2, dT/T +-0.06, v_r +-20 km/s in two diagonal bands = the longest wavelength of the periodic square) with small-scale ripples. So the checkerboard is NEW with the mode-3 production (mode 3 / fixed bottom flux / PCR / halo), while the box-diagonal large-scale organisation is common to both and is the 4-H_p periodic box constraining the flow. prod_taper surface v_r +-20 km/s at 20 turnovers vs prod_m3 max 12.7 (checkerboard-dominated) at 8.

DIAGNOSIS 09-15 (scratchpad checker/): Nyquist fraction <= 4e-7 (not a checkerboard); 70-91 % of the horizontal power in the 12 <= |k| <= 34 annulus, diagonal/axis ratio 10-25, four 45-deg lobes at |k| ~24 (lambda 9-13 cells); band rms v_z 2.9 km/s / T' 3 % at tau 0.7 in every dump from the first (saturated, v and T together); depth: band power 0.01 (tau 35) -> 0.18 (10) -> 0.45 (5.8) -> 0.72 (tau < 3) = tracks rad_tr_tau 3/10, not the EOS rho taper; flux map rms 8 %, band fraction 0.91 (light curve unaffected: zero-mean high k); prod_taper (mode 0, same rad_force and EOS taper) clean at all 20 dumps -> refutes the force and EOS taper; mechanism = whole-column implicit two-stream + no horizontal RT above tau 3 = radiatively decoupled columns, hydro alone couples them and selects the 10-cell diagonal ring. TEST RUNNING (bench/bstar_fecz/plaid, 336x64x64 box, 1.2e4 s): rad_tr_tau 3/10 (control), 1/3, 0.3/1, 0/0, and rad_force off.

PLAID TEST 09-15 (bench/bstar_fecz/plaid, 336x64x64, 2 GPUs, tlim 8e3 s): L0 (3/10, production) reproduces the plaid (band v_z 2.81 km/s, T' 3.1 %, diag/axis 26, e-fold ~900 s from a 0.03 km/s seed, saturated by 6000 s, Ftop/Fbot 1.0059); L4 (3/10, rt_rad_force off) 2.24 km/s -> the force is not the driver; L1 (1/3) 0.10 km/s, isotropic, Ftop/Fbot 1.0001, 126 ms/cyc vs 130 (FREE: the plaid itself was stiff); L2 (0.3/1) same cure at +13 %; L3 (off) +30 %. Deep planes bitwise-similar across arms. => the production's Ftop/Fbot 1.008 excess is largely the plaid. ACTION: restart prod_m3 from its newest rst with hydro/rad_tr_tau_lo=1.0 rad_tr_tau_hi=3.0 on the command line (keys exist in the input; only the lid physics changes; deep convection continues) - user decision.

APPLIED 09-15 07:55 (user go): prod_m3 restarted from rst 00011 (t 1.1e5 = 11 turnovers) with the override on both srun lines and rad_tr_tau 1/3 in the input; chain 11711199 -> 11711222 -> 11711223; the first 11 turnovers carry the plaid in the lid (deep convection unaffected); expect the plaid to decay after the restart - check the flux-map band power at the next dump.

RESTART CHECK 09-15: clean - first post-restart hst row at t 110100 has identical dt (1.49123), KE 3.3424e33 vs 3.3469e33 (0.13 %, the lid), E 3e-8 off; the hst files carry 2 DUPLICATED rows (t 110100, 110201.5 from the cancelled link) -> dedupe by keeping the last occurrence in analysis. The taper change produced a lid TRANSIENT, not a kick: Ftop/Fbot 1.008 -> 1.036 at the first row, decaying 1.032, 1.028, 1.024, 1.020 over 400 s (the plaid's stored structure radiating away under the new horizontal coupling); expect ~1.000 after a few thousand seconds (plaid test L1: 1.0001).

CONFIRMED GONE 09-15 (rt_surface.bin after the restart): band fraction 0.78 -> 0.002 with e-fold 1.6e3 s, below 0.15 by t 1.121e5 (2e3 s after the restart); diag/axis 22 -> ~4.3 (a residual diagonal preference from the box geometry remains; prod_taper's obs_maps also showed box-diagonal organisation); map rms/<F> now 1.6-9.3 % record to record (low-k convective/pulsation signal); <F> 4.267 -> 4.24e13. Ftop/Fbot settled 1.036 -> 1.005-1.006 within 2e3 s (Ttop rms 1.6-2.3 %).

TAPER 1/3 RUNAWAY 09-15 ~09:05: after the restart the TOPMOST ROW (i=335, tau 3e-3, rho 2.6-5x dfloor) ran away thermally from t 1.26e5: top-row T rms doubling every ~100 s (8e3 -> 4.5e5 K by 1.287e5), FOFC 126 hits in one 15x15 patch at i=335 only, dt 1.497 -> 0.52 and falling; KE decaying, flux untouched. Mechanism: with the taper at 1/3 the layers 1 < tau < 3 got lateral coupling but the rows above tau 1 (incl. the top row) have EXACTLY ZERO horizontal conductance and no lateral heat sink; a Prad-dominated near-vacuum top cell runs away (rt-thin-region-lte-prad family). With 3/10 the whole lid was uncoupled and the plaid formed instead. FIX: transverse operator EVERYWHERE (rad_tr_tau_lo/hi = 0/0 = taper off; plaid test L3 ran clean and plaid-free, +30 % cost, 158 substages/call) - restart from rst 00012 (t 1.2e5, pre-runaway). submit.sh + input prepared with 0/0 (backup submit.sh.taper13); ADI lod2 would make the everywhere-operator cheap (stiffness-independent) - for a later run.
APPLIED 09-15 ~09:15: chain 11711557 -> 11711558 -> 11711559 from rst 00012 with rad_tr_tau 0/0 (operator everywhere). Watch: dt back to 1.497 and steady, Ftop/Fbot ~1.000, top-row T rms (user.hst Ttop2-Ttop^2) not growing, substages/call ~150, ms/cycle ~+30 %.
