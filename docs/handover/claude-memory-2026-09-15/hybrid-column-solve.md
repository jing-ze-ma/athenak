---
name: hybrid-column-solve
description: Hybrid mode-3 column (rt_col3_hybrid_tau, worktree bench/wt_hyb from 91d895de, UNCOMMITTED, Thomas path only): diffusion (1 unknown/cell) below tau_h, two-stream (5) above, one Newton system with implicit interface coupling; EXACT (|dT/T| <= 1e-12 vs the whole column, telescoping 1e-9), 1.28-1.46x on Thomas; needs the PCR port to beat production (PCR whole column 71 ms vs hybrid Thomas 138)
metadata:
  type: project
---

Design: per column isp = first cell with tau < tau_h (clamped [ic+2, ie-2]); deep cells [ic, isp-1] carry b only with F = kflx dB/dtau (kflx = 2 sum w_q mu_q = 4 pi/3 for 2-pt GL, so rt_bot_flux reproduces bitwise at the deep bottom); thin cells [isp, ie] the unchanged 5-unknown two-stream; interface: the thin lower BC = the deep limit U_q = b_f + mu_q dB/dtau with b_f interpolated and dB/dtau implicit (5x1 column into the thin rows), and the deep top-face flux = the two-stream net flux at that face (1x3 row on D0, D1, b at isp) -> telescope_rel 6e-10..2e-9 (whole column 5.7e-8). Variable-block Thomas (scalar deep, 5x5 thin, spliced at isp). Kept: Newton, tol, clamps, stats, bot_flux, ex_iter (deep source = diffusive flux divergence). pcr + hybrid = FATAL for now. Binary build_hip_hyb md5 2f9348e9. Gates: 1-D B star Ftop identical to 10 digits in all arms, |dE/E| <= 5e-12 at every tau; zc/s 3086 -> 3836 (tau_h 1000) / 4281 (100) / 4498 (30); He 1-D tau_h 100: Ftop to 3e-6, 1.18x; 30: 1.37x; gate box Thomas: 176 -> 138 ms/cyc (1.28x), Ftop 1.00000, v_rms in scatter (the box is not bitwise between H0 and T0t: compiler-level ULP seeds, 1-D bitwise). Recommended tau_h = 100.
**How to apply:** port the deep scalar tridiagonal into the PCR team solve (RTCol3TeamSolve: partitioned scalar Thomas over the lanes' deep sub-ranges + the existing 5x5 PCR over the thin sub-range, spliced at isp) AFTER the parallel reduced-system solve (wt_red) lands, since both edit two_stream_column_partition.hpp; expected column cost -30..-40 % on top of PCR. See [[rt-acceleration-inventory-0915]], [[warm-started-newton-plan]].
