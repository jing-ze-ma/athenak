---
name: restart-wb-cache-phase-bug
description: Box restarts FIXED to roundoff 09-15 (commit 7ca9c57f rg-box-pcr: WB cache invalidated on every rst write + rst written after NewTimeStep + restored dt kept): first post-restart row bitwise in time and E, KE 3e-10 after 400 rows (was 8e-2); a 1-ULP dt residual remains (chase running). Originally: the dynamic WB background cache (wb_cache_every=10) is rebuilt from the restart state while the straight run carries one up to 9 cycles older (KE jump 5e-4); plus the first dt is recomputed from the post-split state (2.5e-10). Pre-existing; fix = invalidate the cache on every rst write + reuse the stored dt
metadata:
  type: project
---

Found by the restart gate on the B-star candidate (final_gate/R_straight vs R_cont): first post-restart cycle differs in KE by 5.3e-4 rel, E 1.9e-11, dt 2.5e-10, time 2.8e-13; chaotic divergence after. Code review: (1) hydro_fluxes.cpp:167-176 rebuilds wbq0 at stage 1 when ncycle % wb_cache_every == 0 or !wb_cache_built; wbq0 is not in the rst (restart.cpp writes u0, b0, wtemp, time/dt/ncycle); 5c0b98e4 fixed only the zero-background half (hydro.hpp comment), so a restart at a non-multiple of 10 cycles rebuilds from a newer state than the straight run uses -> momentum imbalance, KE first. This is the 'mom1 1e-7 residual' left open with 929d752d. (2) driver.cpp: the rst is written before Mesh::NewTimeStep and the restart recomputes dt in Driver::Initialize from the post-split state, while the running code used the in-stage dt. Everything else (mode-3 workspace, ALI, RKL1 counts, potential, BCs) is restart-clean.
**How to apply:** fix = set wb_cache_built=false in the running process whenever a restart file is written (both runs then rebuild from the same state) and store/reuse the next dt; gate with the arm_rbg.sh / arm_rst_rbg.sh pair (must be bitwise incl. the time column). Old production binary shares the defect (bisect pending). See [[wb-restart-cache-bug]], [[box-restart-bug-fixed]], [[bstar-regate-2026-09-16]].

FIX 7ca9c57f (restart.cpp end of WriteOutputFile: wb_cache_built=false for hydro+mhd; driver.cpp: rst outputs moved after Mesh::NewTimeStep via an out_is_due lambda, Initialize keeps the restored dt when res_flag). Gate Rx_straight/Rx_cont (binary athena_pinned_rstfix md5 8b1bcb99): time 3.6e-15, dt 1.1e-13 (1 ULP at the first row), tot-E 1.1e-14, KE 3.1e-10 max over 400 rows; straight run unchanged before the first rst write, changes after (cache invalidation) as expected. Bisect: the old production binary a13cfd16 had the identical defect (KE 5.28e-4), fix+thomas == new+thomas bitwise -> PCR and the flux fix exonerated. Production candidate after this = build_hip_prod7ca9c57f.

1-ULP RESIDUAL EXPLAINED 09-15: the rst stores/restores the next dt bitwise; the residual comes from driver.cpp:851 InitBoundaryValuesAndPrimitives running ConToPrim on the restart path, and the tabulated-EOS inversion is NOT idempotent (eos.hpp:428 warm start zg = log10(tguess*temp_cgs) round-trips wtemp through Pow10/log10, 1 ULP off; SolveLog eos_table.hpp:350-380 always takes one update) -> w0/wder 2e-15 off, dt 5e-16. Exact fix = store w0/wder in the rst and skip that ConToPrim (format change), or cache ltk instead of T + early return in SolveLog. DEFERRED: restarts are exact to roundoff now; not worth a format change for production.
