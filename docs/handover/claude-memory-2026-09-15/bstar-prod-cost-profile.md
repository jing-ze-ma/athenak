---
name: bstar-prod-cost-profile
description: B-star prod_m3 kernel profile 09-16 (Kokkos kernel timer, 1 production GPU): column solve rt_col3 56 %, bvals 21 %, RT sweep 9 %, hydro 6 %; PCR32 halves the column (1.39x), Newton ~48 % of the column
metadata:
  type: project
---

bench/bstar_fecz/m3pcr/prof (jobs 11709182, 11709210), kokkos-tools kernel timer at bench/kokkos-tools/profiling/simple-kernel-timer/kp_kernel_timer.so (prints to stdout). p_prod = prod_m3 input shrunk to one production GPU (nx2 108, nx3 54, same cell size): 367.5 ms/cycle, rt_col3 102.9 ms x 2 stages = 56 %; bvals+MPI buffers 21 %; explicit RT sweep 9 %; hydro 5.7 %; radial conduction 3.6 %; transverse RKL1 3.6 %. rt_col3 is ONE fused par_for (assembly+Newton+solve), so inside it only the arm differences split it: Newton iterations ~48 % of the column (maxit 2 arm), Jacobian/cv assembly ~1.5 %. PCR32 halves the kernel (50.8 vs 104.6 ms/call) -> ~264 ms/cycle, 1.39x; PCR32 + looser Newton (tol 1e-6) would be ~1.7x. Profiling overhead 8.5 % wall.

**How to apply:** speed order = PCR32 (gate G3p) > Newton tolerance 1e-6 (gate G3q, 11709219) > one column solve per cycle instead of per RK stage (a scheme change, not tried) > bvals packing (21 %, untouched). See [[pcr-timing-result]], [[bstar-column-pulsation]].

STRONG SCALING 09-15 (same 4-block mesh, binary 995ab40b): 2 ranks x 2 blocks 380-390 ms/cycle, 4 ranks x 1 block (2 nodes) 376 ms/cycle -> efficiency 0.52, NO speedup from more GPUs (something latency/launch-bound dominates; the RKL1 halo count is the suspect). 8-node option DEAD. AND the 4-rank run DIVERGES from the 2-rank run inside cycle 0 (stage-2 rad_implicit_ang planes needed 3012 vs 2942, cycle 1 max z_i 4225 vs 3015): the result depends on block-to-rank placement -> MPI-vs-intra-rank path bug, bisect (old final / pcrfix2 / 995ab40b at 1/2/4 ranks) + code read running. Production = 1 block per rank. Chain stays HELD.

E1 09-15 (transverse operator OFF: rad_implicit_ang=false rad_sts_perplane=false rad_cap_ang=0 rad_tr_tau_lo=1e30 rad_tr_tau_hi=1e31): noang2 311 ms/cycle, noang4 186 -> scaling efficiency 0.83; 2 and 4 ranks agree to roundoff. With the operator: 18 % of the cycle at 2 ranks, 50 % at 4 ranks (inter-node), AND the 4-rank run is WRONG (Ftop 1.33e12 vs 4.21e13). The decomposition dependence is entirely in the RKL1 exchange cycle (conduction_transverse.cpp:470-476, ~49 InitRecv/pack/spin-recv/clear rounds per stage, repeated tags; suspect: requests not drained -> substage j's message matched by substage j+1's receive; invisible at 2 ranks). Window exonerated on paper (symmetric, global max). Pending: d4_final (old binary: pre-existing?), d4_pcrfix2, E2 (rad_tr_window=false), drain read.

BISECT 09-15: PRE-EXISTING. Old production binary (Thomas, no PCR, no window) blows the implicit angular source on 4 ranks x 2 nodes too (max|de| 2e8, |sum V de|/sum V|de| -> 1) at cycle 1 stage 4; pcrfix2 at cycle 1 stage 3; 995ab40b at cycle 0 stage 1 -> timing-dependent = a race in the per-substage exchange cycle, inter-node only (1 rank x 4 blocks == 2 ranks x 2 blocks == 2 ranks on 1 node bitwise). prod_taper (the finished 20-turnover production) ran 2 ranks on ONE node -> unaffected; the held 4-node chain would have been the first run through the broken path. max|dT*|/T* is a per-rank diagnostic (rank-count dependent even for bit-identical states). Arms m3pcr/prof/{d1_new,d2_*,d4_*,noang*}.

E2 09-15 (switch <hydro>/rad_tr_window, build_hip_trwin md5 1327a8a9, uncommitted): window OFF at 4 ranks x 2 nodes restores cycle-0 call 1 to the 2-rank value -> the windowed exchange IS broken inter-node despite the symmetric-range argument; a SECOND, pre-existing defect remains (call 2 blows, t@60 = 24.6). Drain read: InitRecv/Isend/MPI_Test-all/ClearRecv+ClearSend MPI_Wait-all is correct on paper; same-rank path = direct write into the neighbour's recvbuf, fence at bvals_cc.cpp:539 before Isend. Hydro halo over the same code is fine inter-node (noang4). Running: X1 2 ranks on 2 nodes (inter-node vs rank count), X2/X3 host-staged MPI (UCX env), X4 HSA_XNACK toggle.

X-TESTS 09-15: X1 2 ranks on 2 nodes GPU-aware = bitwise correct (NOT inter-node transport per se); X3 4 ranks x 2 nodes with host-staged MPI (OMPI_MCA_accelerator=^rocm, UCX_MEMTYPE_CACHE=n): cycle-0 explosion gone (first 3 lines = ref) BUT t(60) 82.0 vs ref 89.8 -> not fully clean either; X4 HSA_XNACK=0: irrelevant (wrong). Diagnosis: device-buffer race in the hand-rolled substage exchange loop: RecvAndUnpackCC launches the unpack kernel async, the next InitRecv re-posts Irecv into the same device recvbuf, a neighbour ahead lets the NIC overwrite it while the unpack still reads; hidden with 2 blocks/rank (local copies stream-ordered) and by a whole stage of kernels in the task-list path. Fix under test: Kokkos::fence() after RecvAndUnpackCC completes at every exchange site (build_hip_fence, arms F4/F2). Host-staged MPI = stopgap at zero cost (378 ms/cycle).

DECOMPOSITION SHAPE 09-15 (S22 2x2 blocks with 8 horizontal neighbours vs S14 1x4 slabs with 2, 4 ranks x 2 nodes, full 336x216x216 mesh, 60 cycles): 403 vs 408 ms/cycle, bitwise identical physics -> the per-swap cost is the round trip + launches + fences, NOT the message count. Slabs and the edge-message drop are DEAD levers; only FEWER SWAPS help (halo_every = several substages per exchange) and, for He, fewer substages. Per cell: 1.03e-7 ms/cell at 2 blocks/rank vs 1.32e-7 at 1 block/rank (F4).
