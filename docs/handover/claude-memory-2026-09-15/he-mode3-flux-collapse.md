---
name: he-mode3-flux-collapse
description: He mode-3 flux collapse SOLVED 09-16 - mode 3 never received rt_bottom_flux (local Planck gradient instead); fix committed 1c45e532 on rg-box-convection, unpushed, Q1f Ftop/Fbot 0.998, E/E0 1.021; mode 0 bitwise unchanged (G0f)
metadata:
  type: project
---

bench/hestar_fecz/qual_m3, 300 time units each: Q0 (mode 0) Ftop/Fcut 0.956, E 0.996. Mode 3: Q1 0.030 / E 0.363; Q1b (rad_x1_uform=false, kiter 1) BITWISE identical to Q1 -> those switches do nothing under mode 3, so the control tested nothing; Q2 (EOS taper + rad_rad_force) 0.029; Q3 cfl 0.15: 0.081 / E 0.449; Q4 cfl 0.45: 0.009 / E 0.323. Collapse is monotone from t=0 (Q1 ratio 0.88, 0.73, 0.55 at t 0.55, 1.0, 1.5) and scales with dt -> an O(dt) drain in the mode-3 column apply, not a steady-state error. Newton healthy (it_max 3, resid 1e-12) except Q2 (clamps, Ftop_impl/Ftop_sweep 0.76-1.40). B-star gate G3 showed no such drain (Ftop/Fbot 1.00004) - the difference between the columns (He much thicker/kappa-unstable) is the lead.

ENERGY BUDGET (Q1, hst, F_bot = rad_flux_inner 2.475e15, A 7.03e12): dE/dt = -A*Ftop exactly; residual vs A(F_bot-F_top) = -1.0 F_bot*A at t 5, 20, 100 -> the injected bottom flux is NEVER deposited in the gas under mode 3 on He; Fcut/F_bot pinned at 1.0000 (8 digits, so it is the imposed value, not a measurement); Ftop/F_bot 0.73, 0.12, 0.05, 0.03 at t 1, 5, 20, 100. Q0: residual < 2 %. user.hst has NO Fbot column (cols 4/7 are squares of 3/6). Script scratchpad he.py.

CAUSE + FIX: RTCol3::Solve formed dbdtau from Bb(ic)-Bb(ic+1) only (two_stream_column_implicit.hpp:512); the explicit sweep's override `if (bot_flux>0) dbdtau = 3 bot_flux/(4 pi)` (two_stream_rt.hpp:2082) had no mode-3 counterpart. Fix: member RTCol3::bot_flux, set in the mode-3 setup, override after the dbdtau block. Binary wt_rgbox/build_hip_botfix (md5 03dba9f0). Q1f (fix, thomas): Ftop/Fbot 0.943, 0.996, 0.996, 0.998 at t 1, 5, 20, 300; E/E0 1.021; 3289 cycles vs 7613 (dt no longer shrunk by cooling). G0f bitwise = G0ref. B star gate G3f (fix+thomas) and G3p (fix+PCR nseg32) pending; prod_m3 held until they pass. On the B star the old binary was right only by physics coincidence (radiative bottom cells).

**Why:** He production cannot use mode 3 until this is found; the B-star prod_m3 chain may carry a weaker version of the same drain.
**How to apply:** first check the energy budget (boundary vs internal sink, see the 09-16 budget note), then look in the mode-3 apply block for a dt-proportional term; a run of prod_m3 link 1 should be checked for tot-E drift beyond the mode-0 baseline. Note run.sh does rm -rf of the arm, so a rerun destroys out.txt. See [[rt-source-dt-forcing]], [[bstar-top-flux-deficit]], [[fecz-column-relaxation]].

He STATUS 09-15: the code fix is verified on the He column (Q1f thomas, Q1p2 pcr), but EVERY pre-09-15 mode-3 He conclusion (flux-collapse arms, the ramp convergence matrix, the '0.707 flux loss in every ramp config' = this bug's signature) ran without the bottom flux and must be regenerated; the mode-0 organ-pipe diagnosis stands. No 3-D He box has run with fixed mode 3 (1-D arms do not exercise the transverse operator / bvals window). Regeneration of Q2p/Q3p/Q4p/Q0n on athena_pinned_995ab40b launched 09-15 (qual_m3).

He MATRIX REGENERATED 09-15 on athena_pinned_995ab40b (fix + PCR nseg32): Q1p2 / Q2p (taper+IC+rad_force) / Q3p (cfl 0.15) / Q4p (cfl 0.45) all clean to t=300, Ftop/Fbot 0.943, 0.996, 0.996, 0.997-0.998 at t 1, 5, 20, 300 on every arm (cfl-INDEPENDENT to 4 digits), E/E0 1.020-1.021, KE_max 6.0-6.6e25; Q2p Newton it_max 3, nclamp 0, Ftop_impl/Ftop_sweep 0.999-1.001 (was 8 / 5-6 / 0.76-1.40). Q0n (mode 0) bitwise = Q0. The residual +2 % E over 300 = 0.5 % of the injected energy = the 0.2-0.4 % Ftop shortfall, not dt. Mode-3 He column is READY; next = the He 3-D box (transverse operator untested on He) and the ramp convergence matrix on the fixed binary.
