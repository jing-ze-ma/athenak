---
name: adi-transverse-operator
description: ADI horizontal diffusion COMMITTED 91d895de on rg-box-pcr (09-15): rad_ang_solver=adi, rad_adi_scheme=lod2 (Richardson of LOD, ONE sweep order, 2nd order, L-stable) is RKL1-ACCURATE on the B-star gate (dT_tau10 6.6e-8 vs 2.8e-7, dT_tau1 6.8e-6 vs 7.5e-6) at 59.9 vs 71.4 ms/cyc (1 rank), 209 vs 222 (4 ranks), He 0.76x; partitioned line solve bitwise across ranks; Douglas dies; default solver stays sts
metadata:
  type: project
---

Design: two tridiagonal line solves per stage over beta_dt (x2 then x3), same frozen conductances/flux form/plane skip; lines across blocks by a partitioned (spike) Thomas: 3-RHS factorization -> 6 interface coefficients per line, ring-gathered over the <= 8 blocks of the line (device copy on-rank, MPI off-rank on a dup'd communicator), the 2 nblk cyclic reduced system solved redundantly in absolute block order (bitwise identical interface values), NO cell halo exchange. Schemes: douglas (theta) and lod (default). Refuses rad_sts_split/rad_sts_all and non-periodic multi-block directions; rad_tr_halo_every ignored. md5 2a732035 (box), 0f36b01d (gauss). Gaussian L1 (32/64): sts 1.04e-7/2.79e-8; lod 5.78e-7/1.61e-7 (order 1.85); douglas 0.5: 2.0e-7/5.3e-8; 2 ranks bitwise = 1 rank. B star: A0 bitwise = T0; douglas theta 1 and 0.5 DIE (dt collapse, transverse KE 2e4x; analytic: (a+b)/((1+a)(1+b)) ~ 2/a relaxation of doubly-stiff modes); lod A1c to tlim: v_rms 924 (scatter), Mmax 0.221, Ftop 1.00000, dT_tau10 2.3e-6 vs 2.8e-7, dT_tau1 5.6e-5 vs 7.5e-6, P_nyq 1.3e-5 vs 6.0e-3, 59.6 vs 71.4 ms/cyc. 4 ranks x 2 nodes: 200 ms/cyc (F4 259, halo_every 222, floor 186), A4 vs A2 bitwise in time/dt/E/KE. He 1 node: 7.63 vs 10.33 s (0.74x), same drift/Ftop, conservation 8e-18.
**How to apply:** candidate for the He box (stiff lid, RKL1 clamped) after a 3-D physics gate; for the B star understand the 7-8x larger tau 1/10 contrast first (first-order LOD vs RKL1's better error constant); a Strang-symmetrized LOD (x2 half, x3, x2 half) would give second order at 1.5x the sweep cost - not built. See [[implicit-transverse-raddiff-plan]], [[rkl1-halo-every]], [[bstar-photosphere-checkerboard]].

SECOND ORDER 09-15: lod2 = 2 LOD(tau/2)^2 - LOD(tau) with the SAME sweep order in all sub-steps (alternating the half steps = lod2a symmetrises the halves so the full step's Lie splitting error survives -> reproduces lod exactly: the B-star residual is x2/x3 SPLITTING error, not backward-Euler damping; the separable Gaussian test is blind to it). Gaussian: lod2 2.06x sts at 64 (order 2.13) = the LOD N->inf floor; 2 ranks bitwise. B star R2 (lod2): dT_tau10 6.61e-8, dT_tau1 6.75e-6, v_rms 932, Mmax 0.246, Ftop 1.00000, 59.9 ms/cyc; lodn2 statistically the same at 59.8 but 1st order. 4 ranks: lod 200, lodn2 205.5, lod2 209.1, halo_every 222, floor 186. He 1 node: lod2 7.80 s vs RKL1 10.33. Commit d48625a9 in wt_adi, cherry-picked as 91d895de onto rg-box-pcr (wt_m3acc). USE: rad_ang_solver = adi + rad_adi_scheme = lod2 for the He restart (with the open top); the B star stays on RKL1 (6 % at 4 ranks is not worth a third restart).
