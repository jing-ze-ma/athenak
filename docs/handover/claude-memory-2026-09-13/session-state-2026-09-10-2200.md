---
name: session-state-2026-09-10-2200
description: START HERE (written 2026-09-10 22:00 CEST, orion) — implicit radial diffusion + angular cap implemented and gated; I2 (V9f rst 5e5, both switches) alive past both death points; I3 (I2 + opac_tmin off) and I4 (from scratch, 400 K 1e-18 radiatively active medium, WB on inside) launched with monitoring agents; tree still uncommitted (now ~29 files)
metadata:
  type: project
---
Supersedes [[session-state-2026-09-10-0910]]. Read [[red-giant-common-dt-collapse-571e5]],
[[red-giant-implicit-radial-diffusion]], [[red-giant-hot-corona-is-a-parker-wind]],
[[red-giant-two-stream-cannot-own-interior]] first.

## Runs (all /orion/ptmp/jinma/Athenak/red_giant/<dir>, binary = I2_implicit_cap/bin/athena
## = build_impl/athena_pin2 = build_guard + rad_implicit_x1 + rad_cap_ang)
- I2_implicit_cap 195846: V9f rst 5.0e5 + rad_implicit_x1 + rad_cap_ang=0.5, tlim 9e5.
  At 21:30: t=6.84e5, dt 30.63, past 5.7125e5 and 5.7604e5; one hydro dip to 6.1 s at
  5.83e5 (block 85, 1.069 R, x_i up to 1.3e6, 9 cells over cap) that healed by 6.7e5;
  faces capped on most cycles (evacuated photospheric cells persist). hst tot-E 9.88440e49
  flat. Agent monitoring; report pending.
- I3_implicit_cap_noclamp: I2 + opac_tmin=-1 (clamp OFF, open top). Tests whether the
  fixed RT makes the clamp unnecessary in the OPEN case (B13 showed it for the lidded).
- I4_medium400: FROM SCRATCH: I3 switches + V10-style medium bg_rho=1e-18 bg_temp=400
  bg_hydrostatic=false, NO rad_kappa_rmax/above (medium radiatively active), dfloor 1e-19,
  pfloor 1e-21, WB ON with wb_rmax 3.3e12 (V10 had WB off everywhere: 4.4x deep noise).
  Needs ~7 h to 9e5 -> chain a restart after the 4 h job.
- Decision context: V8f 6e5 K corona DROPPED (Parker wind). Corona inert by design
  (rad_kappa_above=0): see the 'why no RT in corona' answer in [[red-giant-implicit-radial-diffusion]]'s
  neighbourhood -- the corona is a numerical buffer; V10-style active medium is the alternative.
- WB cutoff wb_rmax is legal: the dynamic WB is per-cell (own background in reconstruction
  AND source, same x1v test in hydro.hpp:754 and red_giant.cpp:1898); the cutoff face loses
  only exact hydrostatic cancellation, not conservation.

## Tree
Uncommitted: the 24 files of the 09:10 note + conduction.hpp/.cpp, hydro.hpp,
hydro_tasks.cpp, mesh.cpp hunks for rad_implicit_x1 and rad_cap_ang, + driver.cpp <utility>
include. Style: 0 new violations on + lines (258 pre-existing on the branch). Commit plan of
the 09:10 note still applies; add commit g. "implicit radial radiative diffusion + angular cap".

## Open physics
Why cube-edge/vertex downdrafts evacuate cells 60x ([[red-giant-vertex-chimney]]); the
fixes make the code survive them, not remove them. Hydro dt dips at those cells remain.
