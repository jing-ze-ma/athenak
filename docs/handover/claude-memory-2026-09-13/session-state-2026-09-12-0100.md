---
name: session-state-2026-09-12-0100
description: "START HERE 09-12 01:00 (orion): FOFC complete and COMMITTED locally (e1db81d8 RT guard, b3345f55 FOFC steps 3-5+tests), viper MERGED 7cee2eb7 (4 ahead of origin, NOT PUSHED); gates: 48 fofc tests pass, red giant bit-identical, dhj hydro identical, dhj ck MHD+RT differs 1 float32 ulp from the RT GUARD (e1db81d8) not the merge; RG_fofc 196662 running (1.5e5 at 45 min, dt flat, no counters)"
metadata:
  type: project
---
Commits: e1db81d8 (red-giant RT top-cell guard + dfloor_keep_temperature; the keep_temperature body
lives in gnomonic_raisevel.hpp, i.e. in b3345f55), b3345f55 (FOFC curvilinear/MHD/cs-MHD/WB/MPI +
wb_atm pgen + 9 test files), 7cee2eb7 merge of fb69c554. Merge resolution: viper's legacy kernels
verbatim; our vceil_test flagging + general-EOS non-finite bail-out ported into the new
eos/{ideal,general}_hyd_floors.cpp; dfloor_keep_temperature ADDED to the floors_legacy condition
(else silently ignored); prolong_prims passes vceil_test on the non-legacy calls.
Gates: 48 fofc tests (not 53: pytest collects 48) pass incl. 4 mpicpu; red giant GV3b 150 cycles
merged vs pre-merge: 5/5 dumps + hst identical; dhj sp hydro 100 cycles merged vs origin identical;
dhj ck (sp MHD + general table + RT) 50 cycles: vel/bcc3 differ at 1 float32 ulp (median 8e-8),
dens/eint identical, deterministic; ablation = ONLY two_stream_rt.hpp reverted -> identical, so the
seed is the RT guard commit e1db81d8 (CPU FMA-contraction from the added branches), not FOFC/merge.
OPEN: (1) push? (2) gate the RT guard like floors_legacy if dhj must stay bitwise (viper's call);
(3) GPU build of the FOFC refactors on viper (expect 1-ulp drift, see [[viper-hip-code-conventions]]);
(4) wiki entries for mhd/fofc_rsolver, mhd/vceil, problem/bazi, wb_atm pgen, cs MHD FOFC supported.
Build dirs left: build_fofc3/4/5, build_fofc_rg, build_merge, build_merge_rg, build_merge_dhj.
RG_fofc: see [[red-giant-rg-fofc-run]]; at 45 min t=1.5e5, dt 30.67 flat, rg.log has no rows
(no floor/fofc event yet). Deaths to beat 5.7e5/5.87e5/6.13e5, expected during link 2 (196663).
