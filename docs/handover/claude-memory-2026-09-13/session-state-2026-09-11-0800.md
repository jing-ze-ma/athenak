---
name: session-state-2026-09-11-0800
description: "START HERE (09-11 12:50 CEST, orion; body has an 08:05 section and a 12:50 update): the whole red-giant tree is COMMITTED in 9 commits (3a41ac5b..70a146a6) and viper's branch MERGED (a10e367d), gates bit-identical both sides, NOT PUSHED (user decides); I8_vceil 196221 (pin10, vceil 5e7) running to 9e5, past every old death; next physics = denser corona test; hang fixed (pin11); the ptmp run inputs need the SIX rt opt-ins for any binary built after pin9"
metadata:
  type: project
---
Supersedes [[session-state-2026-09-11-0340]]. Read: [[red-giant-vceil-r4-passes-then-hangs]],
[[red-giant-r3-dfloor-keep-velocity-not-the-loop]], [[red-giant-rt-bface-switch-and-head-regression]],
[[viper-merge-trial-2026-09-11]], [[save-tokens-everywhere]].
Branch polar-average-perf = 10 commits ahead of origin (9 topical + merge a10e367d). git status
clean. Post-merge HEAD builds: regress/b_M_rg, b_M_dhj; gates: red giant vs pin11 GV3b bit-identical,
dhj ck vs pure viper bit-identical. The main-tree build_impl binary is pin11 (pre-merge); a
post-merge red-giant binary is regress/b_M_rg/src/athena (pin it as pin12 before using).
Open: (1) push a10e367d? (2) I8 outcome at 9e5: dt, L_rad,out/L (I5 ref 1.2/3.3/2.0 at 7/8.2/9e5),
eos_vceil counts, tot-E; (3) denser-corona test (base rho x1e3-1e4 ~1e-14, T 2e4 K, rad_gate_rho
just above it, vceil kept as guard, counter as the diagnostic); (4) what launches the shell at
5.7e5 in every open-top run; (5) latent stale-primitive audit items (hygiene commit); (6) dhj:
rt_use_cons is OFF there (bug latent) -- opt-in + before/after comparison if wanted; (7) end of
session: copy memory to docs/handover/claude-memory-2026-09-11/ + HANDOVER note (CLAUDE.md rule).

## UPDATE 12:50 (read after the above)
- PUSHED a10e367d to origin (user's fork) at 08:15. Then FOFC steps 0+1+call order COMMITTED
  c0163568 (local, NOT pushed): [[fofc-compatibility-plan]].
- I8_vceil FINISHED 9e5, healthy, analysis in _analysis_0911/i8_report.txt; C1_dense_corona
  (196255 + chain 196256, pin11, bg_rho 1e-12, gate 8.1e-13) was at 2.9e5 at 09:40 with 0 vceil
  hits: CHECK IT FIRST (shell test at 5.7e5; L_out/L; eos_vceil; dt).
- UNCOMMITTED in the tree: (a) RT top-cell NaN guard in two_stream_rt.hpp (KEEP, gated bit-identical)
  [[red-giant-top-cell-negative-density]]; (b) dfloor_keep_temperature switch across eos/*,
  hydro.cpp, coordinates.cpp (VACUOUS, my spec was wrong; drop or repurpose as a vacuum reset)
  [[red-giant-dfloor-energy-diagnosis-wrong]]. Do not commit (b) as is.
- DECISION PENDING (user): the top active cell goes to NEGATIVE conserved density mid-stage; the RT
  rescue was an accidental energy sink; with the guard the implicit conduction fails at 6.1265e5.
  Options: FOFC step 3 (cubed sphere) [recommended], conduction inert-cell clamp as stopgap,
  or find why the top cell's mass goes negative.
- Token rules: [[save-tokens-everywhere]]; the NaN chase cost 483k agent tokens (too much).
- End-of-session duty still open: copy memory to docs/handover/claude-memory-2026-09-11/ +
  HANDOVER-2026-09-11.md (CLAUDE.md rule) before viper needs it.
