---
name: session-state-2026-09-10-0300
description: START HERE (written 2026-09-10 ~03:00 CEST on orion, session 2c3cf987) — floor death CLOSED (two independent cures), port + random seed in the MAIN TREE (uncommitted), smoke tests V5/V6 PASSED, prod12 ready to launch (user decides); only prod11_catch still running
metadata:
  type: project
---
Supersedes [[session-state-2026-09-10-early]].

## Done this session (all numbers verified against raw logs by me)
- Floor A/B: FL2 (efloor_from_ekin only) survives, FL3 (eos_floor_consistent only) dies 1.43089e6.
- wb_rmax A/B: FL4 (wb_rmax=3.3e12 alone, floor switches OFF) survives to 1.56e6 with the
  floor energy creation fully active (efloor_de 3e18/interval); FL5 (wb_rmax+ekin) identical dt.
  => chain = cold floored cell -> WB source kick -> conduction-dt collapse; either cure works.
  See [[red-giant-floor-energy-creation-fix]].
- V4 (vpert_cart chart-free seed): vertex copies identical to 1e-6, noise 3-30x below T6, no
  cold cells to 3.6e5, BUT exact 48-fold cube symmetry (stripe_j==stripe_k, counts x12).
  See [[red-giant-vpert-seed-chart-imprint]].
- MAIN TREE (branch polar-average-perf, UNCOMMITTED, on top of the older uncommitted pile):
  floorfix.diff ported byte-identical + ThermoAt sub-floor hunk (eos.hpp); vpert_cart ported;
  NEW random solenoidal plane-wave seed component: problem/vpert_rand_amp (1.0), vpert_nrand
  (32), vpert_seed (1234), vpert_lmin/lmax (cgs; defaults 1.14e12/5.53e12 on the V4 grid —
  PIN THEM in production). Binary: /orion/ptmp/jinma/Athenak/red_giant/build_port/src/athena
  (md5 56d405fc...). The port DROPS rad_dt_face, rt_dump_rank, open_guard_fallback: inputs
  that set them fatal.
- V5_portcheck (195318): new binary, vpert_rand_amp=0 -> dumps 00000/00001 BIT-IDENTICAL to
  V4 (data arrays; headers differ by PAR_DUMP). efloor_de column present, 0.
- V6_randseed (195319, to 1e5): symmetry broken (chart copies of a vertex differ 8-45% at
  t=0; stripe_j!=stripe_k in 96/96 blocks); adjacent-cell ratio 1.003-1.026 (V4 1.002-1.013);
  0 cold/floored cells; rms|v| 1.28x V4 (random part 0.80x the structured, not 1.0x, within
  the 32-wave sampling scatter); max Mach 0.12 (V4 0.10; vpert_mach_max=0.05 is NOT enforced
  on the total — pre-existing).
- Helpers: _analysis_0910/cmp_dumps.py, task_V56.py, task_V4_*.py, run_V6_report.py.
- Memory rule: [[delegate-heavy-work-to-opus]] now covers MONITORING too (user, 01:35).

## Running
- prod11_catch 195261: at 2.87e7 at 02:40, ~1.6e4 s/min -> death 3.2106e7 expected ~06:00;
  the watcher agent (session 2c3cf987, agent a12b9b6af661e3d86) is armed to classify the seed.
- V4_prod12cart 195275: to 3e6 (~4 h wall from 00:55); check census at 4.4e5+ (T6's first
  collapse) and the 7.7e5-1.04e6 death window.

## prod12 recipe (READY, not launched — user decides)
build_port binary; V4 input + vpert_rand_amp=1 (pin vpert_lmin=1.14e12, vpert_lmax=5.53e12,
vpert_seed) + hydro/wb_rmax=3.3e12 + hydro/efloor_from_ekin=true (+ eos_floor_consistent:
true is the correctness fix but its ThermoAt branch has never run; recommend FALSE for prod12
and test it separately); open top, sponge off, opac_tmin 2500 (grains off) then 3200 by restart
at ~1.4e6 if the knee runaway appears; nan_check_cycles<=10; rst every 2.5e5; <output4> log
block IN the input file (command line cannot add blocks/params); UCX_LOG_LEVEL=error.
TO DO: commit the port in pieces (needs partial staging of red_giant.cpp; the old pile is mixed
in) and update the event-log parsers for the new efloor_de column.

## UPDATE 05:30 CEST (real clock; the timestamps inside today's notes run ~45-60 min AHEAD)
- prod11 death: ROOT CAUSE = WB polytropic walk overflow, FIXED + gated (build_guard binary,
  4 hunks, bit-identical on healthy data, B11 passes with WB on, sawtooth gone). See
  [[red-giant-wb-polytropic-walk-overflow]]. Also in the tree: gravity-source eos.Pressure
  guard, <problem>/nan_report instrumentation (default off, 9 files).
- Second, independent problem: single-cell cold collapse through the opacity knee ->
  conduction dt (B4/B5 at 3.2256e7); opac_tmin=3200 is the only remedy tested (B2).
- V4 (seed alone, old binary) DIED at 9.45e5 (interior cold collapse); seed buys 2x time only.
- RUNNING: V7_prod12gate (2 nodes, full prod12 recipe from scratch with build_port, tlim 1.6e6,
  agent a15f7a87), B12_fix_tmin3200 (6 nodes, build_guard + opac_tmin=3200 from rg.00124 to
  3.45e7, agent a564796a). B11 per-cycle dumps deleted.
- TO DECIDE (user): prod12 launch (recipe must use build_guard + opac_tmin=3200 + wb_rmax +
  efloor_from_ekin + random seed), prod11 revival from rg.00124 with the fixed binary + clamp
  (B12 is the gate), and committing the working tree in pieces (WB fix; HLLC/flux guards;
  floor fix; vpert_cart+random seed; nan_report; gravity guard).
- 06:20: V7 (prod12 recipe from scratch, open top, opac_tmin 3200) is FAILING: hydro-dt
  collapses on rank 26 from 5.9e5, stuck at dt 0.2 s at 6.05e5 (earlier than V4's 9.45e5);
  RT "Newton step left e<=0, rescued" line just before. B12 (fixed binary + clamp, lidded)
  HEALTHY at 3.339e7, dt flat. User: "try the hot corona again" -> V8_corona launched
  (agent a40decc1): V7 recipe + bg_hydrostatic corona at 6e5 K above the join (R7_hot_open's
  parameters), grid extended to ~1.3 R keeping V7's cells inside 1.1 R, build_guard binary.
- 07:05: B12 gate PASSED (lidded revival viable; user notes the lid damps/confines the
  photosphere: sponge_zbot 0.98 starts AT the photosphere; the open top is the real goal).
  V7 failed cold (open-top recipe from scratch). V8_corona (195417, 6e5 K, dfloor 1e-26,
  join 1.216 R, 480 cells to 1.30 R, the two-stream caps nx1 at 516) running clean at 6.7e4.
  USER DECISIONS: dfloor back to 1e-18 -> corona must be denser/joined deeper; corona
  temperature 2e4 K (chromosphere-like; H = 0.052 r; rho_c ~3e-17 at a join ~2e-16, drops
  ~20x to 1.30 R). V9_corona_dense (2e4 K, 1e-18 floor) being submitted by agent a40decc1,
  watched together with V8. Pending user go-ahead: prod11 revival chain from B12 rst; commits.
- 07:35: V8 (6e5 K corona, dfloor 1e-26) DIED like R7: hydro-dt COLLAPSE at 2.957e5 (rank 5,
  hydro 3.6 s, cond 182 s), then dt 0.1 s; RT "Newton step left e<=0 in 192 cells, rescued"
  x3 just before; L_out/L fell 0.81 -> 0.66 over 2.8e5 (atmosphere cooling under the corona,
  as R7). Cancelled 07:35; death analysis + V9 comparison with agent a40decc1.
  V9 (2e4 K, dfloor 1e-18, join 1.141 R at column rho 1e-15, rad_kappa_rmax 3.6528e12) clean
  at 8.6e4, dt 30.6.
- 08:35: RT NEIGHBOUR-PLANCK BUG fixed in build_guard (see [[red-giant-rt-neighbour-planck-bug]]).
  Gates: B13_rtfix (lidded, fixed RT, NO clamp, no wb_rmax) ran 3.205e7 -> 3.24e7 with dt
  flat, 0 collapses (old RT collapsed at 3.2256e7 without the clamp) -> the cold-cell collapse
  family was very likely this RT bug. V8f_rtfix (6e5 K corona): join cell 3271 K at 1.3e5
  (V8: 31 K in one cycle), L_out/L 0.90, no rescues. V9 (old RT, 2e4 K): alive at 3.09e5 but
  the join is erased (corona drains, atmosphere heated to 1e4 K). User: "try the fixed RT with
  V9" -> V9f_rtfix launched (agent a5d31353). Pending: the RT-fix agent's diffs + 1-D
  validation + B13 census (agent a369a053); V9 milestones (agent a40decc1).
- 08:45: user asked for the COLD FREE-FALL medium again, finite opacity (no rad_kappa cutoff),
  WB fully OFF, fixed RT -> V10_freefall launched (agent aa693526; bg_hydrostatic=false,
  bg_rho 1e-17, 400 K, dfloor 1e-18). V9 (old RT) at 4.6e5: envelope inflated through the
  corona and vents it; comparison value only. Running: V9 (195436), V8f, V9f, V10.
