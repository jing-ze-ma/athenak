# Handover 2026-09-26: viper (MPCDF) -> Caltech cluster -- START HERE

This session moves from viper to the Caltech cluster. The Caltech machine has **different GPUs and possibly
a different batch system**, and it sees **none** of viper's files outside this git repository. This page
assumes a reader who is new to AthenaK. Everything needed is in this repo on branch `rt-integration`, except
third-party data, which comes as one tarball (section 3).

> **NEW TASK FOR VIPER (from Caltech, 09-25 PDT): implicit M1/VET on a REAL spherical wedge with rad-hydro
> (gravity, moving gas); settles open decision #2. Handed to viper because Caltech is GPU-bound. Read
> `docs/handover/TASK-2026-09-26-m1-real-wedge.md`.**

> **CALTECH PORT MERGED into `rt-integration` (merge 39ca34a6, from Caltech, 09-25 PDT).
> Viper must do these two things once:**
> 1. **`git submodule update --init`**. `kokkos` is a real submodule again. The merge pinned 08ceff92, which
>    is Kokkos **4.4.00** as on `main` (an earlier version of this note wrongly said 4.6.02). Since the
>    "kokkos: 4.4.00 -> 4.6.02" commit (Caltech, 09-26), `rt-integration` pins **4.6.02 (d8e9af03)**, which has
>    `Kokkos_ARCH_AMD_GFX942_APU`, so after `git submodule update` viper can build against the submodule and
>    drop the `bench/wt_rgbox/kokkos` link. Commit 3596f5b3 had replaced the submodule with a symlink to
>    `/viper/u2/jinma/ATHENAK/bench/wt_rgbox/kokkos`.
>    An existing viper checkout may need the old `kokkos` symlink removed before `git checkout` / `git pull`
>    succeeds. The viper build script re-links `kokkos` into each snapshot, so it is unaffected.
>    Incremental build trees configured against 4.4.00 must be reconfigured (`build_inc.sh` now does this
>    itself when the kokkos commit changes).
> 2. **Rebuild and run the usual HIP bitwise gate** (keys off, restart), old binary vs this merge. HIP could
>    not be tested on Caltech. The ported code is identical on HIP except for these changes:
>    - three `static constexpr` constants in `two_stream_rt.hpp`;
>    - `EOSTable` host mirrors, which HIP kernels do not read;
>    - `DeepCopyAcross`, a plain `deep_copy` wherever the host can reach device memory, as on the APU;
>    - an added `mb_panel` `sync_device`. On the APU this is a no-op, because the host and device views alias.
>
> **What the port fixed.** The MI300A APU shares host and device memory, which hid three bug classes that
> discrete GPUs expose:
> - nvcc rejects generic (`auto`) device lambdas and helper lambdas captured into kernels.
> - Host code read device Views: the EOS table in the IC setup, and strided host<->device `deep_copy` in
>   restart I/O.
> - A DualView filled on the host was never synced to the device. `MeshBlock::mb_panel` stayed zero on the
>   GPU, which **silently** gave 8 of 24 C32 blocks the wrong initial state.
>
> Also fixed:
> - `m1_impl_wb` read the class member `coupling` inside a kernel, i.e. it captured `this`;
> - `sqrt(2)` was called with an int argument in device code.
>
> Not yet checked: host-filled DualViews off the dhj path (geodesic grid, `cart_grid` and
> `spherical_surface` `iindcs`/`iwghts`). **Rule for new code:** never index a device View on the host, and
> `sync_device()` every DualView after filling it on the host.
>
> **Verified on Caltech (H200, CUDA 12.9, HPC-X CUDA-aware MPI).** WASP-121b 1x base arm, production keys,
> new seam defaults on:
> - CPU bitwise, viper's latest 3f13cd38 vs the merge;
> - GPU vs CPU: the t=0 snapshot byte-identical, the cycle-20 history within 1e-13;
> - 2 H200 vs 1 H200 bitwise;
> - GPU restart bitwise;
> - GPU run to run deterministic.
>
> Timing, nx1 256 8-coef on 2 GPUs: H200 42.8 ms/cycle at xstep 2 (viper MI300A 75.3, so 1.76x faster) and 34.5
> ms/cycle at xstep 8 (-19 % on H200 vs -11 % on viper). An nsys profile shows the ck two-stream kernels are
> about 67 % of GPU time, and the GPU is idle about 40 % of wall time (host gaps).
>
> Caltech scripts are in `caltech-2026-09-26/scripts/`: `build_inc.sh` (incremental), `run_caltech.sub`,
> `cpugate_caltech.sub`. Use `srun --mpi=pmix`, and `-q debug` for tests.
>
> **viper HIP gate 09-26: bitwise, keys off + restart, dhj WASP-121b 1x / He box M1 / cs shock_tube MHD / M1 sp wedge** (77755bc1 vs a88984f2, both Kokkos 4.6.2 GFX942_APU; /viper/ptmp2/jinma/hipgate_0926/RESULT.md). Caveats: the submodule 08ceff92 was Kokkos 4.4.00 (not 4.6.02) and has no GFX942_APU arch, so viper had to link bench/wt_rgbox/kokkos (4.6.2) -- resolved by the 4.6.02 bump (item 1); on a discrete memory space `RadiationM1::Time2RstSet` still deep_copies host -> strided device (M1 restart aborts; needs DeepCopyAcross).

Read in this order:
1. The top-level `CLAUDE.md`: what AthenaK is, build, tests, architecture, kernel conventions.
2. This file.
3. `docs/handover/claude-memory-2026-09-26/MEMORY.md`: the assistant's memory index, about 400 short notes.
   Copy the whole directory into the local Claude memory dir so recall works (see CLAUDE.md, "Session
   handover"). Many notes cite viper paths (`/viper/...`, `/u/jinma/...`); those files are NOT on Caltech.
   Treat the paths as provenance only.
4. `docs/handover/caltech-2026-09-26/`: production inputs, reference build and job scripts.

## 1. Repository state

- Branch `rt-integration` on the fork `git@github.com:jing-ze-ma/athenak.git` (remote name `fork` on viper).
  All work is merged here; `main` is the upstream IAS code and does not have it.
- Merged and pushed 09-26:

| commit | what |
|---|---|
| e26c433e | m1-fast5-box: `<rad_m1>/implicit_precond = mg_gf` (global Fourier coarse space in front of mg; opt-in; He box -5.9 % on 1 GPU) |
| 1f61da16 | m1-fast5-sp: sp-wedge implicit M1 + vet_col kernel speed-ups (-21 % on 1 GPU; v9/v10 regroup sums, round-off only) |
| 7d68dd70 | cpplint fixes in rad_m1 (no code change) |
| 78de21dd | ck-ncloc: default-off diagnostics `problem/ck_impl_ncloc`, `problem/seed_restart`, `problem/dtloc_every` |
| 427f9f58 | ck-newton10x-b: `problem/ck_impl_kkt_row` (default off; production turns it ON, section 4) |

- **The merged HEAD 427f9f58 passed its build check** (`/viper/ptmp2/jinma/mergechk2_0926`):
  - the merged hunks are identical to each branch's own diff;
  - CPU and GPU builds are OK;
  - with the keys off, results are bitwise vs cab48eb7 / 7d68dd70;
  - with kkt_row on, bitwise vs the branch tip;
  - with ncloc / dtloc on, bitwise vs the ck-ncloc tip.
  cpplint reports 267 old errors in `deep_hot_jupiter_rt.cpp`, unchanged since before the merge.
- The build check on 1f61da16 (rad_m1 merge) did pass: bitwise against both branch tips.
- `docs/handover/RESUME-*.md` hold the resume notes of the two merged branches, kept for history.

## 2. Porting to a new cluster (GPUs, scheduler)

**Build.** There is no globbing: new sources go into `src/CMakeLists.txt`. Always build in a separate
directory, and build the problem generator you need:

```bash
git submodule update --init --recursive           # kokkos
# NVIDIA (pick the arch of the Caltech GPUs: AMPERE80 = A100, HOPPER90 = H100/H200, ADA89 = L40S)
cmake -B build_dhj -D Kokkos_ENABLE_CUDA=On -D Kokkos_ARCH_HOPPER90=On \
  -D CMAKE_CXX_COMPILER=$PWD/kokkos/bin/nvcc_wrapper \
  -D Athena_ENABLE_MPI=ON -D CMAKE_BUILD_TYPE=Release -D PROBLEM=deep_hot_jupiter_rt
# AMD (viper was MI300A: -D Kokkos_ENABLE_HIP=On -D Kokkos_ARCH_AMD_GFX942_APU=On -D CMAKE_CXX_COMPILER=hipcc)
make -C build_dhj -j 16                            # -> build_dhj/src/athena
```

- Problem generators in use:
  - `deep_hot_jupiter_rt`: the hot-Jupiter runs, cubed sphere + correlated-k RT;
  - `box_convection`: the He / massive-star M1 box;
  - none, i.e. built-in pgens, for the tests.
- `docs/handover/caltech-2026-09-26/scripts/build_viper_reference.sh` is the exact viper recipe. It builds
  from a `git archive` snapshot so that a dirty tree never leaks into a binary; keep that habit.
- Check the build with `./build_dhj/src/athena -c`.
- CPU builds are used only for correctness gates, e.g. bitwise comparisons. **All timing and cost
  comparisons must be made on the GPU, same binary, arms interleaved** (user rule).

**Viper-only facts. Do NOT carry them over; re-establish them on Caltech:**
- **GPUs per node.** Viper: 2 MI300A per node. The apudev partition allowed 1 node and 15 minutes; apu
  allowed longer jobs. Find Caltech's GPUs per node, partitions and limits first (`sinfo`, site docs), and
  record them in memory as a Caltech fact.
- **AMD env vars.** Every viper GPU job exported `HSA_XNACK=1` and `HSA_NO_SCRATCH_RECLAIM=1`. These are
  AMD/ROCm (MI300A APU) settings and are irrelevant on NVIDIA. On NVIDIA, check GPU binding instead:
  one MPI rank per GPU, `CUDA_VISIBLE_DEVICES` per rank or `srun --gpus-per-task=1`.
- **Timings.** All ms/cycle numbers in the notes are MI300A numbers. Re-measure before comparing.
- **viper-specific machinery.** Login-node memory caps, the inode quota and the `claude-work.slice` memory
  hook are all viper-only.

**Job scripts.** `scripts/run_viper_reference.sub` is the viper Slurm driver for the WASP-121b runs. Its
logic is portable:
- planet selection;
- the key groups T4 / C2 / E4;
- sponge arms;
- restart from the newest rst;
- `-t` wall-clock stop, with a STOP file so chained links skip finished arms.

Rewrite only the `#SBATCH` header, `module load` and the `srun` line for the Caltech scheduler. If Caltech
is not Slurm (e.g. PBS), keep the body, translate the header, and replace `--dependency=afterany:` chaining
with the site's equivalent. Note that Slurm snapshots the script at `sbatch` time: after editing a submit
script, cancel and resubmit.

## 3. Data (not in git)

**Download** it from the data branch `data-2026-09-26` of the fork. The branch has no code and shares no history with `rt-integration`:
```bash
curl -LO https://github.com/jing-ze-ma/athenak/raw/data-2026-09-26/athenak_data_2026-09-26.tar.gz
md5sum athenak_data_2026-09-26.tar.gz   # 057e168a9a1559e9eb419bf362dae0b1 (12.7 MB)
tar xzf athenak_data_2026-09-26.tar.gz  # -> athenak_data/ ; per-file md5s in MANIFEST.md5 on that branch
```
Viper copy: `/viper/ptmp2/jinma/caltech_handover_0926/`. The Exo-FMS tables have no upstream licence and
are for research use only; see the branch README and `data/exo_fms_ck/PROVENANCE.md`.

| dir in the tarball | what | used by | placeholder in the inputs |
|---|---|---|---|
| `exo_fms_ck/` | Exo-FMS correlated-k tables, 11 bands (1x solar), hiT/hiT2 extensions to 10100 K, CIA, Rayleigh, SW band fluxes. Third-party, gitignored (`data/exo_fms_ck/PROVENANCE.md`, `HITEMP.md`); regenerate hiT with `data/exo_fms_ck/tools/gen_hitemp.py` (needs pyfastchem) | WASP-121b 1x | `ATHENAK_CK_DATA` |
| `ckdata10/` | the same at 10x solar metallicity | WASP-121b 10x | `ATHENAK_CK_DATA10` |
| `he_box/` | He-box IC profile, M1 radiation IC, Rosseland/Planck opacity tables, a_rad file | He M1 box | `HE_BOX_DATA` |

Unpack the tarball. Either replace the placeholders in the inputs with absolute paths, or put
`data/exo_fms_ck` back in place: the repo expects it at `data/exo_fms_ck`, and `ck_ce_table` is relative to
`ck_data_dir`. The IC profiles `ic_w121_1x.txt` / `ic_w121.txt` (1-D ck radiative-convective equilibrium)
are small and are committed next to the inputs. `ic_profile` must reach >= 250 bar.

## 4. Science threads, decisions in force, and what comes next

### 4a. Deep hot Jupiter (dhj): WASP-121b on the cubed sphere

- **Target.** WASP-121b, parameters from the literature: P_rot = P_orb = 1.27492504 d = 1.101535e5 s.
  1x solar is the running campaign; the 10x inputs exist too.
- **Inputs:** `caltech-2026-09-26/inputs/wasp121_1x/`:
  - `sparc_w121_1x.athinput`;
  - `grid_w121_1x.env`: radial grid overrides. `G_SPARC` is nx1 76; `G_PROD` is nx1 256 with an
    8-coefficient polynomial stretch;
  - `ic_w121_1x.txt`.
  10x: `inputs/wasp121_10x/`.
- **Production physics (decided):**
  - route B: ck to the inner wall, `isotropic_conduction = none`, `ck_pcut_bar = 1e8`, `ck_int_at_cut`;
  - `ck_nquad = 2`;
  - hiT2 ck tables;
  - initial condition = 1-D ck RCE profile via `problem/ic_profile`;
  - hydro, cubed-sphere C32 (the SPARC-like grid) for tests.
- **Implicit ck key groups.** These are given on the command line by `run.sub`:
  - T4: `ck_implicit`, `ck_impl_frozen_op`, `ck_impl_lin`, `ck_impl_fuse`, `ck_impl_jac_lin`, `ck_impl_cvsec`;
  - C2: `ck_impl_once`, **`ck_impl_xstep = 8` (changed 09-26 from 2, which was a no-op because xstep counts
    cycles)**, `ck_impl_jreuse 0.2`, `ck_impl_pred`, `ck_impl_nosync`;
  - E4: `ck_impl_every = 4`.
- **New 09-26: `problem/ck_impl_kkt_row = true`.** It is in both WASP-121b inputs. It fixes stalled Newton
  columns next to cells clamped by `ck_impl_kkt_demax`. The effect on 1x production: NOT-CONVERGED 20/2113 ->
  0 at the same cost.
- **Accuracy rule (user): only p > 1e-6 bar counts.** The layers above may be crude, but they must not
  significantly limit dt, crash the code, or cost much. Grid resolution counts only below 1e-6 bar.
- **Sponges (user 09-26): keep BOTH top and bottom.**
  - The top sponge buys dt 1.8x: 13.6 vs 7.5 s. Without it the flow at 1e-11 bar reaches 20-50 km/s.
  - With no sponges at all, dt collapsed at 0.18 rotations.
  - The bottom sponge showed no effect up to rotation 8. **Recheck base vs nobot at rotation 20** (deep flow
    and T at 1-100 bar, plus the T-floor census).
  - The T-floor census found 1-11 cells at the 200 K floor at 1-20 microbar on the night side, at
    |lat| 15-26 deg, in every arm (at most 3e-5 of the cells).
- **Sponge arms CRASHED 09-26 ~02:30** (FATAL dt collapse in an evacuated upper-atmosphere cell at the
  density/pressure floors, T 1e11+ K). nobot died at rot 6.03, notop at rot 5.59. Only base (both sponges)
  survived longest, but **base ALSO crashed at rot 27.04** (same FATAL dt collapse, gid 1; 7 collapse warnings
  before it). Every arm of the campaign has died. The sponges only delay it: the seam/floor defect is general, and a
  long production run is NOT safe without a fix (at least the floor switches below).
  **Diagnosis:**
  - Both crashes sit on cubed-sphere face-edge SEAMS on the night side, locally at 1e-7..4e-7 bar. 6 of 7
    dt-collapse sites are on seams.
  - A seam row pinned at the 200 K T-floor (nobot), or a M 10-20 seam downdraft (notop), turns supersonic.
  - The legacy density floor then raises rho but keeps momentum and energy, giving v ~3e11 cm/s and dt
    collapse.
  - base has the same precursor; the bottom sponge is not shown to be causal.
  **Floor-switch fix, gated 09-26:** `<hydro>/dfloor_keep_velocity = true`, `dfloor_keep_temperature = true`,
  `vceil = 1e7`. Both crashes disappear when rerun from just before them; vceil fires only near the old crash;
  floor counts are unchanged. This is stability only: the switches change the solution wherever the density
  floor fires, mostly at p < 1e-6 bar, and accuracy is not shown. The keys exist in the code
  (`GetOrAdd`, accepted on restart). Viper files: `sponge_crash_0926/`.
  **ROOT CAUSE FOUND (09-26, merged 2d07119a): the cubed-sphere seam flux is not positivity-preserving.**
  - `src/bvals/flux_seam_cc.cpp` sets each seam face flux to the average of the two panels' fluxes. Each
    panel computes its flux from along-seam resampled ghosts (`bvals_cc.cpp`).
  - An emptied seam cell looks denser to the other panel, which drains it. At the first dt collapse the drain
    is 38x the cell's mass per step. The legacy density floor then keeps the momentum, and the cell runs away.
  - New key `<mesh>/cs_seam_flux = average` (default, bitwise as before) `| upwind | positive`. When both
    panels agree on the mass-flux direction, the donor panel's flux is used; `positive` also zeroes the mass
    flux when both panels claim inflow. Mass and energy stay exactly conserved.
  - Reruns from the crash restarts: `average` crashes; `upwind` and `positive` are clean.
  - Smooth rigid rotation: errors within 2 % of `average`.
  - **Speed settings cleared:** the crash persists with the ck solved every step at tol 1e-9.
  - **Multi-rotation check DONE 09-26** (base rot 20 -> 32, positive and upwind, production physics):
    - **Stability:** 0 collapse warnings in either mode, vs 7 plus the FATAL at rot 27.04 in base. Minimum dt
      9.4 / 8.1 s vs 0.36 s in base.
    - **Mean T at 1e-2..10 bar:** within 0.6 K of base (noise 0.3 K).
    - **At 1e-6..1e-4 bar:** 10-75 K, but positive vs upwind differ by the same 20-60 K there, so that layer is
      chaotic.
    - **Deep eint (1-10 bar):** no mean bias, but the cell-by-cell rms difference from base grows about
      linearly to 6.4e-3 after 7 rotations. That is a real operator difference showing up as scatter, not
      drift. A 7-rotation noise member was not run, so it is not yet known whether ordinary chaos grows as
      much.
    - **Near vertices:** fewer supersonic cells, but T roughness up 1.6x at the top.
    - **Verdict: stable, and the climate is unchanged within noise.** Use `cs_seam_flux = positive` in all cs
      dhj runs; without it every WASP-121b arm crashed. A new key cannot be set on the command line with `-r`; pass an overlay with `-i`.
  - Details: `docs/dev/cs_seam_defect_0926.md`. The floor switches above remain a second safety net.
  - **MHD and vertex audit (09-26, viper `seamaudit_0926`):**
    - MHD uses the same cc seam flux (`mhd_tasks.cpp` -> `flux_seam_cc.cpp`), so `cs_seam_flux` already covers
      it. An MHD low-density hole on a vertex crashes under `average` and is clean under `positive`: use
      `positive` for MHD productions too.
    - The EMF / face-centred path shows no bug. Seam EMFs are averaged, 3-way at the vertex edge, and div B at
      seams and vertex stays at the initial projection level (~4e-5).
    - **Vertex defect: found and fixed (merged 6a3d051c).** An isothermal density hole on a cube vertex went
      NaN in 2 cycles under every seam option.
      - Cause: the along-seam quadratic GHOST RESAMPLE (`bvals_cc.cpp`). Its negative weights and
        per-variable clamp make an emptied seam cell's ghost dense, and build ghosts with E < KE next to
        floored cells.
      - FOFC does not cure it (the damage is in the ghosts).
      - New keys, default off and bitwise when off: `<mesh>/cs_seam_resample = linear` (convex interpolation)
        and `<mesh>/cs_seam_rho_guard = 4` (copy the face-sharing cell when densities differ by more than 4x).
        They act on the halo only, so updates stay exactly conservative.
      - Gates: cavity cases hydro + MHD 32/32 clean; rigid rotation L1(v) +4.2 % (n16), +0.9 % (n32),
        +0.13 % (n64); the nobot/notop crash reruns clean.
    - **Recommended cs set:** `cs_seam_flux = positive`, `cs_seam_resample = linear`, `cs_seam_rho_guard = 4`.
      **They are already written into the WASP-121b inputs in `caltech-2026-09-26/inputs/` (`<mesh>` block).**
      **They are now DEFAULT ON (merge of cs-seam-default 01df7e26).**
      - The old scheme stays available with explicit values; the overlay is in the doc.
      - Old restarts without the keys switch scheme.
      - The cs tests passed 15/15; non-cs problems are bitwise.
      A 12-rotation dhj run of the full set plus a 1e-14 noise twin was at rot 23.4 of 32 with 0 warnings when
      viper was left.
      Only `positive` alone has had the 12-rotation dhj check; run a multi-rotation check of the full set
      before a long production.
    - Cheap reproducer: cs_test cavity keys `cav_*` (a hot hole at mid-seam crashes at cycle 100 under
      `average`); see `docs/dev/cs_seam_defect_0926.md`.
  Both sponges stay ON.
- **Cadence retest (09-26, 3 rotations, against a 1e-14-kick noise member):**
  - xstep 8: -11 %, within noise. Adopted.
  - `ck_impl_every 8` + `xstep 16`: -28 %, 1.4-1.9x the noise at 1e-3..1 bar. **Borderline; needs a second
    noise member to decide.**
- **Still open before the long production:**
  - ~~the radial-grid A/B~~ DONE 09-26 (viper `radab_0924/ana_0926`). The comparison is on the old planet,
    p > 1e-6 bar:
    - Below 1e-2 bar, nx1 128 is within 1-13 K (<= 0.4 %) of nx1 320, not growing; winds agree.
    - At 1e-4..1e-2 bar, 128 is NOT converged: a 45 K mean offset (~20x noise), and 650-800 kinked night
      columns that 320 does not have.
    - a320 costs 14.8x per rotation.
    - **Recommendation: spin up on 128, then remap to 256 8-coef (or 320) for the science stretch.** The offset
      re-forms within 0.5 rotation, and this matches the two-phase policy. 256 itself was not tested.
    - These runs predate the seam fix.
  - then a fresh spin-up, 300 rotations goal (about 1060 Earth days);
  - MHD only for the last 10-20 rotations: a hydro spin-up is enough (tested 09-25).
- **Weak scaling (viper MI300A, 09-26):** C64 nx1 256 on 8 GPUs vs C32 on 2 GPUs, same blocks per GPU.
  - hydro 27.2 vs 29.4 ms/cycle;
  - hydro + ck RT 62.0 vs 66.6 ms/cycle.
  That is about 100 % weak efficiency across nodes. Re-measure on Caltech's interconnect.
- **10x only:** kkt_row halves the failures (164 -> 85/200); the rest are linearly contracting stiff
  day-side columns and need a Broyden or banded-Jacobian step (design in `RESUME-ck-newton10x-b.md`).
  Only relevant if 10x is run.

### 4b. Massive stars: implicit M1 radiation (VET closure)

- **Code.** `src/rad_m1/` (design docs `docs/dev/rad_m1_*.md`). Implicit two-moment RT with a variable
  Eddington tensor:
  - `vet_sc`: short characteristics, Cartesian;
  - `vet_col`: column rays, on the spherical-polar "sp" grid.
- **Accuracy rule (user 09-26): the atmosphere near the top boundary does not matter.** Judge accuracy below
  the photosphere only: Rosseland tau >= 1, excluding the top cells and the sponge. The same caveat
  applies: the top must not limit dt, crash, or cost much.
- **Decided for the next M1 run:** `time_scheme = hesdirk2` with `time/cfl_number = 0.9`. On the 3-D He box,
  09-24, it matched be at 0.3 in accuracy and was 1.57x cheaper.
- **He box cfl VERDICT (09-26 night, viper `hebox_cfl_0926/RESUME.md`): cfl 0.9 is NO-GO for production.**
  - Setup: 8 turnovers from the most evolved 3-D M1 He box restart, averaged over 27000-34000 s.
  - H9 is stable and 2.3x cheaper, but its interior KE is 3.8x R3's (noise 2e-8). T and rho are within
    1e-3..7e-3, and the flux profiles within 8e-5 of F.
  - **Bigger finding: the saturated KE rises steadily with cfl.** Mean horizontal KE: R15 (cfl 0.15) 1.9e28
    and still growing; R3 3.6e28; H6 (0.6) 7.3e28; H9 1.3e29. **So even cfl 0.3 is not converged in dt.**
  - The box is not yet convecting (F_conv/F < 1e-4), and the same dt dependence was seen before with be +
    Eddington (runs_3o). So it points at the hydro + radiation coupling, not the M1 time scheme.
  - **This must be understood before any M1 cfl choice** (open decision for the user). The earlier "keep cfl
    0.9" decision is on hold. `vet_sc_every = 2` is also no-go (7 % slower, no more accurate).
- (Earlier partial result, kept for context:) **He box check of cfl 0.9 with MOVING gas: PARTIAL RESULT (viper `hebox_cfl_0926`).**
  - **Cost:** cfl 0.9 is 2.28x cheaper per simulated second than cfl 0.3. `vet_sc_every = 2` adds 7 %: not
    worth it.
  - **dt:** set by the hydro sound speed at the BOTTOM (tau 66-98), never the top.
  - **CONCERN:** at cfl 0.9 the kinetic energy grows ~100x faster early and saturates at ~4.6x the cfl 0.3
    level. This box barely convects (F_conv/F ~ 1e-5), and its noise copies agree to 1e-8, so the flow is
    wave/laminar growth that depends on the step size.
  - **Open at handover:** cfl 0.15 and 0.6 runs, to tell whether cfl 0.3 itself is converged in KE. Until
    that is settled, treat cfl 0.9 as a relaxation-phase setting and recheck on a truly convecting state.
- (Original description of the check, kept for the setup details:) **The He box check of that cfl with MOVING gas was running on viper at handover**
  (`hebox_cfl_0926`: R3 = cfl 0.3, H9 = 0.9, H9S2 = 0.9 + `vet_sc_every 2`, plus noise arms). Start: an
  evolved 3-D He box restart that is not on Caltech. **Result unknown.** To redo it on Caltech:
  - input `inputs/he_box/box.athinput`;
  - reference arm command lines in `arms_viper_reference.sh`;
  - start fresh, because the restart is not available, and wait about 1 convective turnover (H_p/v_MLT
    ~ 3800 s) before comparing.
- **Correction (09-26).** The "sp wedge" used for the fast5-sp / vet_col / cfl timings is test T-S4
  (`rad_m1_beam`, `m1_test = sph_atm`): a grey atmosphere with NO gravity and the gas held fixed. It is fine
  for radiation-solver timing, but it is not a star. **No gravity-bearing spherical M1 He wedge exists.**
  The 4 Msun presupernova He-star global model is only a plan (memory `he4-presn-global-plan`).
- **Speed-ups measured on the static wedge (radiation side only):**

| option | effect | status |
|---|---|---|
| cfl 0.9 | 2.75x cheaper per simulated second, interior deviation <= 7e-6 | radiation OK; hydro untested |
| `<rad_m1>/vet_col_every = 4` | -23 %, interior deviation <= 5e-6 in the transient, 0 later | needs `time2_vet_col = lag` in the INPUT FILE (a command-line override works only if the key is in the file) |

  Confirm both on an evolving state before production.
- **Tolerance sweep: DONE 09-26 (viper `tolsweep_0926`).** No big lever.
  - Eisenstat-Walker, warm start and one-pass acceptance already exist and are on by default. Warm start is
    worth 17 % on the box.
  - Keep `lin_tol` 1e-10: loosening it fails on the wedge (6e-7 to 9e-6).
  - Picard `implicit_tol` 1e-7 saves about 12 % on the box, at an interior deviation of 2e-9. That is below
    the 1e-7 time error but 60x the box round-off spread. **User 09-26: the bar is round-off, so keep 1e-8.**
  - Nothing beats timing noise on the wedge.
  - The top layers (tau < 1) own the largest residual in about 95 % of the box Picard passes. A Picard test
    that ignores or down-weights tau < 1 cells is the remaining lever. It needs code and a user decision.
- **M1 multi-rate (`implicit_mr_every` 2/4): REJECTED 09-26, even for relaxation** (viper `mr_0926`). It
  requires hesdirk2, and on top of hesdirk2 cfl 0.9 it fails:
  - He box: k2 is no cheaper; k4 is -26 % but crashed.
  - Both inject 1000-4000x the kinetic energy.
  - After switching back to off, the box stays 6-11 % off in plane-mean E. It does not relax back.
  - Static wedge: 1.6-1.7x slower.
  The old gains were measured on be.
  **What failed is the multi-rate sub-stepping (radiation on a different step from hydro, every 2 or 4 hydro
  steps), NOT cfl 0.9.** The reference arm of this test was hesdirk2 at cfl 0.9 with mr off. It ran cleanly on
  box and wedge: no crash, the top never set dt, noise copies agreed to round-off. cfl 0.9 vs 0.3 on a
  convecting He box is the separate He-box check.
  Setup pitfalls:
  - a command-line key must already be in the input file;
  - viper's `SBATCH_EXPORT=NONE` dropped environment variables;
  - an input with two restart output blocks is fatal.
- **Round-off sensitivity.** On the sp wedge a round-off difference grows about 1e6-fold within 3 cycles, at
  tol 1e-10. Compare arms against a noise member (1 vs 2 GPUs, or a 1e-14 kick), never pointwise against 0.

## 4c. M1 real wedge (Caltech TASK-2026-09-26-m1-real-wedge): DONE on viper, merged 09-26

`m1_test = sph_wedge`, new file `src/pgen/tests/rad_m1_wedge.cpp` (keys `wg_*`; doc `docs/dev/m1_wedge_0926.md`;
runs `tests_m1/runs_6a_m1wedge/`). The route is (b): the test pgen gained gravity, not red_giant. The rad_m1
coupling on sp needed no changes.
- **(A) Static grey atmosphere with a point mass:** PASS. Interior Mach_rms 4.8e-5, converging about 2nd
  order in nx1; luminosity within 4e-4; dt is set at r_in.
- **He wedge** (the V3edd box star on a sphere, M 3.15 Msun, 84 radial cells, 2x2 deg; the FeCZ is only
  0.7 % of R here, so a wider wedge would give 60:1 cells):
  - restart bitwise on CPU and GPU;
  - 1 vs 2 GPUs 4e-11;
  - L_top/L_in = 1 +- 2e-6 over 1.5 turnovers;
  - a slow radial ringing grows (Mach_rms 9e-4 -> 1.4e-3);
  - no convection onset yet.
- **(C) Hot spot:** cfl 0.9 is 2.9x cheaper per simulated second, with 2-3 % error in dT (cfl 0.3: 0.5-1.6 %).
  `vet_col_every = 4` + lag shows no measurable error and is -8 % per cycle.
- **Lever timings** (from a near-static state; re-measure from an evolved one): rbgs_fwd +10 %; no predictor
  -6 %; the others are within noise.
- **Existing problems bitwise** vs c1dc8226 (T-S4 wedge, He box, dhj). NOT yet compiled with nvcc: Caltech
  should build it for H200.
- **Next:**
  - analyse the seeded 84x64x64 run (viper job 11984805, `sprhd_0926/RESUME.md`);
  - decide whether the ringing is the Fe-bump kappa-mechanism or the closed walls (test an open/damped wall);
  - the extended presupernova star needs an MLT-based IC, because a purely radiative column is
    super-Eddington in the Fe bump.

## 5. Viper work: finished (results in this file) and still running

**Finished 09-26.** Results are in sections 1 and 4; viper directories are for reference only.

| what | result | viper dir |
|---|---|---|
| M1/VET kernel speed-ups (fast5-sp, fast5-box) | merged; wedge -21 %, box -6 % | `fast5sp_0925`, `fast5box_0925` |
| vet_col every-N | N=4: -23 %, static wedge only (relaxation use) | `vetevery_0926` |
| hesdirk2 cfl 0.9 on the static wedge | 2.75x cheaper; radiation side OK | `m1wedge_0926` |
| M1 tolerance sweep | no big lever; keep lin_tol 1e-10, implicit_tol 1e-8 | `tolsweep_0926` |
| M1 multi-rate | rejected, also for relaxation (the sub-stepping fails, not cfl 0.9) | `mr_0926` |
| ck stalled Newton columns | `ck_impl_kkt_row` merged; production ON | `wasp121_0925/smokediag`, `bin_0925` |
| ck cadence retest | xstep 8 adopted; e8 + x16 borderline, relaxation only | `ckcad_0926` |
| dhj weak scaling 8 vs 2 GPUs | ~100 % | `ckfast2_0925/gpu` |
| WASP-121b 1x sponge arms | ALL crashed (none 0.18, notop 5.59, nobot 6.03, base 27.04 rot) | `sparc_w121x1/<arm>` |
| T-floor census | a few floor cells just below 1e-6 bar in all arms | `sponge_chk_0926` |
| crash diagnosis + floor-switch gate | the floor switches prevent the crashes | `sponge_crash_0926` |
| cs seam root cause + multi-rotation check | `cs_seam_flux` merged (default average); `positive` stable for 12 rot, climate within noise | `seam_0926` |
| a320 vs a128 radial A/B | spin up on 128, remap to 256/320 for science | `radab_0924/ana_0926` |
| MHD and cube-vertex seam audit + vertex fix | MHD covered by `cs_seam_flux`; EMFs OK; vertex fixed by `cs_seam_resample`/`cs_seam_rho_guard` (merged) | `seamaudit_0926` |

**Still running at handover.** If their results are not added below, redo them on Caltech.

| what | viper dir |
|---|---|

## 6. Decisions waiting for the user

**FIRST CODE TASK ON CALTECH (user 09-26): make the tested speed/robustness settings DEFAULT ON.**
Today they are default-off keys that are set only in input files. Each flip changes every input that does not
set the key, so for each one:
- keep the old behaviour available with an explicit value;
- record the value with GetOrAdd;
- note in the doc that old restarts without the key change behaviour;
- rerun the regression tests (`cd tst && python run_test_suite.py --style` and `--cpu`, plus the relevant
  test inputs);
- check that problems not using the feature stay bitwise.

| # | key | new default | evidence |
|---|---|---|---|
| 1 | **DONE, merged into rt-integration 09-25 PDT (default-flips 7df65ad2):** `problem/ck_impl_kkt_row` (`src/pgen/deep_hot_jupiter_rt.cpp`); `false` restores the old rows. A restart keeps the value stored in its file; only a restart file written before the key existed (before 427f9f58) changes behaviour (it picks up true) | true | 1x production NOT-CONVERGED 20/2113 -> 0 at the same cost; keys-off and restart gates bitwise (section 4a) |
| 2 | **DONE, merged into rt-integration 09-25 PDT (default-flips 7df65ad2):** `problem/ck_impl_xstep`. Default 8 only when `ck_impl_every > 1` AND the stored operator exists (`ck_implicit`, `ck_impl_frozen_op`, not `ck_impl_refresh_kappa`; otherwise 8 would be FATAL), else 0 as before; an explicit value (0 = off) always wins. A restart keeps the value stored in its file (old runs stored 0); only a restart file written before the key existed changes behaviour | 8 when `ck_impl_every > 1` (today's value of 2 is a no-op there) | -11 % per cycle, interior within noise (ckcad retest, section 4a) |
| 3 | **DONE, merged (cs-seam-default 01df7e26); the 12-rotation dhj check of the full set passed: rot 20 -> 32, 0 collapse warnings in the run and its noise twin, min dt 7.7 s vs 0.36 s in base, floor counts 6 % below base (viper `csdef_0926/multirot_ana.txt`).
  12-rotation NOISE twin, 1e-14 kick, at rot 32: deep eint rms 4.5e-4 at 1-10 bar, mean T within 0.05 K at
  1-10 bar. Full set vs positive-only: deep eint rms 9e-3, about 20x the noise, and mean T 0.6-0.8 K at 1-10
  bar. So each seam-scheme change is a real operator difference in the deep flow: scatter, sub-kelvin mean
  shifts, no drift. It is not round-off-neutral, but it is a bug fix and required for stability:** `<mesh>/cs_seam_flux`, `cs_seam_resample`, `cs_seam_rho_guard` | positive, linear, 4 | root cause of every WASP-121b crash. `positive` is stable for 12 rotations, climate within noise. The full set was at rot 23.4 of a 12-rotation check with a noise twin on viper, 0 warnings. cs tests and cavity reproducer: `docs/dev/cs_seam_defect_0926.md` |
| 4 | **HELD (user 09-25): coded on branch default-flips f3703090, not merged; at input tolerances it differs 4-6x the round-off spread (solver-tolerance level, within noise at tol 1e-12) and its H200 gain is unmeasured (host LU + Allreduce per application may cost more on a discrete GPU). Decide after the Caltech M1 H200 timing.** `<rad_m1>/implicit_precond` (Cartesian implicit M1) | mg_gf (`implicit_gf_modes` 2, `implicit_mg_levels` 3) | He box -5.9 % (1 GPU), -1.7 % (2 GPUs); differences at the level of switching between existing preconditioners; restart bitwise |

**Do NOT make these default:**
- `vet_col_every` / `time2_vet_col = lag`: relaxation only, tested only on the static no-gravity wedge.
- cfl 0.9: an input value, and the He-box KE question is open.
- the floor switches (`dfloor_keep_velocity`, `dfloor_keep_temperature`, `vceil`): they change answers
  wherever the floor fires; the user left this open.
- the tolerances: they stay put under the round-off rule.

0. **First:** put `<mesh>/cs_seam_flux = positive` (plus `cs_seam_resample = linear`, `cs_seam_rho_guard = 4`
   after their multi-rotation check) into the dhj production inputs. `positive` passed the multi-rotation
   check. Without it every WASP-121b arm crashed within 27 rotations. Optional: a 7-rotation noise member, to
   judge the deep-eint scatter.
0. Left open by the user at handover (09-26):
   - continue nobot (and notop) with the floor switches as 100-rotation arms;
   - add the floor switches to the production inputs (suggested: relaxation phase only, until the seam root
     cause is known);
   - add them to base.

1. ck cadence e8 + x16: run a second noise member to decide it (-28 %)?
2. M1: build a gravity-bearing He spherical wedge (the first step to the presupernova He star)?
3. dhj production: confirm the grid plan (spin-up on 128, remap to 256/320), MHD vs hydro phase, node count, start date.

## 7. Working rules from the user (portable)

- **No accuracy sacrifice for speed.** Speed-ups must be accuracy-neutral in the region that counts: the
  deviation from the reference must be no larger than the reference's own ROUND-OFF spread (1 vs 2 GPUs, or a
  1e-14 kick). The time-discretization error is not the bar (user 09-26).
- **Two phases (user 09-26): cheap schemes are allowed during relaxation, accurate schemes at the steady
  state.**
  - Relaxation / spin-up may use multi-rate, the ck cadence e8 + x16, `vet_col_every`, a looser Picard tol,
    or a cruder top treatment, if the run is stable.
  - Then switch to accurate settings and check that the deep T and flux do not drift afterwards.
  - The measured steady state uses only round-off-neutral speed-ups, except the step size: hesdirk2 at cfl
    0.9 is kept in both phases.
  - Memory `two-phase-relax-then-accurate`.
  Multi-rate and similar levers are opt-in, never recommended as defaults.
- **Measure cost on the GPU**, same binary, interleaved arms, repeats. CPU runs are for correctness gates.
- **Merge finished, gated branches into `rt-integration` promptly and push to the fork.** Gates:
  - new keys default off and bitwise with the keys off;
  - restart bitwise;
  - the relevant test suite and style checks (`cd tst && python run_test_suite.py --style`).
- **Jobs:**
  - no blocking sleep/until loops in the foreground;
  - submit, check once, return control;
  - a background poll every 10 min is fine;
  - no heavy or rolling monitoring.
- **Agents:** one deliverable each, narrow prompts. Report only results, decisions and failures, not
  "still waiting" acknowledgements. No new side threads without asking the user.
- **Never write into `run/`**: old output, gitignored.
- **Keep login-node analysis memory modest:** at most 40 GB, one dump at a time.
- **Housekeeping at minimal token cost.**
- **Before claiming a machine fact** (GPUs per node, partition limits), check it with the scheduler.
  Mistaking this was a recurring error on viper.

## 8. Starting prompt for the first Caltech session

The user pastes this as the first message on the Caltech cluster:

```
You are taking over an AthenaK project that was running on another cluster (viper, MPCDF). You are new to AthenaK, and none of the old machine's files are here except what is in git.

1. Get the code:
   git clone -b rt-integration git@github.com:jing-ze-ma/athenak.git && cd athenak && git submodule update --init --recursive
   (or clone over https if ssh isn't set up). All work is on the rt-integration branch, not main.
2. Read CLAUDE.md, then docs/handover/HANDOVER-2026-09-26.md. That file is the starting point: repo state, production settings, accuracy rules, open decisions and the user's working rules.
3. Copy docs/handover/claude-memory-2026-09-26/ into your Claude memory directory and read its MEMORY.md. Those notes cite /viper/... paths that don't exist here; treat them as history only.
4. Download and unpack the run data (section 3 of the handover): the branch data-2026-09-26 of the same repo; check the md5.
5. Learn this cluster before doing anything else: the GPU model, GPUs per node, the scheduler (Slurm or not), partitions and time limits, and modules. Check each with the scheduler and site docs, not by assumption. Save them as a memory note. The viper facts in the handover (2 MI300A per node, HSA_* env vars, the apudev limits, the ms/cycle numbers) do NOT apply here.
6. Build deep_hot_jupiter_rt for this GPU (section 2 of the handover) from a git archive snapshot, and run a short CPU and GPU smoke test.

7. After the smoke test, do the "FIRST CODE TASK" in section 6 of the handover (flip the tested keys to default on, with regression tests).

Then stop and report: the cluster facts, the build status, the default-flip test results, and a proposed job-script template. Don't start production runs or long jobs until I decide. The open decisions are listed in section 6 of the handover.
```
