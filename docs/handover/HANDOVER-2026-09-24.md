# Handover 2026-09-24 (viper; session 09-23, written through 09-23 evening, Opus 5.5 medium)

Read this first, then MEMORY.md. New standing rules 09-23: housekeeping (copies/cleanup) with
minimal tokens, also for agents (in .claude/agents/worker.md); athenak/kokkos is a SYMLINK to
bench/wt_rgbox/kokkos -- never delete that directory (restored from snapshot 09-23 after it was
removed with the worktree; its dangling .git pointer renamed).

## Productions (running, untouched)
| run | at 10:55 09-23 | notes |
|---|---|---|
| prod4 MHD (bench/cs_mhd_prod4) | rot 19.1, dt 20.8 s, ~3 rot/h | eos_fail 0; FLAGS: eos_efloor ~6-7.5e5/interval flat-rising (NOTES item 2 says ~0); ME x2.8 since rot 1.2 (prod3 decayed). Pace -> ~270-290 rot over 4 links. |
| hyd4 (bench/cs_hyd4_prod) | rot 33.3, dt 20.5 s | |

## Branches (all unpushed, unmerged; worktrees on /viper/ptmp2/jinma)
| branch | commit | what | status |
|---|---|---|---|
| rt-integration | 51a9adb2 | M1 FAST PATH + vet_mb_halo 3 DEFAULT ON (eddington/vet_sc/tau) | gates passed; vet_sc 103->53 ms/cycle, Eddington 114->49 (1 GPU) |
| m1-space2 | 89dc28d7 | <rad_m1>/implicit_enthalpy = upwind(default,bitwise) / central / plm | plm: 2nd order in space everywhere (radwave 12 cases, drift, xyz 16 ranks, vet_sc), E>0 by construction, 4 % faster. tests_m1/runs_3s_space2 |
| m1-vimp | 2c3c4178 | <rad_m1>/implicit_vimp: Newton-implicit gas velocity in the enthalpy flux (option 1), on plm | all correctness gates pass (bitwise off, restart bitwise, NON-CONVERGED 0); +7.5-8 % GPU; under BE it over-damps thick P=100 waves (model-predicted) -> only useful with H-ESDIRK2. tests_m1/runs_3v_vimplicit |
| m1-time2b | 26666e8e+ | H-ESDIRK2 (<rad_m1>/time_scheme = be default bitwise / hesdirk2; time2_enth_vel = central; vet_sc uses D^n, time2_vet_extrap option) on m1-vimp | G0 bitwise CPU+GPU; G1 Eddington 1.89-2.00 and vet_sc(D^n) 1.92-2.00 in all 12 cases; stiff tau 1e5-1e6 bounded; He slab 0 nonconv/0 fallbacks; G8 restart bitwise; G7 GPU: Eddington 1.48x be (pass), vet_sc 1.57x (opacity-caching fix in progress); G5 invalid as a gate: the KE_h dt dependence is a START-UP KICK of the eint seed (T_gas != T_rad in cycle 1), not an ongoing error; from a common restart cfl 0.3/0.15 agree to 0.96-1.00 (tests_m1/runs_3o_vet3d_cfl/RESULTS.txt ROOT CAUSE). Open: order tail beyond nt 2048 (cause unknown). tests_m1/runs_3x_hesdirk2 |
| sc-scale | bbf94802 | SC sweep: one message per rank (default, exact), hybrid angle/space (off), coalesced moments | SC 14-33 % cheaper, still flat 3.9-4.2 ms 1-8 GPUs; tests_m1/runs_3q_scscale |
| m1-krylov | c1ad9964 | implicit_halo_mpi (bitwise; candidate default), implicit_krylov_pipe (1-2.6 ms) | radiation 27->22 ms at 4-8 GPUs; weak cycle 3.3-3.9x -> 2.7-3.0x hydro; tests_m1/runs_3w_krylov |
Merge order proposal: space2 -> vimp -> time2 (H-ESDIRK2); krylov + sc-scale separately (disjoint files).

## Findings 09-23
- 3-D He box CFL problem SOLVED (09-24): a start-up kick -- the eint seed leaves T_gas != T_rad and the first hydro step (before the M1 solve) acts on it for a full dt; from a common restart dt-independent to 2-4 %. (Earlier reading: coupling; hydro alone
  0.4 %, fofc inert, all closures equal). Cure = H-ESDIRK2 (G5 gate). tests_m1/runs_3o_vet3d_cfl/RESULTS.txt
- Implicit M1 radwave (tests_m1/runs_3r_radwave): frequency 2nd order, eigenmode 1st order (fixed by
  plm), time 1st order (split BE); explicit M1 far worse. vet_sc lag +15-23 % O(dt) damping.
- T4 implicit ck well-posed suite (tests_ck_implicit/wellposed/README.md): at production dt 20 s T4 is
  ~35x more accurate than semi, holds equilibrium, energy ~1e-8; at 200-2000 s ONLY T4 holds
  equilibrium (semi unstable in the thin top, T3 non-converged); H2 c_v spike breaks large dt.
  Newton globalisation optional.
- RESTART JOLT (undiagnosed, may hit productions): after ANY restart of the ck test, upper cells step
  ~5 K on the first call, then settle -- some state not in the restart. FIRST TO-DO: check whether a dhj
  production restart is bitwise (run N vs run+restart).
- noH2 A/B (tests_ck_implicit/README_noH2.md): removing H2 does not cure the night-side kinks (HSE
  transient dominates). Night-side beam (bench/nightbeam_0923): no production bug that explains the
  kinks; small real bug (top-face tau = 0 for mu0 < 0, ~1e-6 F) -- patch beam_night_top_fix.patch NOT
  applied (user decision). Kinks: next suspects = thermal top boundary, floors.
- u2: 941 -> 595 GB (9 finished bench dirs moved to /viper/ptmp2/jinma/u2_bench_archive, symlinked;
  SCRATCH -- archive what must be kept), 17 frozen worktrees removed (wt_he4 kept).

## Decisions for the user
1. Default plm (implicit_enthalpy) after vimp/time2? Default implicit_halo_mpi?
2. Apply beam_night_top_fix.patch (fabs hunk changes 22 day columns by 5.5e-6)?
3. prod4 efloor flag and ME growth: look now or at rot ~50?
4. T4: adopt at tol 1e-10 in a long A/B vs semi? globalisation?
5. Krylov next (halo overlap / 2-layer halo, block-crossing preconditioner) -- waits for m1-vimp.

## Plan agreed with the user (09-23 afternoon)
1. Merge everything finished into rt-integration via m1-int (halo agent, Step 0; gate incl. dhj production
   inputs bitwise), fast-forward rt-integration (stash the 2 uncommitted working-tree edits).
2. Default commit: implicit_enthalpy = plm and implicit_halo_mpi ON (quick gate); vimp / hesdirk2 / pipe off.
3. Beam night-top patch: gate + commit after the kink agent reports; productions stay on their binary.
4. After the merge, two agents: (a) VALIDATE vimp+hesdirk2 on nonlinear problems (Lowrie-Edwards M2/M5
   implicit, a radiation-dominated shock, thin beam/shadow, vet_sc He box cfl pair from a common
   restart); (b) ACCELERATE hesdirk2+vimp (cheaper stage 2, fold vimp +-2 terms into the stencil,
   preconditioner reuse). User's aim: vimp+hesdirk2 as the accurate all-regime scheme.
5. M1 scaling order: Krylov halo overlap (running) -> block-crossing preconditioner -> SC temporal blocking.
Running: kink diagnosis (a4c4...), T4 globalisation (branch ck-glob), merge + halo overlap (branch m1-int).

## Night-side kinks: CAUSE FOUND (09-23 evening; /viper/ptmp2/jinma/kinks_0924)
Not the RT: lambda*dt ~0.15 (semi is effectively explicit there), no clipping, no floor hits, the RT step
smooths the kinks; T4 and half dt do not reduce them. They are supersonic night-side fronts kept sharp by
HLLC on an under-resolved radial grid: the stagnation shock of the downflow, the shear/contact layer on top
of the day-night jet, and the H2 front; dr/H ~0.5-1 (band 2) and 2-3 (band 3, p < 1e-7 bar, at dfloor)
vs 0.15 on the day side -- the r-stretch (f_stretch_r_c1..4) was fitted to the hot day structure; cold
night gas has H 3-4x smaller. HLLE (0.2 rot A/B from hyd4 rst 111, rot 55.5): kinked columns 1368 -> 891,
bands 1-2 down 2-4x, alternating runs s3 335 -> 25. Cures to decide: refit the r-stretch on night-side H /
raise nx1; more diffusive solver or reconstruction near fronts; stronger dhj HLLC contact smoothing.
The fronts are partly physical -- the issue is per-cell sharpness.
Beam night-top patch: gate + commit on branch beam-night (agent running); merge after m1-int lands.

## prod4 flags checked (09-23 ~21:30; bench/prod4_flags_0923/README.md)
- eos_efloor: HARMLESS. Counters include ghost cells; the active zone has 0 cells at pfloor/tfloor (min p =
  73 pfloor, min T 411 K). All floor events are top-radial / corner GHOSTS on the night panel at dfloor (ox1 user
  BC puts night ghosts at 200 K under ~400-600 K active cells; same in hyd4). No pfloor/tfloor change needed.
- ME: WATCH. The x2.8 at rot 19 was the spin-up transient (peak 3.8e33 at rot 9.8, prod3 did the same), then
  decayed to 7.0e32 at rot 43; new day-side rise since rot 43 (+0.16/rot at rot 47-49.5). prod3's "-5.5 %/rot" was
  only a rot 25-30 window (long-term -0.4 %/rot). Check at end of link 2; act if ME > ~7e33 or active min beta < 0.1
  (now 1.1).
- CAVEAT: restart T caches are in code units (kT/amu): T[K] = T/8.3145e7; bench/impl_t4p4_0923/scan4.decode does not convert.

## Implicit M1 scope (user decision 09-24 ~01:00)
- Use implicit M1 + vet_sc (or Eddington / tau) for optically thick envelopes with a quasi-static thin top:
  He slab / box run with 0 failures. Use EXPLICIT M1 for genuine free-streaming problems (beams, shadows).
- chi(f) closures (m1 / minerbo / kershaw) with implicit transport: use trans_limit = lp 0.5 + implicit_offdiag = none
  (0 failures on the He slab; /viper/ptmp2/jinma/m1clos_0923/README.md).
- Branch m1-thin (cb7ea901, implicit_thin = hll) is KEPT UNMERGED: positive and fixes beams at c dt/dx <= 2, but
  diffuse thin radiation is unstable above c dt/dx ~ 4-8, T7 M5 shock shifts 18 cells, shadow arrival 11-17 % off.
  Follow-up if ever needed: predictor-corrector with a limited predictor (tests_m1/runs_4g_thin/README.md on the branch).
- Accepted: m1-accmerge be defaults (levers, one_pass 8, predictor order 2, conduction skip, halo_overlap) despite
  1-2 per-cell Newton bracket fallbacks per ~1.35e8 cell-passes on the GPU box.
