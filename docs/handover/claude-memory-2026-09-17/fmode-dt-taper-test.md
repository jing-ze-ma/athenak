---
name: fmode-dt-taper-test
description: B-star surface f-mode growth is NUMERICAL (O(dt): gamma 0.86 -> 0.42 /turnover when cfl 0.3 -> 0.15, ratio 0.49; frequencies dt-independent; bulk convection identical); USER HYPOTHESIS = the EOS-LTE / two-stream hand-over band (eos_rad_rho_hi/lo) drives it; taper scan + cfl 0.075 arms SUBMITTED 09-15 20:35 (jobs 11718115-8, bench/bstar_fecz/plaid, README ARMS 2)
metadata:
  type: project
---
dt test (plaid/F03 vs F015, small box 336x64x64, tracks the box's own (1,1) diagonal f-mode at 91.4 c/d = sqrt(g k), NOT the production's 139 Mm (2,2) which cannot exist there): narrow-band demodulated envelope gamma +0.864+-0.108 (cfl 0.3) vs +0.421+-0.076 (cfl 0.15), ratio 0.49-0.63 over windows; half-turnover rms x2.66 vs x1.13; linear intercept a = -0.02+-0.13; v_z at tau 30 identical in both arms (defect in the emergent-surface radiative source, not hydro). Caveats: different mode/box, 1.6 turnovers of fit, noisy beating with a 113.5 c/d companion. Script/figure bench/analysis_0915/fmode_dt/.
HAND-OVER BAND FACTS (B-star IC column): current band 1e-8..1e-9 = tau 20..2.5 (NOT 30..1 as the input comment says), 37 cells, 10 g/cm2; tau 1 at i=312 rho 1.7e-9, tau 0.1 at i=323 rho 4e-10, tau 0.01 at i=331 rho 9e-11; top cell tau 0.003. Ceiling for shifting the band up: above tau ~0.3 the column is isothermal so the LTE Prad gradient vanishes while kappa F/c does not -> the two-stream force must own the lid by tau ~0.1. Arms: F0075 (cfl 0.075) 11718115; TA 1e-9..1e-10 (tau 2.5..0.02) 11718116; TB 1.7e-9..4e-10 (tau 1..0.1) 11718117; TC 8e-10..2.5e-10 (tau 0.3..0.05) 11718118; all cfl 0.3 except F0075, same binary a1ef8822, IC unchanged (band transient, skip first 0.8 turnover). Analysis: extend fmode_dt.py (same demodulation + window). If gamma falls with band mass -> shift the production bands (B star and He at 6e-8/6e-9) at the next link; the productions restarted 09-16 morning still carry the old band.
He BAND FACTS (box_w4 IC column, hew4/rho_ic.npy + tau_cell.npy): the He band 6e-8..6e-9 is ALREADY tau 1.9..0.04 (i 111..128 of 134; = the B-star arm TA position), rad_gate_rho 6e-9 = tau 0.04; tau 1 at i=115 rho 4.1e-8, tau 0.1 at i=125 rho 1.1e-8, tau 0.01 at i=132 rho 2.5e-9, top cell tau 0.007. So the He box runs with the high band and still carries the 150 s lid pulsation (1-D damping was dt-independent there). If the B-star scan says "shift", the He band needs no change; if the B star's TA arm still grows, the He result is a second data point against the band hypothesis.
SCAN RESULT 09-15 21:20 (fmode_scan.py/.png/.json in analysis_0915/fmode_dt): gamma(TA, band tau 2.5..0.02) = +1.031+-0.115, gamma(TB, tau 1..0.1) = +0.826+-0.077 vs F03 +0.864+-0.108 -> ratios 1.19 / 0.96 (within 1 sigma), companion + broadband control the same; frequencies unchanged (doublet 92-97 + 113-115 c/d in every arm); Ftop identical to 5 digits after a -0.5 % transient of 0.3 turnover; eigenfunction below the photosphere identical. THE HAND-OVER BAND IS NOT THE DRIVER (hypothesis refuted; do not shift the production bands for this). The O(dt) anti-damping is in the radiation coupling itself; 3-D-only ingredient vs the dt-clean 1-D mode 3 = the horizontal ADI operator / its per-stage split against the column source. NEXT ARM (proposed): baseline cfl 0.3 with the horizontal operator OFF. F0075 (11718115) pending read ~22:25; TC (11718118) now superfluous.
FINAL 09-15 22:45 (fmode_scan2.*): dt LADDER F03/F015/F0075 (dt 1.50/0.75/0.37 s): gamma +0.864/+0.421/+0.177 per turnover; linear fit gamma = a + b dt with a = -0.048+-0.085 (0.6 sigma from 0), b = +0.615+-0.109, chi2 0.03; through zero b = 0.562+-0.055, chi2 0.35/2. => THE f-MODE GROWTH IS PURELY O(dt) NUMERICAL ANTI-DAMPING; at dt -> 0 the mode is neutral-to-damped (no kappa-mechanism instability). OPERATOR-OFF ARM (TR, rad_tr_tau 1e30/1e31, job 11718440) is INCONCLUSIVE AS A DRIVER TEST: without horizontal radiative smoothing the lid goes TRANSONIC (Mach 1.2-1.7 in 67k cells, Ftop horizontal rms 0.21 vs 0.001, no discrete mode line, mass/E drift 1e-3), gamma(TR) 0.205 is broadband noise; lesson: the horizontal operator is REQUIRED for a quiet lid (the plaid of prod 1 was the tapered version of this). Remaining candidates for the O(dt) term (all per-stage explicit couplings of the column to the hydro): the radiation force applied with the stage-start flux, the column heating applied with the stage-start state; tests are code changes (stage-end / IMEX centering), not switches. Production prod_w4/prod_w4_p at dt 1.49 s carries gamma ~0.10/turnover of artefact on the (2,2) mode; cfl 0.15 halves it at 2x cost.
FIX ATTEMPT 09-16 00:20 (rt-integration, wt_merge, commits 6ed1fb66 + 82d2c619, NOT pushed; notes analysis_0915/fmode_dt/coupling_notes.md): per-stage sequence = fluxes from stage-start w0 -> RKUpdate -> BoxConvSrcs (gravity etc.) -> two-stream on u0: entry sweep, then the exact column writes u0(IEN) implicitly, then rt_apply applies ONLY the radiative momentum FORCE, explicitly, driven by the ENTRY SWEEP's flux (not the converged one) -> ADI horizontal operator -> ConToPrim. Switches (default off, CPU bitwise off vs base_g3a/g3b): problem/rt_force_center 1 (force with this stage's converged column flux) / 2 (mean entry+converged); rt_src_theta (factor-space re-centring backward-Euler -> exact exponential; the naive theta blend blows the He column up 1e5x; diagnostic, non-conservative, forces the entry sweep on); rt_strang already EXISTED (BoxConvRTSplit via user_split_func, half-dt column + force before/after the integrator). HIP binary wt_merge/build_hip_center md5 cc301383. ARMS 4 running from 00:15 on the small box, cfl 0.3, plaid/fmode_c.sh + fmode_c.athinput: C0 control 11720875 (must give gamma +0.86), C1 fc=1 11720876, C2 fc=2 11720877, C3 theta 0.5 11720878, C4 strang 11720879. READ: extend fmode_scan2.py (91.4 c/d band, window 8e3-2.4e4 s) against C0; the arm at gamma ~0 names the term. ImEx variant (branch rt-imex, bench/wt_imex, agent) in progress in parallel -> arms X0/X1.
IMEX BUILT 09-16 00:40 (branch rt-imex, bench/wt_imex, commit 5323923b, NOT pushed, notes bench/imex_0915/imex_notes.md): problem/rt_imex (needs time/integrator imex2 or imex2+ and mode 3) makes the WHOLE radiation operator (energy + force + work, 4 stored components) the implicit stage operator of the driver's ImEx scheme (a_impl 0.5 for imex2; a_twid re-applies stored stage sources; the radiation module itself never used it, only ion-neutral). Hooks: pgen user_imex_func, Hydro::RTImExFirst/RTImEx in hydro_tasks.cpp. Gates: bitwise off; imex2+rt_imex=false bitwise = rk2; on: stable, Ftop differs from rk2 by 5.9e-3 at cfl 0.45 and 2.5e-3 at 0.225 (the O(dt) split error converging away), B star 336x8x8 clean, 1.09x cost; 1-D lid mode damping dt-independent under BOTH (not the f-mode driver). Caveat: imex2 pre-stages run before the stage-1 ghost exchange (ghosts one pre-stage stale); imex2+ has no pre-stages. HIP wt_imex/build_hip_imex md5 1c5ebc6e. ARMS 5: X1 imex2+rt_imex 11720903, X0 control rk2 11720904 (plaid/fmode_x.sh, fmode_x.athinput), pending at 00:40. Read with fmode_scan2.py (X1 vs X0, and C0-C4 vs C0).
ARMS 4+5 READ 09-16 01:00 (fmode_scan3.*): NO FIX. Controls C0/X0 = F03 exactly. C1 (force with converged flux) is a NULL test: under rt_col3_skip_sweep the force ALREADY uses the converged column flux (bitwise = C0). C2 trapezoidal: +0.867 (unchanged). C3 theta 0.5: +0.97, contrast x2.7 (unchanged/worse). C4 rt_strang and X1 imex2+rt_imex BROKE the solution (surface contrast x100/x68, <Ftop> drifts 1 %, C4 v_z rms 10x and growing 5.8x faster = unstable) -> their low gamma is an artefact; both implementations need debugging before they mean anything. => the per-stage force/heating coupling as tested is NOT the O(dt) driver. Remaining O(dt) candidates: the rad weights/kappa/tau frozen once per CYCLE (a lagged opacity response at the iron bump = a numerical kappa-mechanism; check whether the column's kappa is per-stage under skip_sweep), the ADI horizontal operator's Lie split with the hydro (applied after the source each stage), the SSP-RK2 combination of exactly relaxed stage states. Cheap switch-only arms next: time/integrator = rk3 and rk1 at cfl 0.3 (integrator dependence at fixed dt).
ARMS 6 SUBMITTED 09-16 02:00 (branch rt-kappa, bench/wt_kappa, 134838c7+91326d43, unpushed; notes bench/kappa_0916/kappa_notes.md): STEP-1 TABLE: column kappa kc_g, Planck, T are per stage from the RT-entry u0 (frozen over the Newton iterate, deliberately: a per-iterate kappa would make Newton inconsistent -> rt_kappa_iter NOT built); the force uses the CONVERGED flux; BUT rad_tauf/rad_w (tau + blend weights, RT cut, sponges) and the ADI conductances capc2/capc3 are built from w0 = STAGE-START primitives (one sub-step stale) = the remaining O(dt) lag in phase with rho'/T'. Switch problem/rt_weights_per_stage (ConsToPrim over the full range + BuildRadWeights + BuildAngularCoeffs at each RT call): bitwise off, 1-D on = 2e-14 in Ftop, 1.62x cost/cycle. HIP build_hip_kappa md5 18001d83. Arms K0 control 11721449, K1 11721450 (plaid/fmode_k.sh); also R1 rk1 11721219, R3 rk3 11721220 (production binary). Read all with fmode_scan3.py + the sanity gate.
ARMS 6+7 READ 09-16 02:30 (fmode_scan4.*): ALL NULL AGAIN. rk3 (3 column solves/cycle, same dt) gamma +0.847 = rk2 +0.864 -> gamma tracks dt per CYCLE, not the stage count/bdt (rk1 not comparable: lid breaks, contrast x13). K1 per-stage tau/blend weights + ADI conductances: +0.891 (unchanged). Debugged Strang S1 and ImEx X2 (RTOpSplitBvals ghost fix, rt-imex) STILL break the lid (contrast x106/x65, Ftop drift 1 %, S1 v_z 7x growing) -> their gammas meaningless; the ghost update was not the whole defect. Controls K0/X00 = F03 to 1e-10. INTERPRETATION: the O(dt) driver is the split ITSELF - each cycle the hydro compresses adiabatically, then the column relaxes T fully to the radiative equilibrium of the compressed state (numerical thermal lag = dt instead of the physical t_rad << P), giving work in phase with compression at the iron bump = a numerical kappa-mechanism proportional to dt and independent of how the stages are arranged. Only a time-centred split (Strang/ImEx) or a truly coupled hydro+radiation implicit step removes it; the existing Strang/ImEx implementations must first be made to preserve the quiet lid (suspect: the horizontal ADI operator and the sponge/open-top/wall sources being applied to states inconsistent with the moved column call; the TR arm showed the lid needs the horizontal operator consistent with the column every stage). Branches: rt-kappa (weights switch, keep as an option, 1.24x), rt-imex (Strang/ImEx WIP). Production keeps the artefact (gamma 0.1/turnover on the (2,2) mode at dt 1.5 s); cfl 0.15 halves it at 2x cost.
STRANG/IMEX DEBUG 09-16 03:00 (rt-imex, wt_imex, commits 14533f4e + fe89e41f, PUSHED): defect 1 = the moved column call wrote active cells only with no ghost/physical-BC update (RTOpSplitBvals fix, 14533f4e) - real, but NOT the mechanism (contrast x100 persists). Defect 2 (hypothesis, fe89e41f): the column and the horizontal ADI must act TOGETHER in the lid (column = projection onto radiative equilibrium, ADI = very fast horizontal diffusion chi ~5e17; back-to-back per stage they nearly cancel; moving only the column out leaves the ADI acting on an unrelaxed state -> same breakdown as arm TR). problem/rt_split_transverse (default true under strang/imex) moves the ADI into the split/implicit operator. ARMS 8 running 03:00: S2 strang 11721843, X3 imex 11721844, X01 control 11721845 (plaid/fmode_x.sh, binary 1ce37b6f). READ with fmode_scan4.py + sanity gate; if the contrast is still ~0.1 the remaining suspect is the effective-step mismatch (column over dt/2 or a_impl dt vs hydro fluxes/sponge/open top at stage weights) -> the lid may need an implicit hydro top face. Diagnosis method that worked: 336x8x8 CPU, profile dump every 2 s, first departure in rows 280-336 (lid) at cycle ~35.
ARMS 8 READ 09-16 03:10 (fmode_scan5.*): rt_split_transverse CURES the lid breakdown (S2 strang / X3 imex contrast 1.2-1.4e-3 = control, <Ftop> exact, mass/E clean) BUT gamma UNCHANGED: S2 +0.834+-0.088, X3 +0.861+-0.096 vs control +0.864. => the hydro/radiation SPLIT is NOT the driver (the split-lag interpretation above is refuted for the centred splits). ARMS 9 submitted 03:15: T0 closed top (bc_mode_top=-1, rk2) and X4 imex2+ (stiffly accurate 4-stage ImEx; imex2 is not stiffly accurate -> stiff order reduction to O(dt) is the remaining scheme-side suspect); plaid/fmode_x.sh, binary 1ce37b6f, control X01. Read with fmode_scan5.py.
ARMS 9 READ 09-16 04:15 (fmode_scan6.*): CLOSED TOP T0 gamma +0.756+-0.107 (unchanged; the closed lid quiets the 113 c/d companion and the broadband but not the f-mode band; mass drift exactly 0). imex2+ (stiffly accurate, 4 implicit stages) X4 +0.789+-0.126, sanity passed (unchanged; +24 % wall). => neither the open-top boundary lag nor stiff order reduction. EVERY arm sits at gamma/(0.615 dt) = 0.82-0.94. FAVOURED READING: effective thermal time = dt (every scheme relaxes the lid fully within one step, so the kappa-mechanism work integral is evaluated with t_rad -> dt); the DECISIVE TEST is to SUB-CYCLE THE WHOLE COLUMN (mode 3 + ADI) N times per stage with bdt/N at fixed hydro dt: gamma should follow dt/N (~+0.43 at N=2, ~+0.22 at N=4). NOTE rt_relax_sub is the OLD per-cell source sub-cycler, inert under mode 3 -> a new switch (e.g. rt_col3_sub) is needed: loop the column call inside the stage. If confirmed: the physical growth is ~gamma x t_rad/dt (tiny), and the production cure is an asymptotic-preserving / sub-cycled lid or simply accepting the artefact as known and small at cfl 0.15.
SECOND OPINION (Opus 5, 09-16 05:00) - THE KEY LAW: gamma = C omega^2 dt with C = 1.31+-0.1 (omega^2 = g k_h): fits F03/F015/F0075 (1.31/1.28/1.08) AND the production (2,2) mode (predicted 0.127/turnover, observed 0.104) = a RESTORING FORCE APPLIED ~2.6 STEPS LATE (harmonic oscillator with lagged restoring force: gamma = omega^2 tau/2, positive for any lag, no opacity derivative needed). Consequences: (1) the column sub-cycling test is PREDICTED NULL (the relaxed-lid column solve is an idempotent projection; N sub-steps = 1); only a non-null would falsify the stiffness picture; (2) the invariance under rk2/rk3/imex/Strang is exactly what a stiff projection gives (order reduction) - so the radiation arms could never discriminate; (3) NO ARM HAS TESTED THE HYDRO. Ranking: H1 hydro/gravity/EOS step first-order for this mode (WB gravity source read from stage-start w0 = a lagged restoring force; taper Gamma_1 in the Riemann solve; ADI Lie split) >~ H2 lagged EQUILIBRIUM PRESSURE (the favoured reading restated correctly) > ADI projection > explicit radiative force (estimated 0.16/turnover = 19 % of the total, a contributor not the driver) >> band/taper/inversion (refuted by the band scan) > top BC (refuted) > floors. The T'-rho' phase from rt_profile.bin is DEAD ON ARRIVAL (predicted lag 0.03 rad, unresolvable at 100 s cadence). DECISIVE ARMS: (A) RADIATION-FREE LINEAR f-MODE: relaxed profile, no convection, closed top, RT + ADI + force OFF (LTE Prad everywhere), seed (1,1) at 1e-4, 3 periods at dt and dt/4 -> gamma = 1.3 omega^2 dt means the hydro step is the driver; ~0 means radiation is required; (B) the same with radiation on + per-term work integrals (pressure, WB gravity, column heating, force, ADI), sum = 2 gamma E as closure, + a kappa-frozen variant; (C) same-box k-scan at fixed dt for the omega^2 law. Production artefact fully accounted for by the law; physical growth consistent with zero. Cure if hydro: fix the lagged source (evaluate the WB gravity / equilibrium pressure consistently), else dt or an asymptotic-preserving lid.
ARMS 10 SUBMITTED 09-16 05:20 (rt-imex 329af6a3, problem/rt_col3_sub N: whole column + ADI N times per stage with bdt/N, weights rebuilt per sub-step; bitwise off; 1.62x at N=2): N0 11722417, N2 11722418, N4 11722419 (plaid/fmode_s.sh, binary e9078f92). PREDICTED NULL by the second opinion (idempotent projection) -> a non-null falsifies the stiffness picture. Reader fmode_scan7.py (extends scan6). ARM SET L (radiation-free seeded linear f-mode, the decisive hydro test) being staged in bench/linmode_0916/.
ARM SET L READ 09-16 05:50 (bench/linmode_0916/read.*; branch rt-linmode 617e01ea in bench/wt_lin: seed_fmode_amp/m/n, fmode_hist hst cols fmAc/fmAs, ic_hse_retune; radiation off = drop <hydro>/isotropic_conduction + rt_two_stream=false; LTE Prad everywhere via eos_rad_rho 1e-20/1e-21; hydrostatic column re-derived by mk_hse.py to 1.9e-3 c_s residual, (0,0) only): THE RADIATION-FREE BOX DOES NOT SUPPORT THE SURFACE f-MODE AT ALL (closed or open top: no line within 5 % of 945.6 s, in-band power 1-3 %); the open-top box carries a different dt-CONVERGED oscillation at P ~ 1770 s (waveforms at cfl 0.3 and 0.075 correlate 0.9992); growth there is dt-INDEPENDENT (1.1e-4 vs 9.5e-5 s^-1) and the (2,2) seed DECAYS -> H1 (hydro/gravity step is the O(dt) driver) REFUTED; the 1.31 omega^2 dt law does not survive removing radiation. The f-mode is a RADIATION-HYDRO mode (needs the radiative relaxation to exist at sqrt(gk)); the O(dt) lag therefore lives in the coupled system. NEXT = ARM SET B: radiation ON, same seeded linear setup (no convection), PER-TERM WORK INTEGRALS band-passed at the mode (pressure, WB gravity, column heating, radiative force, ADI) closing to 2 gamma E, + a kappa-frozen variant; secondary: identify the 1770 s open-top mode before trusting the (1,1) projection.
ARM SET B FIRST READ 09-16 05:45 (bench/armB_0916, rt-linmode e765a5c9: problem/work_hist per-term mode-projected work budget with exact closure, rt_kappa_frozen; BUG FIXED: BoxConvHistory zeroed fmAc/fmAs in any radiating run): the SEEDED LINEAR RADIATING box (no convection) SUPPORTS the mode (line at 991 s, 4.8 % above sqrt(gk); noise control 9 decades below). WORK BUDGET in units of 2 gamma E: flux -7.4, gravity +7.2, RADIATIVE FORCE -0.02 (RETIRED as driver), column heating +1759, ADI heating -1798 -> THE GROWTH IS THE IMBALANCE OF THE COLUMN-HEATING / HORIZONTAL-ADI PAIR, each ~1800x the growth and nearly cancelling = exactly the pair a Lie split evaluates at different states. kappa frozen: gamma x0.67 (partial contribution, not the mechanism). BUT in the linear box gamma(B03) = 2.8e-4 = 3.2x the 1.31 omega^2 dt law and the (2,2) seed gives ~0 -> the omega^2 law fails there (recipe-sensitive: a slower component beats with the line); the B dt ladder (B0075 11722768, B00375 11722770) + BS03 strang 11722787 + BF0075 11722769 decide. NEW FIX CANDIDATE: symmetric (Strang) ordering of ADI and column WITHIN each RT call: ADI(bdt/2) -> column(bdt) -> ADI(bdt/2) (cost one extra ADI = 2.4 %), or Picard-iterate the pair to consistency; verify with Wtco+Wtad across dt.
ARMS 10 READ 09-16 06:05 (fmode_scan7.*): sub-cycling the column N=2/4 per stage at fixed hydro dt: gamma +0.927/+0.946 vs control +0.864 (unchanged; sanity pass, N4 contrast at the 2x edge) -> the second opinion's prediction HELD (idempotent projection); the effective-thermal-time reading is REFUTED. Together with arm set B's budget the driver is the column-heating / horizontal-ADI pair imbalance, not the step over which either is relaxed; the symmetric-pair arm (C03) is the live test.
INTERIM 09-16 06:20 (armC_0916/read.json): in the LINEAR seeded radiating box gamma is dt-INDEPENDENT (B03 2.78e-4, B0075 2.60e-4 s^-1) and unchanged by the symmetric column/ADI pair (C03 3.09e-4) or Strang (BS03 2.93e-4), while the pair sum Wtco+Wtad swings 15x across those arms -> the pair imbalance does NOT set the growth (the set-B 'budget names the pair' reading was over-interpreted; the thermal-work terms are not part of the exact KE closure). The linear box's growth (3x the convecting box's rate at cfl 0.3, 12x at 0.075) is NOT the convecting box's O(dt) growth: either a genuine dt-independent instability of the linear radiation-hydro model damped by convection in the full box, or non-modal transient growth of the seed over only 4 periods (recipe-sensitive 2.6x). Discriminator requested: 20-period arms B03L/B0075L (gamma over periods 5-20) + amplitude check B03A (seed 1e-3). kappa-frozen still takes 1/3 in both boxes.
ARM SETS B/C FINAL 09-16 07:00 (armC_0916/README, rt-linmode a479afa3 pushed): RETRACTION - THE SEEDED LINEAR RADIATING BOX DOES NOT GROW: over periods 5-20 (B03L) gamma = +8.5e-6 +- 1.6e-5 s^-1 (consistent with zero, <0.1/turnover), the amplitude ladder is non-monotonic, so every 4-period gamma of arm sets B/C was a beat/transient; the "pair imbalance" reading is refuted independently (the pair sum ANTI-scales with dt and Strang cuts it 14x at constant gamma). rt_pair_sym (symmetric ADI-column-ADI, 1.40x) is a NULL; do not adopt. SOLID FACTS: (1) the convecting box grows at gamma = 1.31 omega^2 dt; (2) the seeded mode WITHOUT convection is neutral at every dt; (3) every radiation-internal rearrangement is null. => THE O(dt) DRIVER NEEDS THE CONVECTIVE BACKGROUND: the natural candidate is the Lie-split commutator between ADVECTION (hydro flux, ~v.grad) and the stiff radiative projection, which is proportional to the convective velocity and vanishes at rest; order reduction under stiffness explains why Strang/ImEx did not remove it. Tests must be in the CONVECTING box (plaid/fmode.athinput vs F03, work_hist on): (a) reversed Lie order within the stage (radiation BEFORE the flux update) - a commutator flips sign -> gamma should turn negative; (b) advect the equilibrium state / asymptotic-preserving treatment of the relaxed lid. Production artefact unchanged (saturated at ~1 km/s layer mean in prod_w4_p since turnover 24).
He BOX SURFACE MODE IDENTIFIED 09-16 08:00 (analysis_0915/he_mode_0916): (0,3) along x3 (3 wavelengths, 3.68 Mm = 5.26 H_p, k 1.71e-8), 52-56 % of the v_z variance at tau 1-20, frequency = sqrt(gk) to 0.4 % (P 76 s; the 75.22 s rt_surface cadence ALIASES it - use the 10 s profile 2-omega line), surface-trapped (e-fold 1.07 Mm = 1.8/k), T' in phase with v_z. HISTORY: decaying over turnovers 4-9 (-2.7e-5/s), flat 9-14, GROWING since ~14 at +7.1e-5/s (e-fold 3.7 turnovers), |A| 0.10 now; F_top rms flat (mode taking over the variance from convection). LAW: 1.31 omega^2 dt = 8.2e-4/s predicts an e-fold of 0.33 turnover -> observed is 9 % of that and was negative earlier => the He box does NOT follow the B-star artefact law; numerical anti-damping nearly balanced by physical damping, or the He mode is not purely O(dt)-driven. Same class as the B star's mode, different quantum. NOTE the page maps' He "lanes" at tau 1-20 are these stripes; convection visible only at tau 50.
ARMS 12 READ 09-16 08:35 (fmode_scan9.*; rt-linmode 0aab7684 pushed; problem/rt_before_flux via a new pre-flux task Hydro::RTBeforeFlux + pgen hook): REVERSED LIE ORDER (radiation before the flux update) gives gamma +1.206+-0.091 vs control +0.865: NO SIGN FLIP (14.7 sigma from -gamma), mildly LARGER -> the advection/radiation COMMUTATOR is REFUTED. The O(dt) anti-damping is sign-definite and order-independent yet requires the convective background (neutral without convection). RV1h (reversed, cfl 0.15, job 11723223) pending read: tells whether the reversed arm is still O(dt). STATUS OF THE HUNT: every ordering/centring/operator-internal candidate is null; remaining ideas: (a) a term EVEN under operator exchange and proportional to dt and to the convective amplitude - e.g. the per-stage RESET of the lid to radiative equilibrium acting on convectively advected T' (a rectified, not commutator, effect), (b) the SSP-RK2 averaging of a projected state (0.5 u^n + 0.5 u^(1) is not on the equilibrium manifold), (c) the WB gravity source built from stage-start w0 in a convecting flow. Production artefact: saturated ~1 km/s layer mean in prod_w4_p since turnover 24; He (0,3) growing slowly since turnover 14.
RV1h READ 09-16 09:10: reversed order at cfl 0.15 gives +0.625+-0.119 (RV1h/RV1c = 0.52 = the dt ratio): the reversed ordering is ALSO O(dt) through zero, at 1.35x the standard ordering at both dt. Final: the driver is a sign-definite, order-independent, dt-proportional term that needs convection (lag/dissipation-like, not commutator-like). Reader fmode_scan9.*; ARMS 12 in the README.
