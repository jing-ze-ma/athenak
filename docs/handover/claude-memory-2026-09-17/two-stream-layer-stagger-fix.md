---
name: two-stream-layer-stagger-fix
description: 2026-09-14 TWO-STREAM SWEEP BUG FIXED on rg-box-convection (02f7c2fd RTLayer + flag, a4bdad5d grey sweep, d4a0f288 pgen wiring; flag problem/rt_layer_legacy, default fixed): layers spanned a whole cell in dtau but the linear source ran centre-to-centre (half-cell stagger) -> F_code/F_exact 0.947-0.964 at tau 1-10, wandering 0.73-1.06 at dtau/cell 3-6; NOW layers between cell centres with the face intensity through half-layers: 0.999-1.001 at every face tau 0.3-300 (Gate A, accurate formal solution, He column). Bitwise under legacy. B-star column changes 1-2 % at the thin lid, 0.3 % rms. EVERY existing relaxed column (He, B star) was relaxed with the buggy sweep and is ~4 % out of balance under the fix -> re-relax (agents 09-14). PORTED + PUSHED to fork polar-average-perf 09-14 (1504f0d3 -> d8f00d49, worktree bench/wt_pap): cut BC fe15ba09, limiter b477d6af, sweep f76f4da0/3281e3b8, HLLD tol 783a460a, wiring 47d496b2 (red_giant + deep_hot_jupiter_rt), ck + picket-fence centre-to-centre layers d8f00d49 (kfar carry, no extra lookup, CPU -7 %). Legacy flags bitwise (ck column, picket-fence dhj, red giant 200 cycles); 4/4 rad + 16/16 lwave pass unchanged; ck 1-cycle heating moves <= 6 % of peak at dtau/cell 0.03-3. RED GIANT with new defaults: dens/eint 1e-8 but RADIAL VELOCITY 25 % rms (ramp-layer flux) -> its state will readjust. No GPU build of wt_pap yet. Also fixed: cut BC 41196b70 (I_up = B + mu dB/dtau), limiter fc15fa06 (c a T^4). rad_blend_use_2s abandoned (blows up).
metadata:
  type: project
---
Gate material: bench/hestar_fecz/gate_layer/ (formal.py, gatec.py). Flux dump for the grey path: problem/rt_dump_file (per-face
Fb at cycle 0). See [[rt-shallow-ramp-missplit]], [[rt-handover-semi-implicit-bug]], [[fecz-column-relaxation]].
