---
name: rt-integration-branch
description: rt-integration (bench/wt_merge, 6c0df36b, 09-15 evening): rg-box-pcr + lhlld + implicit-transverse-raddiff + polar-average-perf merged, all bitwise gates pass; ONE open question (ck sweep lost the mode-3 Jacobian); GPFS quota at the hard edge
metadata:
  type: project
---
HIP BINARY of rt-integration c177c5a5: wt_merge/build_hip_rti md5 8eaf53de, gated on the B-star restart smoke (bench/prod_w4_p_gate): NOT bitwise vs the production binary, but the production binary is NOT bitwise against ITSELF on MI300A either (first record 9e-15 rel, 1.3e-2 by 200 cycles: nondeterministic GPU/MPI reductions); rti-vs-prod spread is SMALLER than same-binary spread, ms/cycle equal -> usable for production links; GPU bitwise gates are impossible on this box, gate on CPU instead. PUSHED to fork at c177c5a5 (was fd0cf4ff) (profile dump merged 8a614e1f; dhj refuses rt_implicit_column fd0cf4ff; red_giant still silently IGNORES rt_implicit_column until S3 wires it). Built 09-15 evening (user go; merge order lhlld -> itr -> pap, settling the handover discrepancy). Base rg-box-pcr 235b05f0 (NOT yet incl. 974a6ad8 profile dump: merge it in). Gates: rg 60 cycles bitwise vs pap binary; dhj ck 30 cycles bitwise; He 1-D + B-star 336x8x8 box bitwise vs 235b05f0; --cpu suite 215 pass, 30 fail = the two dyngrmhd 'compose' shocktube tests, pre-existing; style hits unchanged (464). Resolutions mostly "ours" (box superset); ck/picket-fence sweep hunks took pap's centre-to-centre layers (d8f00d49) which DROPS the ALI/mode-3 Jacobian on the ck path only -> mode 3 + rt_ck would build a zero Jacobian: options (a) startup refusal (~10 lines, recommended, nothing uses it), (b) revert to box hunks (dhj no longer bitwise), (c) port the Jacobian into the ck branch (50-80 lines). ali_on kept as `rt_ali_diag && band_on`. rad_x1_uform test: box version kept, pap's explicit-transverse variant discarded. Gate logs bench/merge_0915/. Not pushed. QUOTA WARNING: viper_u2 998.9 G of 1 T and 261203 of 262144 inodes (hard) -> new worktrees fail with EDQUOT; wt_rgbox 26.9k files, wt_m3acc 23.7k, wt_pap 8.6k, ~20 detached wt_* 3.5-5.8k each; the main repo carries 6 build_hip* dirs of ~850 files.

**FIVE-BRANCH MERGE DONE 2026-09-16, PUSHED to fork at b775a270** (wt_merge; merges 73df4a4a rt-kappa, a6c1adc2 rt-imex, e26882fe rt-linmode, ef64c235 rt-fastcol, + fixup b775a270). Every switch kept: rt_weights_per_stage, rt_imex/RTOpSplitBvals/rt_split_transverse/rt_col3_sub, seed_fmode/fmode_hist (+ the BoxConvHistory slot fix), ic_hse_retune, work_hist, rt_kappa_frozen, rt_pair_sym, rt_before_flux, rt_impl_reuse/_rho, rt_col3_split_deep, rt_impl_mixed, rt_col3_hybrid_tau.
Conflicts (all in box_convection.cpp): member decls kept from both sides; ONE BoxConvRebuildRadWeights (rt-imex body with the phydro null guard, merged doc comment); the in-stage RT call took rt-linmode's rt_pair_sym branch with the rebuild trigger widened to (nsub > 1 || rtwps_). rt_report_every read once (rt-fastcol's), auto-merged clean.
BUG FOUND AND FIXED in rt-imex (b775a270): it inserted an UNCONDITIONAL Hydro::ConToPrim between the ImEx pre-stages and Fluxes, which is NOT a no-op under a general EOS (refreshes wtemp, shifts the next inversion a few ULP) and broke the default-off box bitwise gate at the first record. Wrapped as Hydro::RTImExConToPrim, a no-op unless user_imex_func is enrolled. ANY future branch adding a stage task must gate it on its hook.
Gates (CPU serial Release, logs + binaries bench/merge2_0916/): G1 box defaults BITWISE vs a fresh 334be4d3 build (He relax1d_m3 200 cycles + B-star prod_w4 336x8x8 20 cycles, hydro.hst + user.hst); G2 rg 60 cycles + dhj ck 30 cycles BITWISE; G3 all 12 switches run 20 cycles clean, no NaN, Ftop 4.2146e13 (ctl) with rt_before_flux the only visible outlier at 4.2177e13 and Fcut = F_bot = 4.2140e13 everywhere (work_hist REQUIRES fmode_hist); G4 cpplint classes on the 8 changed files IDENTICAL to every parent, line_length 48 -> 46 (rt-fastcol removed two), no new class; G5 HIP build_hip_all OK, md5 887d1471485ec540d548e2c0865c7bf2, strings carry rt_impl_mixed/rt_before_flux/vdamp_bot_cells/rt_profile_dt.
HIP configure needs BOTH the openmpi_gpu bin on PATH and -D CMAKE_PREFIX_PATH=/mpcdf/soft/RHEL_9/packages/x86_64/rocm/6.3.4 (rocthrust), else cmake fails; CPU box binary md5 6c41407c6c72d0200f3b8bf58df6eefa. Gate recipe: bench/merge2_0916/run_gates.sh + g3.sh (CLI cannot add new params -> g3.sh injects them into a copy of the athinput).
