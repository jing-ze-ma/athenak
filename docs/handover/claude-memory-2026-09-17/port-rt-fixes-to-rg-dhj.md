---
name: port-rt-fixes-to-rg-dhj
description: STANDING INSTRUCTION (user, 2026-09-14): every RT / conduction fix from the FeCZ box work must ALSO be applied to the red giant (red_giant.cpp, grey split path) and the deep hot Jupiter (deep_hot_jupiter_rt.cpp) INCLUDING the correlated-k RT path, on polar-average-perf. ALREADY PORTED (d8f00d49): cut BC, limiter c a T^4, centre-to-centre sweep layers (grey + ck + picket-fence), HLLD_BX_ZERO_TOL. PORTED 09-14 (146c513d..91adfba8, pushed): T-form solver 30ca5ec0, rad_x1_verbose 6ec8bfcc, rad_x1_uform/kiter b605d2e5 (hand-ported; spherical factors inherited via C_f and volume), rt_use_cons docs 069406f2 (red giant ALREADY runs rt_use_cons=true; dhj default false -> 4e-3 in bcc3 if switched), uform test + rad_diff2d 91adfba8; all bitwise off on rg 200 cycles + dhj ck column. CAUTION: on the red giant the u-form makes the frozen-K audit 1.35x WORSE (its error is not T^3-dominated) -> keep OFF there until examined. PORTED 8fce25af (pushed): rt_ali_diag DEFAULT ON on polar-average-perf (hand-applied; ONE apply block serves grey + ck + picket-fence, gated on band_on); legacy false bitwise on rg (nx1 120) + dhj ck column; new default: rg changes confined to the thin upper atmosphere (i 73-100/120, 2e-3), dhj KE 13-23 % at cycle 100 (tot-E 5e-6); rad + lwave suites pass, ck thresholds untouched; rt_ali_diag = true ADDED to inputs red_giant_fofc, red_giant_cs, deep_hot_jupiter_rt_eos (CLI cannot add params). STILL TO PORT: (1) the exact implicit column two-stream rt_implicit_column = 3 (two_stream_column_implicit.hpp; grey only so far - the ck path needs its own block structure: block size 2 N_B N_G + 1, or restrict the implicit column to the thermal channel with the stellar bands explicit - see rt-source-dt-forcing.md) - this is the fix for the 30 % ramp heating loss of the per-cell semi-implicit scheme, which rg and dhj share at their ramps; (2) rad_x1_uform + rad_x1_kiter 2 (radial conduction u-form; spherical geometry factors via curvg/vol_/area1_); (3) rt_use_cons = true for rg/dhj (they still hand the sweep the stage-start state w0); (4) the user-hist emergent-flux outputs (Ftop, Fcut, surface dump) if useful; (5) the radial solver symmetric T-form 61662b2a (on implicit-transverse-raddiff, not yet on polar-average-perf); (6) the restart-potential fix pattern (929d752d) - rg/dhj checked clean. Gates per port: legacy-bitwise, rad suite, RG restart + dhj ck column comparisons, formal-solution flux check on a dhj column.
metadata:
  type: feedback
---
**Why:** the user's science runs are the red giant and the hot Jupiter; box-only fixes leave the production codes with the
same defects (the per-cell semi-implicit two-stream loses 30 % of F at the B-star ramp; rg/dhj ramps have dtau/cell of the
same order).
**How to apply:** after each box-branch fix is gated, cherry-pick/port to polar-average-perf (worktree bench/wt_pap), extend to
the ck kernel, run the rad suite + legacy-bitwise + a column flux gate, push to fork. See [[two-stream-layer-stagger-fix]],
[[bstar-top-flux-deficit]], [[rt-source-dt-forcing]], [[conduction-frozen-k-error]].

**MODE-3 PORT SCOPED + FIRST ATTEMPT 09-15 evening (RESULT: a wholesale header copy is INVALID).** Mode 3 is not pgen code: it lives in src/utils/two_stream_rt.hpp (box branch 4567 lines vs pap 3545) + new headers two_stream_column_implicit.hpp (RTCol3, RTCol3Launch) and two_stream_column_partition.hpp (PCR/team path) + rad_taper.hpp; the box pgen only plumbs ~25 GetOrAdd knobs (box_convection.cpp L1113-1320) and sets `ts::rt_bot_flux = pc->rad_flux_inner` (L1301) - that one assignment is the rg gap. Mode-3 gate `(rt_grey && rt_split) && !rt_ck`, needs rt_use_cons + rt_src_direct true, rt_top_re false. ck path: 2*11*8*ck_nq+1 = 177/354 unknowns per interface -> full block-tridiagonal infeasible on GPU; recommended = thermal channel implicit (5x5 RTCol3) with the stellar-band chains explicit via the existing frozen Qb source. ATTEMPT: copying the box header wholesale into wt_pap drags in conduction.{hpp,cpp} + conduction_transverse.cpp + bvals (windowed CC exchange) + eos_table (13 files, +3661/-991; patch saved bench/port_m3/wholesale_sync.patch + new_*.hpp; tree RESTORED clean). rg bitwise (nx1 120, 60 cycles) but dhj ck DIFFERS at cycle 1 (2-mom 3.3 %) with every knob off, and localized to two_stream_rt.hpp: the overwrite DISCARDS 7 pap-only header commits (0aa55a62 tau-blend, 1504f0d3 Newton safeguard, fe15ba09 cut gradient, f76f4da0 rt_layer_legacy, 3281e3b8 grey centre layers, d8f00d49 ck/picket-fence centre layers, 8fce25af ALI default) that were never merged to rg-box-pcr (merge-base 8a000f54). So the port MUST be a real git merge (or cherry-picks) of rg-box-pcr into polar-average-perf, which is the open merge-order decision (lhlld vs implicit-transverse-raddiff first) = USER call. Also flagged: eos_table.cpp on the box branch carries a non-neutral fix (`es += a_rad tmin^4 / 10^xi`, was 10^xlo) that fires with eos_radiation=true. Style: run_test_suite.py --style fails tree-wide on pap already (2215 cpplint hits, pre-existing; `python` missing on viper).

**STATUS AFTER THE rt-integration MERGE (09-15 evening, user: "we'll do it later").** Active on rg already: dT-form radial solve, centre layers, ALI default, c a T^4 limiter, tau-blend handover, cut gradient, layer/cut legacy switches, rad_implicit_ang. NOT wired for rg: MODE 3 (rt_implicit_column 3 + rescheck/skip_sweep/hybrid/PCR) = step S3: ~25 GetOrAdd switches mirrored from box_convection.cpp L1113-1320, `ts::rt_bot_flux = pc->rad_flux_inner` (zeroing rad_flux_inner) next to red_giant.cpp:982-984, rt_use_cons + rt_src_direct true and rt_top_re false under mode 3 only; gates: bitwise off, rad suite, Ftop/Fbot -> 1.00 at the ramp (per-cell gives 0.71). Then S4 dhj grey, S5 dhj ck (thermal-channel-implicit, stellar bands explicit via Qb). Box-only, never port: open top, sponge, inflow, profile dump, Cartesian ADI. u-form stays OFF on rg. Do it on rt-integration.
