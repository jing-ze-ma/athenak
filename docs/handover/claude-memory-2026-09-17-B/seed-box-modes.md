---
name: seed-box-modes
description: THE OLD box_convection SEED (vpert 1e-2 c_s, whole-column v1 kick at box wavenumbers 1-4) DIRECTLY EXCITED the (2,2) box surface f-mode (~1000 K on the wide-box photosphere, 6e-3 in flux, flat not growing with the fresh WB cache); replaced 09-16 by an entropy seed confined to the convection zone (vpert_var=eint, k 8-32, 1e-3), commit f523f4d4 on rt-integration; productions relaunched as prod_w6/box_w6
metadata:
  type: project
---

Facts (09-16 evening): the pgen's random seed draws k2,k3 as integers 1..4 (the input comment claiming L/1..L/16 was wrong), applies v1 = vpert c_s sin(pi z/Lz) * sum of 16 modes over the WHOLE column. In the 4x wide box those k are the box-scale modes (98-393 Mm). prod_w4 (cache 10) and prod_w5 (cache 0) start with the same (2,2) flux amplitude 5-7e-3; old run: +0.26/turnover growth over 1-3.3 turnovers, new run: +0.007 +- 0.05 (flat), total surface power decaying -> the cache fix holds on the production box; the seed is what puts the mode there (bench/analysis_0916/fmode_w5). No-seed is not viable (onset from round-off would take ~35 turnovers).
New option (f523f4d4, default-off, bitwise with defaults): vpert_kmin/kmax (integer band), vpert_zlo/zhi (cm window, sin envelope), vpert_var = v1 | eint (entropy at constant density, zero velocity). Verified: density bitwise, v = 0, no power at k < 8. Recommended lines: B  vpert 1e-3, eint, k 8-32, zlo -8.3394e9 (tau 3000), zhi -1.0645e9 (tau 10); He  same k/amp, zlo -3.0591e8 (tau 300), zhi -2.7134e7 (tau 3). vpert_var cannot be a command-line override (must be in the file). Also committed: the cbin frame path fix (src/outputs/coarsened_binary.cpp, missing "/").
Relaunch (user decision 09-16 ~20:45): prod_w6 / box_w6 from t=0 on a HIP build of rt-integration with both commits (bench/wt_merge/build_hip_w6), same five-chain size-handoff layout, w5 chains cancelled. CHECKED 21:05 (bench/analysis_0916/fmode_w6): prod_w6 (2,2) at round-off, 7.8e-11 at t=0 and 1.6e-9 over the first turnover vs 7e-3 in w4/w5 (5e6x below); all box-scale bands at the floor; the seed's k 8-32 band holds the surface power and decays ~100x over 1.1 turnovers (radiative relaxation). WATCH: convective onset expected later than the old seed (turnover 4-6, not 3); if the box KE in feczrt.hydro.hst is not rising by turnover ~5, raise vpert (one input line) and restart. See [[fmode-wb-cache-culprit]].

ONSET CHECK 21:55 (bench/analysis_0916/onset_w6): with k 8-32 / 1e-3 the convective scales (k 2-6) DO grow (e-fold 1.3-2.4 turnovers) but from 1e6x below the old run in velocity (70 cm/s vs 1.3e5 at turnover 3): the seed band was too short (radiatively damped at 12-50 Mm) and missed the most unstable ~2x layer thickness (~130 Mm, k 3). RELAUNCHED as prod_w7/box_w7 (~22:00): vpert 1e-2, vpert_kmin 2, vpert_kmax 16 (1-8 H_p), entropy, same CZ windows, same binary 97342038. w6 cancelled at 3.4 / 2.5 turnovers. LESSON: seed the most unstable scale (~2 layer thicknesses), not sub-scale-height noise, when the layer's radiative diffusion is fast.
