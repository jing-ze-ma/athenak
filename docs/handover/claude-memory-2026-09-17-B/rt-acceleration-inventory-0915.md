---
name: rt-acceleration-inventory-0915
description: RT acceleration inventory after 09-15 - what is left and what each is worth: rad_sts_once REJECTED (floor 1.6x, Ftop 1.0004), margin 0.02 ~1 %, split incompatible with halo_every, sweep skip ~8 % B star (no switch yet), frozen Jacobian stage 2 ~15-20 % of the cycle (day), PCG line-preconditioned ~10-15 % He (day); Newton passes at floor; column arithmetic near floor
metadata:
  type: project
---

Measured 09-15 on the B-star gate box (1 rank, binary 76b42ca8, all with rad_tr_halo_every=2 = T0, bitwise with G3g, 71.4 ms/cyc vs 84.4): rad_sts_once (one RKL1 budget per cycle) 63.8 ms (-10.6 %) but v_rms 1.42e3 vs 8.6e2, Mmax 0.30, dT_tau10 5400x, Ftop/Fb 1.00037 -> REJECTED (first-order split matters); rad_sts_margin 0.02: 47.7 vs 49.5 substages, 1 %, quiet -> not worth it; rad_sts_split: FATAL with rad_tr_halo_every > 1 (conduction.cpp:370, the split rewrites active faces only) -> would need the ghost skin to be split too. He 1-node: once -14 % (HS1), margin -1.4 %. Sweep under mode 3 with w=0 everywhere feeds only kc/Bb/icut/Dtop/Qb -> a skip switch would save ~8 % (B star) / 3 % (He) and must keep rt_pre_* kernels, rt_c3_top, rad_f2s, rt_rad_force and the surface dumps (not implemented). Bigger, day-scale: (1) frozen Jacobian on RK stage 2 + warm start (rt_impl_warm exists, uncommitted): needs the stage index plumbed into picket_fence_two_stream_RT and ~25 slots/cell of factorization storage (400 MB per production block) -> column -25-35 % = ~15-20 % of the B-star cycle; (2) line-preconditioned CG for the transverse operator (stiffness-independent 15-25 iterations vs 122 substages on He; scoped in [[implicit-transverse-raddiff-plan]]) -> He operator 3-5x cheaper = ~10-15 % of the He cycle, B star ~5 %. Everything else measured today is at its floor. See [[warm-started-newton-plan]], [[rkl1-halo-every]], [[bstar-prod-cost-profile]].

SWEEP SKIP DONE 09-15: rt_col3_skip_sweep (commit on rg-box-pcr after 76b42ca8, default off; K0 bitwise = T0; K1 +11 % zone-cycles/s on the B-star gate box, 64.4 vs 71.4 ms/cyc, gate numbers in scatter, Ftop 1.00000, NOT bitwise on: rad_force/dumps now use the column's converged flux instead of the sweep's; He 1-D +6 %, He 3-D +1.7 %). Deploy on the B star at the next link/run (input line rt_col3_skip_sweep = true in <problem>); binary build_hip_skip md5 24736878 (pre-commit tree). Also committed: rt_impl_warm, rt_col3_once switches (off).

NQUAD 1 REJECTED 09-15: the mode-3 block stays 5x5 (the second angle's rows get a unit diagonal), so nquad 1 saves only ~10 % (3 fewer expm1 per cell), and it carries a deterministic -9.6 % bias in Ftop/Fbot (0.9036: the hemispheric mean returns 0.904 of the diffusion flux, documented in two_stream_rt.hpp l.760-781) and a 1.5-3 % cold bias for tau < 10. Keep nquad 2. (A 3x3 block for nquad 1 would double the block gain but the bias remains.)

REDUCED-SYSTEM PCR DONE 09-15 (rt_impl_redpar, commit 2cf2776e on rg-box-pcr, default off): lane-parallel cyclic reduction of the nseg 5x5 block rows (log2 nseg rounds, double-buffered, no back-substitution; workspace 96 -> 281 slots when on); bitwise on the 1-D B-star column, 2e-13 on He; speed 1.17x He column, 1.04x gate box, 1.03x timing box (the 29 % 'share' was of a pass on a 32x32 box; on the big box the reduced solve is a smaller fraction). Not in the launched wide-box inputs (gated without it); use in later builds. NEXT: the hybrid column ported to the PCR path (wt_hybp, running) = the last column lever (~30-40 %).
