---
name: session-state-2026-09-11-0340
description: START HERE (written 2026-09-11 03:00 CEST on orion, session f9284861, at the user's "let's continue next time") — the night's chain: 5.71e5 common death = explicit conduction runaway -> implicit radial diffusion + angular cap (done, gated); V8f dropped (Parker wind); I4 active medium died at an ejected-shell front; density-gated opacity (done) confirmed the cutoff artefact; runaway source = RT reading STALE w0 (fixed, rt_use_cons) + density floor keeping momentum (fix in progress); I6 running; one Opus agent (af77147) carries a 3-item queue; the user EXITED so the agent is dead: pick up its queue
metadata:
  type: project
---
Supersedes [[session-state-2026-09-10-2200]]. Read in order: [[red-giant-common-dt-collapse-571e5]],
[[red-giant-implicit-radial-diffusion]], [[red-giant-i5-density-gate]],
[[red-giant-runaway-source-rt-stale-w0]] (has the R2 update + the queue), [[red-giant-stale-primitive-audit]].

## Runs at 03:00 (all /orion/ptmp/jinma/Athenak/red_giant/<dir>; binaries build_impl/athena_pinN)
- I6_fixes job 196082 (pin7: implicit x1 + cap 0.5 + gate 1e-16 + clamp off + rt_use_cons +
  bc_use_cons; V9f rst 5.0e5 -> 9e5, 4 h wall, ends ~06:55). At 02:58: t=5.12e5, dt 30.63.
  FIRST THING next session: its outcome (dt history, COLLAPSE lines + cells, L_rad,out/L
  lines: I5 gave 1.2 at 7e5, 3.3 at 8.2e5, 2.0 at 9e5; tot-E; cap stats; max T top 60 shells).
- R2_scan job 196061 (pin6 + runaway_scan): at its 2 h wall, result already in the notes.
- Finished: I2 (pin2) 9e5, I3 (pin2, clamp off) 9e5, I5 (pin3, gate) 9e5, I4 (pin2) died
  6.31e5, R1_scan (pin4) stopped 5.787e5, I1 (pin) cancelled 5.84e5.

## The agent queue that died with the session (re-brief a fresh Opus agent, in this order)
1. Fix (4) dfloor_keep_velocity: general_c2p_hyd.hpp:44-48 floors rho keeping momentum ->
   v = m/dfloor explodes (R2's collapse cell: rho=1e-18, v 4e11). Scale m by fv=rho_old/dfloor,
   u.e -= (1-fv^3) KE_true (cs: metric KE in GnomonicEquiangleRaiseVel, not the orthonormal
   e_k), counter, default false; gates (off bit-identical to pin7; smooth star 0 hits); R3_scan
   (R2 config + the flag, rst 5e5, tlim 6.3e5) vs R1/R2; if clean -> I7 = I6 + flag to 9e5.
2. rt_bface switch (problem/rt_bface default false = old B_far bit for bit; red giant true;
   8 BFace sites + 4x guard). NO default flips of rt_semi_lin/rt_newton/rt_rescue_eq/
   rt_src_direct. REPORT the other un-switched changes in the shared headers vs HEAD
   (two_stream_rt, atm_column, correlated_k, wb_background, conduction) -- user decides.
   Regression comparison vs HEAD (git worktree add .../wt_head HEAD; remove after) for
   solar_convection and the dhj pgen, 50-100 cycles, attribute differences to changes.
   Write docs/handover/NOTE-2026-09-11-red-giant-switches.md with the full red-giant switch
   list; pin the binary.
3. Then: commit plan (see below) and, if I6/I7 hold to 9e5, the physics questions: what
   launches the shell at 5.7e5 (all open-top runs; lidded holds it), and the open-top surface
   luminosity 1.2-3.3 L vs lidded 1.3-1.5.

## Tree (uncommitted, ~30 files): the 09-10 pile + implicit x1 (conduction.*, hydro_tasks,
## hydro.hpp, mesh.cpp), cap, gate (RadGate), hydro dt-collapse cell report (hydro_newdt.cpp),
## runaway_scan.hpp + eint_from_cons.hpp (new, header-only), rt_use_cons (52 sites),
## bc_use_cons + wallflux guard (red_giant.cpp), driver.cpp <utility>. Style: 0 new violations
## per agent gates. Commit in pieces (git add -p): the 09:10 note's a-f, then g) implicit x1 +
## cap, h) density gate, i) dt-collapse hydro cell + runaway_scan, j) eint_from_cons +
## rt_use_cons + bc_use_cons + wallflux guard, k) rt_bface switch + docs note.
## Latent audit items (not enabled in the runs) for a hygiene commit: rg_relax, open-inner BC
## (stale targets, unguarded ei, ORTHOGONAL KE on cs), MLT caps, perturb-reconstruction order.

## Decisions the user owns
- Accept the 5e-3 inner-face budget accumulator change from bc_use_cons (diagnostic only)?
- Which un-switched shared-header changes to switch for other pgens (after item 2's report).
- prod11 lidded revival (unchanged from the 09-10 note): from B12 rst 3.45e7 with build_guard
  or with the new binary; the lid damps the photosphere (interior science only).
- Production open-top config = I6/I7 lineage (corona 2e4 K hydrostatic, gate 1e-16, no
  cutoff, clamp off, all fixes) once I7 holds.
Housekeeping: analysis scripts in _analysis_0910/ (i4.py, task_I3_follow.py, task_dt*.py,
task_I5_gate_choice.txt); scratchpad q*.py of session f9284861 are lost with /tmp.
