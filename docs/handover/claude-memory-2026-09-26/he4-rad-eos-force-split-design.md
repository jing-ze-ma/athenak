---
name: he4-rad-eos-force-split-design
description: design agreed with the user 2026-09-19 night - split the radiation taper's two jobs: EOS weight density-only with a very low window, radiative FORCE gated by optical depth inside the two-stream
metadata:
  type: project
---

**Problem.** The EOS radiation weight w(rho,T) = max(w_rho, w_T) (rad_taper::WeightGated; window rho 4.97e-10..1.93e-9 = tau 0.3..3 of the IC, T gate 4.52e4..6.33e4 K) does two jobs: (a) thermodynamics (e = e_gas + w a T^4, p = p_gas + w a T^4/3), (b) deciding where the force is -grad(P_rad) (w=1) versus the explicit (1-w) rho kappa F/c + P_rad grad w in two_stream_rt.hpp (~line 4776). On the He4 star the density at tau~1 is only 3x below interior densities (CZ 2.3e-9, thins to 6e-10 in the pulsation), so a density window cannot do both; the T gate was the patch, and it wrongly radiation-loads shock-heated transparent gas above the photosphere (tall-domain arm b_tall: c_s hits cs_max, dt 0.6 s). Density-only with the present window fails thermodynamically: w drops at fixed e -> T jumps (e_rad ~50 e_gas) -> evacuation loop (tests_r6).

**Design (user: "we probably need to do this").**
1. EOS: density-only weight, window far below anything stellar: ~1e-11..1e-10 (eos_rad_rho_lo/hi), T gate OFF (eos_rad_t_hi = 0). LTE P_rad = aT^4/3 is fine down to tau~0.1 (Eddington approximation).
2. FORCE, in the two-stream where tau per cell exists: new default-off switch (e.g. problem/rt_force_tau_gate, rt_force_tau_lo=0.3, rt_force_tau_hi=3): s(tau) = 1 for tau<=lo, 0 for tau>=hi, cubic in log tau;
   f1 = s*[rho kappa F/c + d/dr(w a T^4/3)] + (1-s)*[(1-w) rho kappa F/c + P_rad dw/dr]
   (s=0 reproduces today's formula exactly -> bitwise when off); transverse: f2,3 = s*d_t(w P_rad) + (1-s)*P_rad d_t w. Needs w at i+-1, j+-1, k+-1 (WeightGated on rhoN, T_g: T_g is ghost-filled one angular cell wide since de1d41e4) and tau-to-top in the force kernel (a `tautop` exists in the cell-report code: check it is available there).
3. IC: the file stores (rho, eint) with the OLD w. Regenerate eint above ~0.97 R with the NEW w at the same (rho, T) (offline EOS in tests_r12/relax_ic.py: temp_of with old taper_w, then e_gas + w_new a T^4). Gas-pressure balance is unchanged at t=0 IF s(r) = 1 - w_old(r): old net radiative force = -w grad P + (1-w) kappa rho F/c, new = -(1-s) grad P + s kappa rho F/c. The w thresholds were read off tau = 3 and 0.3, so s(tau) with the same two levels is close; verify with tests_r12/fbud.py (per-cell force at t=0, top 5 % of the radius) on the 1-D sp column (tests_r13/r13_arm.sh).
4. Check the EOS table inversion brackets (eos_table.cpp ~300-400 assume "gate => hot cell has w=1"; with the gate off that branch is unused) and that e(T) stays monotone.
5. Then the tall domain (tests_r13: fitgrid.py, mk_tall_ic.py, floor atmosphere rho 3e-13) should no longer need cs_max to tame the gas above the photosphere.

**Why:** makes the region above the photosphere (wind launching, Gamma ~0.83-1) physically sane so x1max can go to ~1.25 R and the pulsation stops draining the envelope through a top at 1.014 R. See [[he4-r11-floor-arms-verdict]] for the evidence.
**How to apply:** implement default-off, bitwise check vs athena_v19, 1-D validation before any 3-D.

**IMPLEMENTED 09-19 ~23:50 (UNCOMMITTED in bench/wt_he4; binaries tests_r11/athena_v20, v21 = + early exit in the tau sum, bitwise same).** problem/rt_force_tau_gate (+ rt_force_tau_lo 0.3, rt_force_tau_hi 3.0) in two_stream_rt.hpp force block (helpers RTWPrad, RTForceTauGate; tau = inner loop to the top + half own cell, stops at tau_hi; unresolved top-closure column NOT included: fine under rt_top_vacuum); read in red_giant.cpp + box_convection.cpp; both He4 inputs carry the params. Default-off BITWISE vs v19 (tests_r13/g19 vs g20). Sign check: correction = rho kappa F/c + grad(w Prad) (hydro already applied -grad(w Prad)); reduces to the old form in the diffusion limit.
EOS side: <hydro> eos_rad_rho_lo=1e-11 eos_rad_rho_hi=1e-10 eos_rad_t_hi=0 (=gate off, rad_tgate = t_hi>0); inversion is monotone with the gate off, force kernel reads the same tbl values. Separate input problem/rad_gate_rho=4.97e-10 (transverse-diffusion gate) NOT yet moved.
IC: tests_r14/convert_ic.py -> ic_he4_neww.txt (from bench/hestar_presn/ic_he4_presn_sph.txt; eint changes only 0.986-1.019 R, up to x17; w=1 rows unchanged to 9e-15).
t=0 CHECK (tests_r14 A vs B, fbud14.py, 300 s): old setup has the alternating residual +0.05/-0.14/+0.22 rho g across 0.994-1.005 R; redesign is smooth to 0.997 R, leftover -0.16 at 1.002 R and -0.085 at 1.010 R (s(tau) is not exactly 1-w_old); totals equal (2.3e27 dyn), KEr 1.8x lower. Dynamic 1-D arms B3 / Btall launched next (tests_r14).
LESSON: `VAR=x sbatch script` does NOT pass VAR on viper; use --export=ALL,VAR=x and check the arm banner in the log.

**1-D DYNAMIC RESULTS 09-20 ~00:40 (tests_r14, script r14_long.sh, athena_v21, all with the redesign + converted IC, 3 turnovers):** problem/rad_gate_rho is NOT just a transverse gate: RadGate(rho) multiplies kc_g (the two-stream opacity) and the conduction kappa -> it MUST move with the EOS window (hydro/rad_gate_rho=1.0e-11); stale 4.97e-10 on the tall domain = dt 0.07 s by 0.6 turnover (Btall, cancelled). Standard domain B3/B3g == old-physics tests_r13/b0 to a few % through 2.0 turnovers (redesign neutral on the interior-driven expansion), then the usual drain (B3g -9.5 % mass at 3.0). TALL DOMAIN Btallg (x1max 1.25 R, nx1 144, ic_he4_tall_neww.txt, rad_gate_rho 1e-11, no cs_max): dt ~4 s and mass constant to 8 digits through 2.2 turnovers (old b_tall: dt 0.54 s from the start) = the top problem is CURED. Death at 2.2-2.6 is the 1-D INTERIOR CAVITY at 0.78-0.84 R (rho 2.7e-9 -> 1.3e-10 at fixed T 1.6e5 K, cs_rad 1.3e8), then radiation-loaded ejecta at 1.01-1.06 R (v1 1.4e7, front at 1.20 R by 2.5; mass loss only 3e-4); marker = vceil counter 0 -> 4500/cycle after 2.25. cs_max=1e8 is UNUSABLE on this star: (10/9) a T^4/cs_max^2 = 6e-10 is only 2.3-2.6x below the unperturbed density at 0.69-0.79 R and the add-at-rest bookkeeping is a positive feedback (Btallgc: mass x45, eint x52 in 150 s); would need >= 3e8 or restriction to above the photosphere. Analysis scripts tests_r14/dtdiag.py, dthist.py. NEXT (needs user OK): 3-D tall wedge with the redesign, no cs_max (3-D never formed the fully evacuated cavity).

**3-D TALL WEDGE 09-20 00:14 (user approved, damping kept): tests_r11/wedge6, job 11853630 (4 nodes, 6 h, athena_v21, script r11_wedge21.sh; resubmit the same command to chain).** Args = wedge5 (cfl 0.15, rt_top_vacuum, mlt_split_deposit, 192^2 / 24^2 blocks, entropy seed vpert_var=eint vpert=3e-2 vpert_sp_rand, vdamp_all_mean_only until 18820 s = 4 turnovers, tau 500, tlim 28230) MINUS cs_max PLUS redesign (hydro/eos_rad_rho_hi=1e-10 eos_rad_rho_lo=1e-11 eos_rad_t_hi=0 eos_rad_t_lo=0 rad_gate_rho=1e-11 problem/rt_force_tau_gate=true) + tall grid (problem/ic_profile=tests_r14/ic_he4_tall_neww.txt mesh/x1max=2.964625e11 nx1=144 meshblock/nx1=144, f_stretch_r_c1..4 = 0.992525 -0.404821 -0.372255 -1.499759). Smoke tests_r11/smoke6 OK (1.3e6 zc/s, 4 GB/node). At 2.06 turnovers: dt 2.94 s, mass to 3e-6, KEr 2.6e36, KEh 1.25e36 (same growth as wedge5 up to 1.5). wedge5 (old physics, 1.014 R) lost dt 3.1 -> 0.4 s over 1.8-2.75 turnovers (cause not examined), CANCELLED by the user at 2.75. An unattended background scancel watcher is refused by the permission classifier: cancel by hand.

**wedge6 DIED 09-20 ~01:45 at 2.49 turnovers (sudden: dt 2.54 -> 0.02 s within 47 s; cancelled; rst at 2.40 kept in wedge6/rst_keep; replay tests_r11/wedge6d with 23.5 s bin dumps, 2nd replay reproduced the collapse).** Site = the density CLIFF at 1.021-1.026 R (i 97-100, IC top glued to the 3e-13 floor atmosphere): standing infall -6e5 cm/s from early on (1-D too), plumes raise the contrast below (x8.8 at i=95) after 2.4 turnovers, infall -9e6, floor cells at 2e5 K, c_s runaway.
**1-D DISCRIMINATOR tests_r15 G0-G7 (tall column, 1.5 turnovers; scripts r15_arm.sh, cliff.py, summary.py, tfloor.py):** (1) the anomalous inward acceleration (-2e4 cm/s^2 = 3 g) sits in the ONE cell with 0<w<1 and exists only with rt_force_tau_gate=true: the centred +grad(w Prad) does not cancel the Riemann solver's -grad(w Prad) at the window edge (G0s vs G1s, 1-s profiles). (2) gate off with the low window (G1) is catastrophic (KEr 2e39 by 0.5). (3) moving the window up (G2: 3e-11..3e-10) does not help. (4) 10x sponge (G3) quiets i>=98 only. (5) rad_gate_rho (documented off = 0.0) at 1e-11 leaves the floor atmosphere with ZERO opacity: it cannot cool (G0 hot cells 1.1e5 K rising); rad_gate_rho=0 (G6) relaxes it to RE 3.54e4 K to 1e-3. The conduction flux limiter is always on (F_free = 4 sigma T^4), so un-gated transverse diffusion is flux-saturated, not unbounded. (6) USER IDEAS COMBINED = G7: problem/rt_rad_force=false (+ rt_force_tau_gate=false) + hydro/rad_gate_rho=0.0 = BEST ARM: dt rising to 4.9 s, KEr/mom within 5 %/3 % of G0, mass to 1e-7, floor T quiet, no cliff (the layer above the window expands at +1e6 -> 4e5 cm/s, decaying, and fills it in: IC there was balanced WITH the explicit force). Force off but opacity still gated (G4) DIES. lout9.py on the sp column gives a flat 0.659 (normalisation not adapted; use only relative).
Productions for reference (prod_w7 / box_w8): density-only window tau~30..1 (1e-8..1e-9 / 6e-8..6e-9), no T gate, rad_gate_rho = window lower edge, whole-column two-stream, old force form, top at tau 1e-2: photosphere had w=0 (physical heat capacity); He4's inversion forbids that window. Idea being scoped: per-cell EOS weight w(tau) from the column optical depth (frozen per step).
NEXT: 3-D smoke tests_r11/smoke7 (G7 config, un-gated transverse diffusion) then wedge7.
wedge7 SUBMITTED 09-20 ~03:30 (user ok): tests_r11/wedge7 = wedge6 args with problem/rt_rad_force=false rt_force_tau_gate=false hydro/rad_gate_rho=0.0 (G7 config; smoke7 clean: dt 3.17 s, 1.3e6 zc/s). Same script r11_wedge21.sh; resubmit the same command to chain. Gate: passes 2.5 turnovers (wedge6 died at 2.49), then the damping release at 4.
PER-CELL EOS WEIGHT w(tau) SCOPED (Explore agent 09-20): feasible default-off/bitwise; Riemann solvers never call the EOS (p, Gamma1 stored in wder by c2p), so only ~20 load-bearing sites: 3 c2p kernels + single-state c2p headers, cs raise-vel sites, 2 FOFC entries, ~7 eos.hpp helpers (EnergyFromPressure/Temperature, SubFloorState...) + eos_table.hpp 293/431/466/523. Hard parts: w over ALL ghosts (c2p runs on the full block; BuildRadWeights fills transverse + inner x1 ghosts but not i>ie), ordering (BuildRadWeights reads wtemp/wder -> accept one-cycle lag; first c2p / restart has no w -> store in rst like wtemp), floors + force must read the same field. Estimate 1-2 days.
09-20 ~02:30: user: "should use cfl 0.3". wedge7 cancelled after 3 min; RUNNING arm = tests_r11/wedge8 (job 11856317) = wedge7 args with time/cfl_number=0.3. Caveat on record: on the OLD setup cfl 0.3 doubled the O(dt) outward push (+1.02e31 vs +4.8e30 at 0.25 turnover) and left a 0.9 % vs 0.4 % dipole with mlt_split_deposit; not re-measured on the no-force setup.
wedge8 (cfl 0.3) FAILED 09-20 ~03:20, cancelled at 1.96 turnovers: atmosphere perfectly quiet (no cliff, floor gas at RE 3.55e4 K, layer above the window drifting out at +4e5 cm/s) BUT a SHELL-MEAN density zigzag grows exponentially at the base of the MLT zone from 0.5 turnovers: i=27 (0.746 R) rho 2.29e-9 -> 2.17 (1.0) -> 1.67 (1.4) -> 5.8e-10 (1.9), pile-up at i=18 (0.675 R) 2.33 -> 3.9e-9, T unchanged; then v1 -1e7 there, vceil events, dt 6.3 -> 1.6 s, KEr x16. wedge5/wedge6 (cfl 0.15) stay smooth there (i27 -3 % at 2.4 turnovers, not exponential) => cfl 0.3 with the Strang-split RT + MLT closure is UNSTABLE on this star in 3-D (1-D S0.3 showed nothing by 1.5; the 1-D cavity at ~2.2 turnovers at cfl 0.15 is probably the same mode, slower). Same family as [[fmode-dt-taper-test]]. Use cfl 0.15. RELAUNCHED tests_r11/wedge7 (cfl 0.15, same G7 config), log wedge7b.log.
09-20 ~06:15: wedge7 (cfl 0.15, no sync) passed wedge6's death point, atmosphere quiet, but dt eroded 2.84 (2.2) -> 0.80 s (3.15 turnovers) with KEr/KEh > 1e38, no vceil/fofc; CANCELLED at 3.15 (user: "start fresh"); dt-limiting-cell analysis in tests_r17x (agent). FRESH RUN tests_r11/wedge9 job 11859593: athena_v22, script r11_wedge22.sh, = wedge7 args + problem/mlt_split_sync=true, cfl 0.3 (see [[he4-strang-vs-unsplit-mlt-seed]]).
wedge7 dt EROSION DIAGNOSED (tests_r17x/dt3d.py, reproduces the run's dt to 0.1-1.5 %): dt is set at r/R 0.60-0.64 (i 10-14, the BASE of the MLT zone), always in the PHI direction (narrowest cell, worst at the theta 45/135 edges), by the SOUND SPEED in under-dense cells: T uniform to 1 %, rho/<rho> min 0.80 -> 0.44 -> 0.14 (max 18) at 2.0/2.5/3.0 turnovers, c_s ~ rho^-1/2 because p_gas/p = 0.009 (Gamma1 1.335); |v| <= 13 % of |v|+c_s. At beta = 0.01, d ln rho = -(4 p_rad/p_gas) d ln T ~ -400 dT/T, so 1 % T fluctuations ARE order-unity density contrasts: physical EOS behaviour, but the Nyquist share grew 1 % -> 10 % and vceil/FOFC started after 3.0. Second eroding band: 0.93-1.01 R, r-limited (13 -> 4.7 s). Atmosphere flat, no active cell near dfloor; the he4.log dfloor counts are ghost-layer constants (172800/cycle) scaled by the time-based log interval: NOT a signal. Open: is the deep base resolved (2-6 cells per plume)?
wedge9 RESULT 09-20 ~07:50 (cfl 0.3 + mlt_split_sync, athena_v22): the SYNC FIX WORKS IN 3-D (deep shell densities smooth at 1.0/1.4 turnovers where wedge8 had the zigzag; dt 6.3 s to 2.5, 4.85 s at 3.0 vs wedge7's 1.35 s at cfl 0.15). BUT from ~2.9 turnovers a violent phase: vceil events 0 -> 4e3 (2.9) -> 2e5 -> 5e6 per interval, FOFC 1e6, efloor_de 1e17; dt 4.0 (3.26) -> 1.04 (3.5) -> 0.4 (4.04); KEr 2e39 >> KEh 5e38 (v_rms ~1.2e7 cm/s); radial momentum strongly NEGATIVE (-1.9e32); tot-E 8.98e40 (2.0) -> 8.93 (3.0) -> 8.32 (3.5) -> 5.70e40 (4.0) = -37 %, i.e. ~0.26 L_wedge (4.2e37 erg/s) net loss; shell-mean T falls EVERYWHERE between 3.5 and 4.0 (0.723 R: 1.70e5 -> 1.25e5 K; bottom 2.31 -> 1.98e5), deep density up x1.5, inflow -1e6..-6e6 cm/s: the envelope COOLS AND CONTRACTS once resolved convection is vigorous. Hypothesis (unverified): double counting of the convective flux - F_res grows x3 per 0.25 turnover while the closure relaxes over mlt_relax_time 1e4 s (2 turnovers), so F_2s + F_used + F_res > F_req; alternatives: energy removed by vceil/FOFC/floors, or physical deflation of the alpha=1.5 IC envelope. Cancelled at ~4.05 (mass still conserved 2e-5); rst + bin dumps kept in tests_r11/wedge9. NEXT: measure L_out (rt_surface) and the closure fluxes vs time (problem/mlt_dump_dt) over 2.5-3.5 turnovers from a wedge9 restart.
wedge9d (restart of wedge9 at 2.40 -> 3.50 turnovers with problem/mlt_dump_dt=47; analysis tests_r18: mltratio.py, budget.py, fres3d.py) 09-20 ~09:30: MY "double counting of RESOLVED convection" HYPOTHESIS IS REFUTED (RETRACTION): the closure's F_conv_res agrees with an independent 3-D enthalpy+kinetic flux to 1-3 % and is only 0.1 % (2.9) -> 2.7 % (3.4) of F_req. The envelope cools because it RADIATES more than it receives: L_out/L_wedge 1.006 (2.4) -> 1.036 (3.0) -> 1.107 (3.3) -> 1.149 (3.5), carried by F_2s (F_2s/F_req 1.008 -> 1.155 at 0.93 R; SUM 1.06-1.17 at ALL radii 0.60-0.96 by 3.5); numerical sinks negligible (efloor_de 2.7e11 erg total; vceil a sink of at most ~4e39 but wrong sign for the residual); gravity releases nothing; an unexplained +0.04 L_w SOURCE late (RT clamps / T clamp at the table's lowest row?). L_wedge = 4.0776e37 erg/s; hst tot-E has a constant +4.14e40 offset vs int e dV (use differences only). Contributing numerics: (1) closure keeps carrying 6-9 % of L over 0.70-0.90 R for > 1 turnover after its own deficit D = 0 (pure exp(-t/1e4 s)); (2) a grid-locked STANDING F_2s <-> F_used oscillation at 0.79-0.92 R, period 3-6 faces, +-0.3 L_w, formed 2.4-2.8 turnovers: the [1 2 1]/4 sync smoother only kills the 2-face mode. MY OPEN PHYSICAL READING (untested): POROSITY - with rho contrast rms 0.9 at the MLT base (p ~ p(T), beta 0.01) the per-column two-stream sees <1/(kappa rho)> >> 1/<kappa rho>, the effective opacity drops, flux above exceeds L and drains the layers below (bottom T 2.31 -> 1.98e5 K); fits F_2s > F_req at all radii incl. 0.60 R. Test = per-column F_2s vs column density in a 3-D dump vs the flux of the shell-mean state. Cures to try for the numerics: asymmetric relaxation (instant downward), mlt_relax_time <= 1e3 s, wider target smoother.
