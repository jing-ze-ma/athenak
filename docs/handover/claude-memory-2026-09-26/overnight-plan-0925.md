---
name: overnight-plan-0925
description: User went to bed 09-25 ~09:30 and delegated: WASP-121b smoke -> submit the 4 sponge arms if clean; merge gated branches; no notification reports
metadata:
  type: feedback
---
User 09-25 ~09:30: "I'm going to bed. can I trust you with these? you don't have to report notifications."
Plan agreed: (1) WASP-121b smoke 11972290 -> if clean (no FATAL, no floor cells at start, dt sane, ck non-converged
~0 or only a few with small residuals), submit the 4 sponge arms (base/nobot/notop/none, 1 GPU each, 2 node-jobs +
afterok links, 100 rot, sparc_0925/run.sub defaults = WASP-121b); if NOT clean, do not submit, diagnose (agent), write
findings. (2) Merge branches whose gates pass (ck-fast2 levers, m1-coarse2, m1-sp-order2b), push. (3) a320 A/B links run
on their own; analyse when done. (4) Update docs/handover at the end. No new threads; no status chatter.
**How to apply:** act silently on completion notices; summarise once for the user in the morning.
- User 09-25 ~09:35 (before bed): also set up KELT-20b (MASCARA-2b) alongside WASP-121b -> agent, /viper/ptmp2/jinma/kelt20_0925, setup only (no sbatch); mass poorly constrained (RV upper limit) -> survey + choose.
- 13:35 WASP-121b smoke 11972290 NOT clean: base segment 13/13 ck NOT-CONVERGED (8 passes), eos floors ~1e6 per 50 cycles, tclamp; sponge arms NOT submitted; diagnosis agent -> /viper/ptmp2/jinma/wasp121_0925/SMOKE_DIAG.md

**09-25 17:50 (user): smooth the hiT extension regardless (C1 at 6100 K; branch ck-hitemp2, new *_hiT2.txt) and use a DAY/NIGHT IC (branch dhj-ic-daynight, wasp121_0925/daynight): mu*-dependent day RCE + redistributed night RCE, common deep adiabat, 2-D ic table. Diagnosis: nightside top collapses from the 3750 K global-mean top; 10x hiT slope jump ~650 at 6100 K breaks the ck Newton. Test 11978739 (10x without extension) pending. Smoke only ran base (my STOP-file mistake; smoke.sub fixed).
- 18:10 test 11978739: 10x WITHOUT hiT extension also 100/100 NOT-CONVERGED (capped ~830 cells/call at dstep 0.5) -> extension slope not the (only) cause; 10x opacity drives a fast dayside transient; diagnosis agent testing every=1, step cap, maxit, dt ramp, day/night start.
- 18:10 smoke diag: 10x non-convergence = (a) stiff dayside upper layers near RE heated 10-30 %/step, (b) nightside top floor-fight cells capped every call; no input knob fixes it (every=1, maxit 24, cfl 0.1 tried). Fix being implemented: branch dhj-ck-floorbound (Newton iterate bounded at floor energy; bound+cooling counts converged), tested with the day/night IC. Keys ck_impl_dtmax (0.5 per-pass cap) / ck_impl_demax (total change) must be in the input to override.
- 20:35 user: day/night IC amplitude a = 0.3 (full ~1000 K night makes a 6.7e9 top pressure contrast and a terminator shock; lower top breaks the 1e-6 bar rule). Agent regenerating + floorbound/kkt inputs + GPU binary.
- 21:20 day/night a=0.3 CPU gate: floors reduced but not gone (night top ~3100 K cools to 200 K by cycle 22). I argued 'no floor events' is the wrong criterion for a 1e-9 bar top; new criterion: floors confined to the night top p<1e-7 bar/in sponge, ck converged, dt stable, floor mass/energy injection small. GPU test 11980129 submitted (new binary e3a8442e = dhj-ic-daynight f637b3be).
- 21:35 user: option A = fix the 10x dayside ck Newton (agent: lever-by-lever arms, then code fix on branch ck-newton10x) + like-for-like 1-D baseline job 11980205 (base.sub) vs day/night a=0.3 (job 11980129: NO-GO, night-top collapse, 76 tfloor cells below 1e-6 bar, ck 100/100 nonconv).
- 21:45 user: WASP-121b at 1x solar for the sponge tests (10x Newton being fixed); setup agent preparing /viper/ptmp2/jinma/wasp121_0925/x1 + PLANET=w121x1 in run.sub/smoke.sub; then smoke -> sponge arms. Literature: WASP-121b atmosphere likely 3-10x (refractories ~10x, volatiles 3-5x; IGRINS ~stellar); 1x is the low edge; 1x deep 560-670 K cooler, RCB 7.5 vs 1.5 bar, p_ref 7e-4 vs 1.8e-4 bar.
- 21:45 like-for-like: day/night a=0.3 IC is NO better (somewhat worse) than the 1-D IC: floors 6691 vs 6159 cells, T-floor cells below 1e-6 bar 76 vs 73, ck 100/100 nonconv both. Floors come from the night top cooling, not the IC -> KEEP 1-D IC; dhj-ic-daynight (a2a42b97/f637b3be) left UNMERGED. Open: ~73 cells at the 200 K T floor below 1e-6 bar (night top cooling) -> consider lower tfloor.
- 22:00 10x Newton diagnosis: NOT the speed levers; the ck Jacobian omits d(kappa)/dT (quasi-Newton by design); at 10x in stiff hot dayside cells the true dR/de ~2x the model -> overshoot, linear convergence stalling ~1e-6, period-2 limit cycle with all levers off. Fix in progress: branch ck-newton10x dd3de8a7, problem/ck_impl_rsec (per-row secant diagonal from the row's own residual across passes; default off).
- 22:05 10x rsec: R20 23/50, R20+maxit12 14/50 nonconv, res 2e-6 -> 5e-8. User: test C1 smooth kappa interpolation (problem/ck_interp_T = pchip; hypothesis: linear-in-log-k T interpolation has slope jumps at every node -> Newton limit cycles; 10x jumps larger).
- 22:40 10x: kink hypothesis REFUTED (k/CE/continuum tables are looked up once per call, not inside the passes; only the Planck fractions are, and pchip changes nothing). rsec 20 helps: 100 % -> 26-46 % nonconv, res ~1e-8..1.6e-7; remaining slow columns converge linearly = dropped long-range couplings in the tridiagonal Jacobian. Merged ck-newton10x (options off). Next: user choice (a) tol 1e-7 with rsec20+maxit12, or (b) add couplings / per-column Broyden-Anderson.
- 22:55 10x confirmation (job 11980525, day/night IC by mistake): rsec20+maxit12+tol1e-7 NOT ~0: 12-14/75 nonconv, growing after cycle 250 (residuals up to 1.1e-5) -> option (a) insufficient; option (b) (Jacobian couplings / per-column Broyden) needed. 10x inputs being reverted to 1-D IC. Binary bin_0925/athena.gpu.6e897934 md5 73ac492c.
- 23:05 user: START option (b) for 10x (branch ck-newton10x-b, /viper/ptmp2/jinma/wt_newton10xb): better Jacobian / per-column Broyden-Anderson on slow columns; target 0 nonconv at tol 1e-8 over >= 400 cycles.
