---
name: bstar-top-flux-deficit
description: 2026-09-14 SOLVED = LAMBDA-ITERATION LIMIT of the per-cell scheme (b825409c rt_ali_diag, default ON, bitwise with false): NO dead band - each thick cell relaxes exactly to equilibrium with its lagged absorbed field A (A/E-1 ~ 3e-9), but A is mostly the cell's OWN returning emission, so the scheme applies 1/x of the true source (x = 4E dt/e up to 2e5): at cycle 0 the divergence sums to 0.293 F over 43 cells of dtau 3-10, the applied change to 0.00009 -> the thick two-stream region never receives its heating (one Lambda iteration per step; tau_tot^2 needed) -> the thermal structure between the handover and the photosphere was FROZEN at the IC in EVERY two-stream run (rg/dhj atmospheres too). FIX: ALI diagonal - rate x (1 - Lambda*_ii) from the layer escape probability: thick cells get the full source per step, thin unchanged; 1-D Ftop 0.707 -> 0.836 in 3000 s and rising; grey_cons unchanged. Partial: cannot close at tau_tot >> 1; full cure = mode 3 (exact column). PORT to rg/dhj as a DEFAULTS CHANGE (flag it). Earlier text: CORRECTED (ramp_deep a-d): the 0.707 loss is NOT the ramp - it recurs identically with the ramp at 3000/5000, 5000/1e4, AND with NO ramp (whole column, rt_bottom_flux: sweep flux 1.000 at the wall, 0.707 at the top, gas dE ~0) -> the standard per-cell semi-implicit scheme is NON-CONSERVATIVE through the whole thick column: in thick cells at rest the assembled source is ~1e-4 F/cell but the APPLIED de is exactly 0 -> a dead band/threshold/precision loss in the apply block discards small imbalances (agent hunting it, bench/bstar_fecz/ramp_deep). NOTE the earlier whole-column 'Ftop/F_bot 0.9999' numbers (rtcol3_disc E1, dtscan_whole) were the GAS BUDGET (that binary predates the user-hist Ftop), not the emergent flux - retracted. Earlier (partly wrong) text: ROOT CAUSE FOUND (822055e2 rt_src_dump; bench/bstar_fecz/budget/F,G): the assembled direct source EQUALS the flux divergence (1e-9/cell, round-off summed) - the entire 0.30 F_bot loss is the PER-CELL SEMI-IMPLICIT FACTOR in the 14 ramp cells with 0 < w <= 0.5 (dtau/cell 5-9, x >> 1): (1-e^-x)/x -> 0 damps the (1-w)-weighted radiative HEATING to zero while the explicit handover is applied in full (the decad388 split protected only half). => THE PRODUCTION SCHEME (mode 0) IS WRONG BY 30 % OF F AT THE B-STAR RAMP: Ftop 0.71 F_bot, ramp thermal structure (tau 100-300, just above the FeCZ) relaxed into that wrong balance; every box result carried it; rg/dhj share it at their ramps. MODE 3 FIXES IT: Ftop 0.89 -> 0.96 -> 0.975 rising over 300 cycles (re-relaxing). PLAN: finish the current production as the old-scheme reference; re-relax the column under mode 3; rerun production with mode 3 + u-form conduction + flux outputs (~1.8x cost, ~13 h/20 turnovers). Earlier text: with the new user-hist Ftop (09dbf055: box-mean two-stream emergent flux, mean square, Fcut, Ttop; surface dump rt_surface_dt/rt_surface_file; hst precision via <output>/data_format) the B-star narrow box shows <Ftop> = 0.879 F_bot while the hst energy budget closes at 1.005 -> 12 % of the injected power leaves by another route (no floors, no events); grey_cons (no taper/force) agrees to 8.5e-4. All 1-D relaxations were gated on the BUDGET, never on Ftop -> may have been present throughout. BUDGET DONE (e2f8c18a rt_budget_verbose; bench/bstar_fecz/budget/A-E): NOT the wall (de <= 5e-6), NOT the taper (no-taper arm 0.740), NOT the force (v.f <= 1e-6), NOT 3-D (1-D bitwise = 3-D); every term <= 1e-4 except the two-stream itself: Fcut = 1.005-1.010 F_bot at the handover, Ftop = 0.69-0.88 at the top, while the RT call removes exactly 1.000 F_bot from the gas -> the APPLIED SOURCE IS NOT THE DIVERGENCE OF THE SWEEP'S FLUX (30 % of F_bot booked as leaving the gas without appearing in the emergent radiation); the relaxed columns (gated on the gas budget) sit in this inconsistent state: Ftop/F_bot 0.707 (fix_taper), 0.740 (fix), 0.896 (taper). Same family as the decad388 non-conservation and the per-cell damping pump. Cell-by-cell source-vs-divergence diagnostic + mode 3's Ftop in progress (agent). CONSEQUENCE: the SLFV emergent flux may be 12 % low and the top structure wrong until resolved.
metadata:
  type: project
---
Input lines for the outputs: <problem> user_hist = true (must be in the file), rt_surface_dt = 200.0, rt_surface_file = rt_surface.bin;
<output1> data_format = %24.16e. Ftop2 is the MEAN SQUARE (MPI_SUM reduction); rms = sqrt(Ftop2 - Ftop^2). Reader in
bench/rtdiag_gate/read_surface.py. Binary build_hip_rtd (build_hip_fix must not be rebuilt while the production chain runs).
See [[bstar-observables]], [[rt-thin-region-taper]], [[bstar-box-growth-and-production]].
