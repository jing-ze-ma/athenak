---
name: session-state-2026-09-10-0910
description: START HERE (written 2026-09-10 09:10 CEST on orion, session 2c3cf987, at the user's "let's continue next time") — the day's results (WB-walk overflow FIXED, RT neighbour-Planck bug FIXED, both gated), what is running (V8f/V9f/V10/V9 corona + free-fall tests, 2 nodes each), what to read first, the uncommitted tree (24 files, 2896 lines) and how to commit it in pieces, and the decisions the user still owns
metadata:
  type: project
---
Supersedes [[session-state-2026-09-10-0300]] (keeps the earlier detail). Real-clock times.

## Read first
1. [[red-giant-wb-polytropic-walk-overflow]] — root cause of the prod11 3.2106e7 NaN AND the
   "WB kick at a cold cell"; fixed (wb_background.hpp + WBGuard + hllc IEN guard + flux floor).
2. [[red-giant-rt-neighbour-planck-bug]] — the two-stream charged each cell the NEIGHBOUR's
   emission; fixed (BFace, surgical form, analytic check says ship it). B13 proved the lidded
   run no longer needs opac_tmin; the cold-cell-collapse family was this bug.
3. [[red-giant-corona-join-cell-cooling]] — V8/V9 (old RT) corona post-mortems.
4. [[red-giant-open-top-prod12-recipe-fails]] — V7 (old RT) failed cold at 6e5.
5. [[red-giant-prod11-died-3e7]] — the whole prod11 chain of evidence + B12 (lidded gate).

## Binary and tree
- build_guard/src/athena (md5 aa5fc2ef..., pinned copy build_guard_pin/athena) = main tree +
  ALL fixes + diagnostics (defaults off: nan_report, rt_cell_report; rt_rescue_eq default ON).
  build_port = older (floor fix + seed only, no WB/RT fixes) -> retire it.
- UNCOMMITTED, 24 files, +2896/-174. Suggested commits, in order (each builds alone):
  a. wb_background.hpp (a_eff clamp + WBGuard absurd) — the WB fix.
  b. hllc_hyd.hpp (IEN guard) + hydro_fluxes.cpp flux-floor hunk — containment.
  c. two_stream_rt.hpp BFace + rt_rescue_eq + rt_top_re warning — the RT fix.
  d. eos.*, eos_table.*, general_c2p_hyd.hpp, general_hyd.cpp, coordinates.cpp,
     prolong_prims.cpp, mesh.hpp, eventlog.cpp — the energy-floor fix (efloor_from_ekin,
     eos_floor_consistent, efloor_de column, ThermoAt sub-floor branch).
  e. red_giant.cpp: vpert_cart + random seed (vpert_rand_amp etc.), gravity-source
     eos.Pressure guard; f. the nan_report / rt_cell_report instrumentation (hydro_tasks.cpp,
     hydro.*, conduction.*, red_giant.cpp, two_stream_rt.hpp, hydro_fluxes.cpp).
  The OLD uncommitted pile (bvals_cc, conduction blend, driver, hydro.hpp/hydro_fluxes
  earlier hunks, mesh.cpp, eos_dump, big red_giant.cpp parts) is mixed in: use `git add -p`.
  Run tst style check first (tools: check_athena_cpp_style.sh from tst/test_suite/style/).

## Running at 09:15 (all /orion/ptmp/jinma/Athenak/red_giant/<dir>/out.txt). The user EXITED
## the session at 09:15, so the watcher agents were killed: NO milestone analysis of V9f/V10
## exists beyond what is noted here. Next session: spawn ONE Opus agent to run v9.py/v9tab.py
## (corona/atmosphere/join tables) and task_V4final_table.py (census) on V9f and V10 at
## 3.09e5, 6e5, 9.45e5, 1.43e6 and the end, plus V8f's post-mortem (collapse at 3.211e5).
- V8f_rtfix 195468 (6e5 K corona, dfloor 1e-26, fixed RT): passed V8's death (2.96e5) but
  dt COLLAPSE at 3.211e5 (hydro 0.97 s, rank 5) -> dying at 3.29e5. Join cell held 3300 K;
  the atmosphere expanded INTO the corona (<v_r> +1e5), corona T 6e5 -> 4.2e5 as it loaded
  stellar gas, L_out/L 0.90 -> 0.73. Post-mortem TODO: which cell/why (v8.py, task_B45_cfl).
- V9f_rtfix 195482 (2e4 K corona, dfloor 1e-18, join 1.141 R, fixed RT): CLEAN at 2.27e5,
  dt 30.6; the cell below the join WARMED to 5128 K by 1.07e5 (V9 old RT: cooled to 2802).
  Milestones 3.09e5 / 6e5 / 9.45e5 / 1.43e6; tlim 1.6e6, wall to ~12:35.
- V10_freefall 195489 (400 K medium at 1e-17, finite opacity, WB OFF everywhere, fixed RT):
  CLEAN at 1.77e5, dt 30.4; medium falls at -5e5, warmed 400 -> 504 K; deep rms|v_r| 4.4x
  V9f's (WB off cost). tlim 1.6e6, wall to ~12:45.
- V9_corona_dense 195436 (old RT, comparison only): envelope vented the corona; conduction-dt
  collapse at 5.72e5 (1.9e-9 s) -> dead. Cancel if still in the queue.
- B12 rst/ (3.45e7) is the prod11 revival point; B13 showed the clamp is unnecessary with the
  fixed RT (rerun a B13-like segment to 3.45e7+ to confirm before reviving without the clamp).

## Decisions the user owns
1. prod11 revival (lidded): from B12_fix_tmin3200/rst (3.45e7) or from prod11_catch rg.00124
   with build_guard, opac_tmin OFF (B13) or 3200 (B12) — the user noted the lid damps the
   photosphere (sponge_zbot 0.98 starts AT the photosphere) so this is interior-science only.
2. Open top for production: V9f (cool corona) vs V10 (free-fall medium, WB off) — wait for
   their 6e5 / 9.45e5 milestones; V10's WB-off deep noise is a cost to weigh.
3. Commit the tree (plan above).
Housekeeping: B11 per-cycle dumps deleted; V4/V7/V8/B5 cancelled; the 09-10 notes' internal
timestamps before 05:30 run ~45-60 min ahead of the real clock.
