---
name: rt-shallow-ramp-missplit
description: 2026-09-14 REOPENED after the sweep fix (hestar_fecz/gate_layer2, re-relaxed columns, one metric): with the ACCURATE sweep EVERY He ramp is growing or marginal - 3/10 +4.0e-3/s Mach 2.3, 10/100 +1.4e-3/s Mach 1.2, 30/100 -5.4e-3/s, 100/300 +2.6e-3/s Mach 0.26 (the old quiet 100/300 was partly the sweep error); the 10/100 relaxation still never settles. The earlier 'numerical' verdict rested on convergence tests run with the buggy sweep -> being REDONE at 100/300 with the fixed sweep (hestar_fecz/conv_fix: nx1/2, x2, cfl/2, x1.5, nquad 1, explicit source, 600 s). If converged -> physical Fe-bump driving (strange modes, see [[he-star-strange-modes]]). He production on HOLD. Earlier: ROOT CAUSE QUANTIFIED (He column): F_diff/F_2s = 1.032 at tau 10, 0.98 at tau 3, 1.004-1.008 at tau 30-100. Three sources: (1) CUT BC BUG - I_up(icut)=B drops mu dB/dtau, F_2s at the cut face = F_diff/2 EXACTLY, heals over 3 cells (hidden by wide ramps); (2) flux LIMITER uses ffree = sigma T^4 instead of c E = 4 sigma T^4 -> -6 % at tau 3, -0.8 % at tau 10; (3) the sweep integrates the source between cell CENTRES, ~4 % low at dtau/cell 1-3 (not fixed). Pumping: spurious deposition 4-9 % of F_b (4-8x the power for Mach 1), ordering set by the local radiative damping at the deposition depth (drive/L 3.9e-3 -> 9.7e-6, 400x, matches the swings). Quadrature exact (Eddington 1/3), same table. FIXES 09-14 on rg-box-convection: cut BC 41196b70 + limiter fc15fa06 (new defaults, legacy flags rt_cut_bc_legacy/rad_flim_legacy; bitwise under legacy; B-star/He deep columns change 1e-4) - CORRECT BUT NOT THE CURE (He swings unchanged: 1.79/0.57/0.056/0.0017); rad_blend_use_2s (164d7618, 3b1cb03a) BLOWS UP (explicit stiff face) and its premise is wrong. THE REAL DEFECT = the SWEEP'S LAYER STAGGER: layer spans a whole cell in dtau but the source runs centre-to-centre -> F_2s/F_int 0.73-1.06 across the He ramp (dtau/cell 3-6), 0.99 above; the B-star ramp (100/300, dtau/cell 3-7) is affected too (absorbed into the relaxed column). FIX in progress (agent 09-14): centre-to-centre layers with consistent dtau, half-layers at top/cut, rt_layer_legacy flag; gate = accurate formal solution within 1 %, then the He arms. RESOLVED = NUMERICAL (hestar_fecz/instab1d, 10 arms): the shallow-ramp He 'pulsation' is the box ORGAN-PIPE mode (P 122-127 s = 2L/c_s) pumped by the two-stream/diffusion FLUX MISMATCH AT THE HANDOVER CUT (driving at tau_lo, not in the FeCZ; swing 1.58/0.50/0.056/0.002 for tau_lo 3/10/30/100; no convergence in nx1 (Mach 7 vs 2.4) or CFL (16x); nquad 1 cuts it 13x; self-sustaining from its own relaxed IC). RULE: tau_lo >= 100 for the He star. He FeCZ horizontal radiative transport needs a horizontal exchange term inside the two-stream region or multi-D RT, NOT a shallower ramp. Strange modes not excluded for the real star but this box cannot host them (t_th < 6 s everywhere, walls fix the period). History: with the 10/100 ramp the 1-D He column is VIOLENTLY UNSTABLE (F_top/F_bot swings 0.04-4x in 50 s, Mach 0.3; the 16x25 s relaxation never converges; hestar_fecz/relax1d_3msun_tau10) while 100/300 relaxes smoothly; the 3-D arm from that column died at 39 s (smoke_rt_3msun_tr10_rlx). After the solver fix the 3-D 10/100 arm had ZERO fallbacks but the SAME +5.6 % E gain and collapsed at 352 s (smoke_rt_3msun_tr10_fix). Surface-layer pulsation instability under the two-stream (physical strange mode amplified? or the semi-implicit two-stream heat exchange in a marginally thick layer?) -> needs a LINEAR ANALYSIS, not more arms. He config stays 100/300 (no horizontal radiative transport in the He FeCZ = the He project's main open item). Earlier: 2026-09-13 DIAGNOSED: the He 10/100 ramp collapse is a SOLVER DEFECT, not physics (RETRACTED: 'FLD cannot carry the flux in a convection zone' - at tau 4-64 F/cE ~ 0.03-0.07, diffusion is accurate to a few %, limiter ~1e-3; convective instability is irrelevant to radiative diffusion). RADIAL SOLVER FIXED 61662b2a (implicit-transverse-raddiff, main tree; NOT yet on rg-box-convection): T-form with rho c_v V/dt on the diagonal = same system, symmetric M-matrix; bitwise on cubed_sphere_raddiff with rad_implicit_x1, conservation residual 3-25x better; He 10/100 RETEST is the gate (needs port + HIP build). The DEFECT in the radial implicit tridiagonal (conduction.cpp ~1245-1290): diagonal uses alpha_i, off-diagonals alpha_{i+-1} -> loses diagonal dominance where 1/(rho c_v) jumps; fallback zeroes x = NON-conservative. Fix: solve in T with rho c_v V/dt on the diagonal + conservative clipping (serves red giant + dhj too). TODO after the unified RKL1 switch. Same tau array + faces in both operators (refuted). After the fix, RETEST the 10/100 ramp in the He box - it should be viable and gives the FeCZ horizontal radiative transport. Earlier symptom text: the two-stream/diffusion tau-blend ramp at 10/100 mis-splits the flux (~0.6 % of F trapped; radial implicit solver falls back continuously at tau 14-21 where the blend weight first becomes non-zero) -> top heats, evacuates to dfloor, transverse stiffness runs away, RKL1 substage cap (rad_ang_maxit 200) clamps, collapse (He 3 Msun smoke_rt_3msun_tr10, dt 1e-8 at 240 s). NOT the operator (conservative 1e-17, never the dt limiter), not the cap. In the He box tau 10-100 straddles the whole FeCZ (tau 4-64); 100/300 sits below it and WORKS (smoke_rt_3msun_tr to 450 s). RULE: use rad_tau 100/300 for both stars. Related to but not covered by decad388.
metadata:
  type: project
---
Analysis: hestar_fecz/smoke_rt_3msun_tr10/analysis/FINDINGS.txt (from log + hst only; the run died before its first dump,
event log empty - next shallow test needs dump/log cadence of seconds). See [[rt-handover-semi-implicit-bug]],
[[rad-cap-ang-spurious-cooling]], [[implicit-transverse-raddiff-plan]].
