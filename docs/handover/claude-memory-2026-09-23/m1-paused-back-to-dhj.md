---
name: m1-paused-back-to-dhj
description: User decision 2026-09-21 night - after the three VET tensor tests, stop all M1 work; next project is the deep hot Jupiter (dhj)
metadata:
  type: project
---

User, 2026-09-21 night: once the three tensor tests (frozen / tilted / tau-refreshed Eddington tensor in
the implicit solve) are done and recorded, DO NOT TOUCH the M1 radiation module any further; the next
step is to go back to the deep hot Jupiter (dhj) work.

**Why:** the M1 module exists to prepare for a VET scheme whose Eddington tensor will come from short
characteristics (numerical closure, refreshed every hydro step); analytic chi(f) fixes do not serve
that goal. Tonight established: every chi(f) closure (Levermore, Minerbo, Kershaw) is unstable in the
optically thin top of the 2-D implicit solve because (chi, n) are refreshed from the cell's own F
(local algebraic feedback at c dt/dx ~ 7e3); a fixed tensor (Eddington) is clean over 5 turnovers.
No further M1 closure / Newton / optimisation work was asked for.

**How to apply:** finish + commit the tensor tests, update the handover doc and [[rad-m1-design]],
then switch to dhj: start from index-hot-jupiter.md (sub-index) and the latest dhj notes; ask the
user which dhj question comes first if it is not obvious. Do not start new M1 agents or items
(multigrid, halos beyond the running agent, (E,F) Newton) unless the user reopens it.

**09-22 ~22:10 USER REOPENED M1 ("continue on implicit M1 in the massive star context").** Pause lifted.
Next step launched (worker agent): grey short-characteristics formal solution -> Eddington tensor K/J, lagged
per hydro step, fed through the dbg_tensor hook as <rad_m1>/closure = vet_sc (default off); gates on the
seeded 2-D He slab (vs T1_frozen / T3_tau), analytic K/J in a static atmosphere, cost per step. Snapshot
bench/m1_vet_0922, results tests_m1/runs_3j_vet/RESULTS.txt, patch vet_sc.patch (not committed).
USER GOAL 09-22 ~22:20: make implicit M1 AS FAST AS POSSIBLE and make sure it WORKS IN 3-D MASSIVE STARS. Plan: measure first (worker: HIP build, 3-D seeded He box, Eddington closure, apudev rocprof + stability + two-stream cost reference -> tests_m1/runs_3k_gpu3d/RESULTS.txt with a ranked speed-up list), then optimise in that order, then 3-D production-like runs; VET (runs_3j_vet) merges in after.
09-22 ~23:00 VET (vet_sc) DONE, NOT committed: bench/m1_vet_0922/vet_sc.patch (applies to 886bd677), tests_m1/runs_3j_vet/RESULTS.txt (verified). Grey SC, 64 rays, lagged per hydro step, one-axis tensor via dbg_tensor hook (axis = SC flux direction). Seeded 2-D He slab 200 s: Picard 4.14/6, 0 nonconv, F1top/Fin 1.0000, KE2 1.81e21 (T3_tau 1.87e21); eigen-axis variant FAILED (KE2 3.5e4x); plane-parallel K/J err 2.2e-3 (4 mu), deep 1/3 to 1e-9; cost 6.1 % of the implicit solve (serial CPU). LIMITS: ONE MeshBlock / one rank only (no pipelined multi-block sweep), one-axis tensor, no GPU timing -> NOT usable in the 3-D multi-block GPU box yet.
09-22 ~23:15 M1 3-D GPU PROFILE (tests_m1/runs_3k_gpu3d/RESULTS.txt, verified): 84x104x104 He box, Eddington, 1 GPU 2.02e6 zone-cyc/s vs two-stream 1.03e7 (5.1x); 2 GPUs 2.21e6 (1.1x); stable 648 steps. Step ~490 ms: BiCGStab 69 % (tridiag line precond 35 %, 1-thread-per-column Thomas), halos 15 %, GPU idle 25 % (3432 launches, 489 blocking reductions/step), EOS <= 1.1 %. Ranked: PCR line solve ~1.5x; looser inner tol 1.2-1.5x; fused reductions 1.15-1.25x; halo copy ~1.1x. FLAG: eos_cache max|dq|/q 8.3e4 on 2 ranks vs 6.7e-2 on 1. IN FLIGHT: PCR agent (bench/m1_pcr_0922), eos_cache diagnosis (bench/m1_eosflag_0922).
09-22 ~23:55 EOS_CACHE FLAG = diagnostic only (divides by net exchange q ~ 0; fix: divide by gross exchange; apply after the PCR/fulltensor patches merge; solution unaffected, tests_m1/runs_3k_gpu3d/EOS_CACHE_FLAG.md). USER: make the TAU closure (column optical depth, Hopf K/J, axis -grad tau; stable in the slab) a real closure option with multi-block/multi-rank support -> worker in bench/m1_tau_0922, results tests_m1/runs_3l_tauclosure/RESULTS.txt. Plan: tau closure = 3-D workhorse; SC VET = accuracy reference / porous regimes.
09-23 ~00:30 PCR COMMITTED 1715190b (pushed to fork): implicit_line_solver = pcr (default thomas), GPU 3-D production box 2.15e6 -> 3.24e6 zone-cyc/s (1.51x), half box 1.70x; factor reuse would add only ~2-4 %. Default flip to pcr pending a check of the gather partition / multi-rank path. Next M1 speed-ups: fewer host syncs (fused reductions), halo copies, inner-tolerance with a scaled norm (mode-3 lessons). Branch-wide cpplint: 2168 pre-existing errors (not from these patches).
09-23 USER: no multigrid for M1 for now (inner its 22.0 -> 24.6 half vs full width, same dx; cost is latency-bound; decide later with a horizontal-resolution scaling test if iterations grow). Host-sync speedup agent running (bench/m1_sync_0922 -> README_SYNC.md).
09-23 ~01:00 VET + FULL TENSOR COMMITTED 1d22a118 (pushed to fork), default off: closure = vet_sc, vet_tensor = uniaxial|full, vet_eig_min; D through M1DDiag/M1POff/M1OffDiv. GPU: SC ~10 % (2-D) / ~6 % (3-D) of the step; full vs uniaxial +0.03 ms. Still ONE MeshBlock only. Tau-closure and host-sync agents told to rebase onto 1d22a118.
09-23 ~01:15 USER: test SC used directly as heating/cooling (no M1/VET; MURaM/CO5BOLD style, ALI implicit per cell, lambda iterations, deep diffusion handover, SC flux force) on the seeded 2-D He slab vs implicit M1+VET -> worker in bench/m1_scheat_0923, tests_m1/runs_3n_scheat/RESULTS.txt. Halo-packing speedup agent also running (bench/m1_halo_0923, touches src/bvals shared by all modules: bitwise gates on prod4/hyd4 inputs).
09-23 USER asked: improve SC sweep scaling across GPUs (18.5 -> 24 ms/call on 2 GPUs; per-layer MPI band exchange, ~84 latency-bound exchanges per sweep). NOT multigrid (sweep, not elliptic). QUEUED until the halo agent and the SC-heating agent land (all touch rad_m1_vet.cpp): try (1) overlap interior-cell compute with the band exchange, (2) wider halo every k layers, (3) angle decomposition (rays split across GPUs, one allgather of opacity/source + one reduce of J/H/K per sweep; the multi-node answer), (4) the > 32 kB functor syncs (halo agent).
09-23 ~02:15 SC HEATING DONE, NOT committed (bench/m1_scheat_0923/scheat.patch vs dccdf506; tests_m1/runs_3n_scheat/RESULTS.txt; timings verified job 11944339): J-S form alone fails deep (0.958 of flux, needs implicit x1 diffusion below tau 3 -> RE matches Hopf to 1.6e-3); 200 s slab stable, within 3e-3 of M1 VET below the top, top 2.2 % cooler (exact grey vs M1 Marshak). SPLIT-LAG: KE growth ratio 1.29-1.32 per CFL halving (M1 0.97) -> dt-dependent; Strang at dt = Lie at dt/2 (not a fix); IMEX needed (not built). Lambda iterations > 1 make horizontal KE grow -> 1 sweep default. GPU ms/step: M1 VET 73.6 (2-D) / 236-255 (3-D); SC Lie 28.1 / 65.5; SC Strang 43.2 / 93.0; at equal accuracy (Strang at half CFL) SC 86 vs 74 (2-D), 186 vs 236 (3-D).
09-23 ~02:25 USER: 3-D CFL pair (0.3 vs 0.15, + 0.075 if cheap) for implicit M1 + vet_sc full over a convective turnover, GPU; worker in bench/m1_vet3dcfl_0923 -> tests_m1/runs_3o_vet3d_cfl/RESULTS.txt; may use apu with restart chaining, cap ~24 GPU-hours before reporting. 2-D pair gave 0.97 (start-up ringing only).
09-23 ~02:35 USER: once the halo agent finishes (verify + commit its patch), START (a) M1 Picard-pass reduction (hunt lagged linear terms -> exact Jacobian, mode-3 exjac lesson; then inner Krylov tolerance with a scaled norm) and (b) SC sweep scaling across GPUs (overlap band exchange with interior compute; wider halo every k layers; angle decomposition across GPUs), both on HEAD incl. the halo commit, all timing on GPU.
09-23 02:40 3-D CFL pair RUNNING (apu, 1 GPU each): C30 job 11944587 (cfl 0.3, to t=3800 s = 1 turnover Hp/v_MLT = 3761 s, ~1.5 h), C15 11944588 (0.15, ~2.5 h), C075 11944589 (0.075, to 1000 s, ~1.1 h); dir bench/m1_vet3dcfl_0923, binary dccdf506. LOOK after ~05:15: python3 tests_m1/runs_3o_vet3d_cfl/gate3d.py C30 C15 C075 (run in bench/m1_vet3dcfl_0923), write RESULTS.txt. Note: the log's 'turnover H_p/v*' line is ~5000x too fast (ignore).
09-23 ~03:30 HALO COMMITTED d14f96de (bvals functors < 32 kB; M1 box 247 -> 192 ms/cycle 1 GPU, 214 -> 161 2 GPUs; bitwise incl. prod4/hyd4). HSA_NO_SCRATCH_RECLAIM=1 (env var): prod4 rot-283 restart 22.5 -> 17.0 s GPU time (1.32x), identical output (README_HALO.md) -> USER DECISION pending: add to prod4/hyd4 submit.sh at a link boundary (pending links snapshot the script: needs cancel + resubmit of links 2-4). Launched: M1 Picard reduction (bench/m1_picard_0923 -> README_PICARD.md), SC scaling (bench/m1_scscale_0923 -> runs_3m_vetmb/README_SCALING.md); T4 told to use the env var.
09-23 ~04:15 PICARD COMMITTED a6202226 (default off): recommended implicit_lres_test=false implicit_conv_est=true implicit_predictor=step implicit_lin_ew_max=1e-2 -> 3-D box Eddington 184.7 -> 116.4 ms/cycle (1 GPU), 153.3 -> 94.3 (2 GPUs); vet_sc full 186.6 -> 116.9 / 166.5 -> 106.2. Cumulative M1 3-D box 1 GPU: ~466 ms/cycle equivalent at 2.02e6 zc/s baseline -> ~116 ms (about 4x). Convergence already quadratic; no lagged linear term. Env sweep: nothing new beyond HSA_NO_SCRATCH_RECLAIM. TODO: flip M1 defaults (pcr, bcg_sync 1, Picard set) after a multi-rank / gather-partition check.
09-23 ~04:25 USER GOAL: implicit M1 cost COMPARABLE TO HYDRO (now ~116 ms/cycle vs hydro ~20 ms on the 3-D box, 1 GPU). Worker bench/m1_fast_0923 -> README_FAST.md: profile, then stronger preconditioner (transverse), mixed precision inner Krylov, launch fusion / HIP graphs, restrict implicit domain (deep diffusive cells), pipelined BiCGStab. Defaults gate agent bench/m1_defaults_0923 -> README_DEFAULTS.md (flip pcr/bcg_sync/Picard set only if multi-rank + gather + all closures pass).
09-23 ~05:30 M1 DEFAULTS COMMITTED 66d5c59a: pcr + bcg_sync 1 for all closures; lres_test false + conv_est true + lin_ew_max 1e-2 only for eddington / vet_sc / tau (NOT m1-Levermore: breaks Picard convergence with lag=pass); predictor kept 'none' (not in restart file -> restarts not bitwise). TODO: write predictor state into the restart file, then make predictor = step default (~29 % fewer inner iterations). Defaults gate README tests_m1/runs_3k_gpu3d/README_DEFAULTS.md. Agents' big runs now in /viper/ptmp2/jinma (u2 inode quota).
09-23 05:40 3-D CFL VERDICT (tests_m1/runs_3o_vet3d_cfl/RESULTS.txt): mean T(z) dt-independent (3e-4), vertical KE converging, but HORIZONTAL KE strongly dt-dependent (~dt^1.7 early, x15 at one turnover C30 vs C15). Suspect the once-per-step lagged SC tensor. Next: same pair with eddington and with uniaxial vet.
09-23 ~05:50 SUBMITTED isolation arms (bench/m1_vet3dcfl_0923/{E30,E15,U30,U15}, jobs 11945504-7, apu 1 GPU, tlim 1000 s): E = closure eddington, U = vet_tensor uniaxial, cfl 0.3 vs 0.15. Compare horizontal KE at t=100..1000 like C30/C15 (C: 8.2e24 vs 2.4e24 at t=100). Analyse with tests_m1/runs_3o_vet3d_cfl/gate3d.py E30 E15 U30 U15 in bench/m1_vet3dcfl_0923.
