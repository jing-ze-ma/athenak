---
name: he4-strang-vs-unsplit-mlt-seed
description: why rt_strang=true and rt_strang=false converge to different limits on the He4 column - the MLT shell closure is seeded before the two-stream has run (unsplit only)
metadata:
  type: project
---

09-20: on the no-force He4 setup both splittings CONVERGE in dt but to different limits (tests_r15 S0.3/G7/S0.075: mom(0.25 turnover) +1.50/+1.39/+1.37e30; U0.3/U0.15/U0.075: -2.96/-4.97/-5.22e30, KEr 40-50x). Strang cfl 0.3 is only 8 % off converged at 0.25 turnover (KEr 1.8x at 1.0): usable (user chose cfl 0.3 for wedge8).
CAUSE (tests_r16, agent, confidence high): red_giant.cpp ~3413 rt_face_flux_ready() is false on the first closure call under rt_strang=false (the in-stage sweep at ~4363 sits AFTER the MLT block), so F_2s = 0 in dfc = F_req - F_rad - F_res - F_2s (3619), min(F_mlt, dfc) picks the MLT amplitude, and the first call LATCHES fmlt1d without relaxation (seed, 3509/3655-3662); mlt_relax_time 1e4 s then freezes 2.05x too much subgrid flux, reaching 4 cells further out -> force dipole at 0.89-0.96 R. Under Strang the split sweep runs in before_timeintegrator first, so the seed is right. Evidence: mlt_alpha=0 -> the two modes agree to 0.04 % (unsplit integration itself is fine); Strang with in-stage MLT deposit == Strang split deposit to 4 %; mlt_relax_time=0 -> same sign, within 25 % (residual = one-stage lag of F_2s amplified by the closure's near-cancellation: F_used is a 13 % residual of O(F_req) terms).
This also explains the 09-18 "unsplit is inconsistent" verdict in [[he4-r11-floor-arms-verdict]]: it was the seed, not the scheme. NOT FIXED yet (fix = build the face flux before the first closure call, or do not latch on a call without it).
Side finding (both modes): fmlt1d is relaxed once per RK STAGE with bdt -> 1.5 dt of relaxation per cycle; mlt_relax_time is effectively 1/1.5 nominal.
See [[he4-rad-eos-force-split-design]].

**DEEP ZIGZAG = mlt_split_deposit AT LARGE dt (09-20 ~04:30, 1-D tests_r15, cfl 0.3, 1.5 turnovers, rho/1e-9 at i=15..39 step 3):** S0.3 (split deposit) 2.45 2.33 2.29 2.44 1.50 2.07 2.48 1.50 2.22 = zigzag; Z_in (problem/mlt_split_deposit=false, deposit in the RK stages, RT still Strang) 2.36 2.13 2.23 2.14 2.13 2.14 2.33 2.41 2.39 = SMOOTH, hst within 10 % of S0.3/G7; Z_r0 (mlt_relax_time=0) worse zigzag + KEr x10; Z_m0 (mlt_alpha=0) smooth but the envelope inflates (rho 0.9e-9, KEr 8e38: the closure's flux is needed). The zigzag is visible in 1-D profiles (not in hst): cfl 0.3 strong, 0.15 weak, 0.075 none. So the 09-19 split-deposit fix trades the O(dt) force dipole at 0.925 R (in-stage: 4 % of rho g at cfl 0.15, 8.5 % at 0.3) for a dt-dependent grid-scale instability at the MLT base (killed wedge8 at cfl 0.3). wedge7 (cfl 0.15, split deposit) at 2.23 turnovers still smooth to ~5 %.

**ZIGZAG MECHANISM + FIX 09-20 ~06:00 (tests_r17; UNCOMMITTED in bench/wt_he4; binary tests_r11/athena_v22; default-off bitwise vs v21).** First quantity to alternate = F_2s (the two-stream face flux), not T (smooth to 1e-3) nor F_used: loop F_used^(n+1) = F_req - F_2s^n, F_2s^n = F_2s^eq - a F_used^n with a = theta/(1+theta), theta = D dt/dx^2 -> gain enormous and ~dt at the GRID scale only; the max(0,.) rectifier turns the symmetric flux wiggle into a one-signed deposit; rho ratchets because p = p(T) to 2 % (no restoring force). In-stage deposit has no loop (zz(F_2s) 8e-3) but pays the O(dt) dipole. FIX = problem/mlt_split_sync (needs mlt_split_deposit + mlt_mean): one [1 2 1]/4 pass over the faces on the closure TARGET before it enters the relaxed profile (conservative, walls mirrored). Tried and FAILED: re-forming the deficit from the sweep just done (explicit treatment of a stiff term, blows up, arm Y3); dropping the rectifier (worse, Z3). New diagnostic problem/mlt_dump_dt [s] appends the face table to <mlt_dump>.ts. ACCEPTANCE 1-D: cfl 0.3 zz(rho) 0.618 -> 0.025 (ref S0.075 0.030), mom/KEr within 4 %/8 % of S0.075 at 1.0 turnover; cfl 0.15 0.134 -> 0.010; force dipole 0.42 % (split only 0.39 %). Caveats: F_2s still noisy +-20 % face to face (out of the loop now), RCB blurred by one face, NOT tested in 3-D.
