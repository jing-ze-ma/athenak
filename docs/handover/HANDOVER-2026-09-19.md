# Handover 2026-09-19 (viper, written ~05:30)

Everything in HANDOVER-2026-09-18-B.md still holds (productions done, pages, parked ports).
This note covers only the global 4 Msun presupernova He star (branch he4-presn-global,
worktree bench/wt_he4, HEAD 27544151, NOT pushed). Full chronological notes, including
three retractions: bench/wt_he4/tests_r11/RESULTS-0919.md (= memory note
he4-r11-floor-arms-verdict in claude-memory-2026-09-19/).

## Established
- Density floors (d1 1e-11, d2 1e-10) only delay the dt erosion; d2 is above the IC top density.
- The blow-up at 1.7-2.0 turnovers was the two-stream TOP CLOSURE (unresolved column) shutting
  when dense gas reached the top. RECIPE: time/cfl_number=0.15, problem/rt_top_vacuum=true,
  rt_strang=true (rt_strang=false DIVERGES as dt shrinks: never use). 1-D alive at 3 turnovers
  (L_out/L 0.99-1.01), 3-D run tests_r11/v3d alive to 4.7 turnovers (L_out/L 1.03-1.08).
- What remains is a slow RADIAL expansion, identical in 1-D and 3-D, dt-converged, independent
  of the top hydro BC (open / wall / new lid), not removed by 1-4 turnovers of global damping.
  Its outward half pushes BOUND gas (4-7 % of v_esc) through the top at 1.014 R: -20 % of the
  domain mass in v3d. A larger domain is not possible with the current EOS table (H_gas 0.002 R).
- Per-cell force measurement (tests_r12/fbud.py, rt_profile_dt=1): the net force is a DIPOLE in
  the density-inversion layer, -1.7 % of rho g at 0.90 R, +3.2 % at 0.925 R, from the first
  step. Unchanged by WB dynamic off, WB static, frozen MLT flux, and the flux corrector.
  nx1=192 halves the peak.

## Retracted (do not reuse)
- "rt_strang is the culprit", "two-stream surface condition / diffusivity 1.5", "t=0 top flux
  excess 1.28 drives the expansion" (it is a seconds-long transient of the tapered top cells),
  "thermal flux imbalance is the driver" (mlt_flux_fix changes nothing). tests_r12/relax_ic.py
  and its ic_passNN.txt chase the transient: not for use.

## New code (all default off, default-off builds bitwise vs c15vac3)
problem/open_outer_lid, problem/vdamp_all_until + vdamp_all_time, problem/mlt_flux_fix (+cap,
+rmax); parameters added to inputs/hydro/he4_presn_cs.athinput because a command-line override
of an absent parameter is fatal. Binaries tests_r11/athena_v8..v10 (v10 has everything).

## Next
1. Confirm wellbalance_static was really active in tests_r12/fbs (startup print), then find the
   momentum term that makes the dipole: geometric source, reconstruction/FOFC at the density
   maximum (0.945 R), gravity at faces vs centres in the inversion.
2. Only after the 1-D column is quiet for 5 turnovers: 3-D with the recipe (user approval).
3. Parked: port 4bcdc855 + 94c7165d to rt-integration, bvals_fc seam defects, push after OK.
   Opus agent quota returns 2026-09-19 23:00.
Also new: bench/analysis_0916/hrd_3d/hrd_3d_dark_arepo.{png,pdf} (+_transparent): AREPO
highlighted, everything else faded (make_hrd.py highlight=, BG_FADE).
