# HANDOVER 2026-09-12 (orion -> viper): FOFC complete, merged, and the RG_fofc red-giant run

HEAD = fork = c90bd869 (+ this commit). Read after HANDOVER-2026-09-11.md and its addendum.
Memory snapshot: docs/handover/claude-memory-2026-09-12/ (copy into the viper memory dir; MEMORY.md there
starts with the standing rules and the 09-12 01:00 session state).

## 1. What landed (all pushed)

| commit | content |
| --- | --- |
| e1db81d8 | red-giant RT top-cell guard + `dfloor_keep_temperature` (its body lives in gnomonic_raisevel.hpp) |
| b3345f55 | FOFC steps 3-5: hydro FOFC on spherical-polar + cubed sphere (area/volume trial update, gnomonic rotation of the single-state solves, deferred cs floors on the test pass via `coordinates/gnomonic_raisevel.hpp`), MHD FOFC (`<mhd>/vceil`, `<mhd>/fofc_rsolver = llf|hlle`, WB full state, sp trial curl), MHD FOFC on the cubed sphere (`gnomonic_raisevel_mhd.hpp`, EMF rotation), built-in test pgen `wb_atm`, `shock_tube` gained `problem/bazi`; 9 test files, 48 cases, incl. `_mpicpu` with static refinement |
| 7cee2eb7 | merge of viper's fb69c554; our FOFC test-pass flagging ported into `eos/{ideal,general}_hyd_floors.cpp`; `dfloor_keep_temperature` added to the `floors_legacy` condition |
| c90bd869 | merge of the 09-12 addendum |

Gates on orion (CPU): 48 FOFC tests pass; red giant merged vs pre-merge bit-identical (150 cycles, switches
on); dhj sp hydro merged vs origin bit-identical; **dhj ck (sp MHD + table EOS + RT) differs by 1 float32 ulp
in vel/bcc3, dens/eint identical, deterministic; ablation: reverting ONLY src/utils/two_stream_rt.hpp restores
identity, so the seed is the RT guard e1db81d8 (CPU FMA contraction from the added branches), not FOFC and not
the merge.** If dhj must stay bitwise on viper, gate the guard the way floors_legacy gates the floors.

FOFC constraints: FOFC + PLM needs `mesh/nghost >= 3`; the ghost count is baked into restart headers (an
override on restart is silently ignored), so FOFC runs must start from scratch or from an nghost>=3 restart.
The cs FOFC kernels capture six extra trig Views; the FOFC refactors split kernels and inline bodies, so expect
last-bit drift on HIP (your 09-11 note) -- build Debug once (Kokkos bounds check) before trusting a GPU FOFC run.
Nothing FOFC has been built on a GPU yet.

## 2. The RG_fofc run (orion, running) and how to start it on viper

Question: does FOFC replace the velocity ceiling on the red giant? Config = I8_vceil's, except
`hydro/fofc = true`, `hydro/vceil = 0`, `dfloor_keep_velocity = true`, `efloor_from_ekin = true`
(floor consistency fixes kept by the user's decision), `dfloor_keep_temperature = false`, `mesh/nghost = 3`,
from scratch, tlim 9e5, rst every 1e5. Deaths to beat: 5.7e5 (shell launch), 5.87e5, 6.13e5.

Files (this commit):
- `inputs/hydro/red_giant_fofc.athinput` -- the exact run input; data paths point at the viper clone.
- `data/stellar_opac/rosseland_gs98_x0.7_z0.014.txt` -- the merged Rosseland table (force-added, 0.5 MB; see
  data/stellar_opac/PROVENANCE.md). The ck tables in data/exo_fms_ck are referenced but unused (rt_ck=false).
- `docs/handover/scripts/rg_fofc/sub_orion_link1.sh`, `sub_orion_link2.sh` -- the orion chain (link 2 is
  generic: restarts from the newest rst; resubmit it for further links), `i8.py` -- the analysis script
  (set RUN to the run dir; reads rg.hydro.hst, rg.log, bin/).

Orion state: jobs 196662 (link 1, pin13 = b3345f55 tree) -> 196663 -> 196664 (pin14 = c90bd869 tree, ~10%
faster from your seam-pack/conduction commits: 0.486 vs 0.54 s/cycle on 32 ranks x 7 threads, 2 nodes),
run dir /orion/ptmp/jinma/Athenak/red_giant/RG_fofc. At 1 h: t=2e5, dt 30.67 flat, no floor/FOFC event.
Costs 1.8x V9f per cycle (ghosts 1.37x + FOFC); expect ~3e5 per 4 h link.

On viper: build with `-D PROBLEM=red_giant` (general EOS table is generated at startup from the eos_* params,
no file), HIP as in scripts/build_gpu_viper.sh. 96 MeshBlocks; the from-scratch start needs no rst. Watch
`rg.log` (columns cycle eos_dfloor eos_efloor eos_tfloor eos_vceil eos_fail c2p_it fofc efloor_de; the first
nonzero `fofc` row is the first flag), `rg.hydro.hst`, and dt in stdout. A GPU run is a different realization
(chaotic convection), so compare against the death points, not cycle by cycle.

## 3. Open

1. RT guard gating for dhj bitwise identity (your call).
2. GPU build + Debug check of the FOFC kernels (nothing FOFC has run on a GPU).
3. Wiki entries: `hydro/fofc` on sp/cs, `mhd/fofc_rsolver`, `mhd/vceil`, `problem/bazi`, pgen `wb_atm`.
4. RG_fofc outcome in the 5.7e5-6.3e5 window (orion link 2, ~09-12 morning).
