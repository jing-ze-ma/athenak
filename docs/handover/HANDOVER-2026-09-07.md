# Handover, 2026-09-07 09:50 CEST, viper -> orion

Viper is in maintenance from 2026-09-07 12:00 to 2026-09-12 12:00. The next session runs on
orion, which cannot see viper's `bench/`, scratch or home directories. Everything needed is in
this branch (`polar-average-perf`, fork `jing-ze-ma/athenak`, HEAD 7c652768):

- this document;
- `docs/handover/claude-memory-2026-09-07/` -- a copy of the assistant's memory notes as of the
  handover (`MEMORY.md` is the index; each file is one topic). A session on orion should copy
  them into its own memory directory or read them from here. Their "CURRENT STATE" block is the
  same as the one below.
- `docs/handover/scripts/` -- the analysis scripts that lived in scratch (excluded from flake8).

## What the code can do now that it could not a day ago

All committed and pushed, all with regression tests that PASS on viper (CPU):

| feature | switch | commit | test |
| --- | --- | --- | --- |
| hydrostatic well-balanced scheme, polytropic closure, cached, cs + sp + Cartesian, stretched r | `<hydro|mhd>/wellbalance_dynamic, wb_x1, wb_option=polytropic, wb_cache_every` | 140dbf9e .. 8c384e46 | wb_column tests (inputs/tests/wb_column*.athinput; memory note wb-hydrostatic-scheme-cs.md) |
| rotating barotropic background + ghosts | `problem/rot_potential` | 5c6acfba | -- |
| radiative diffusion below the two-stream (Rosseland conductivity) | `isotropic_conduction = radiative`, `rad_flux_inner` (<0 = sigma T_int^4 from Teq) | 56d4d2fe | wb_column_radcond |
| optical-depth BLEND of diffusion and two-stream | `rad_tau_lo = 30, rad_tau_hi = 300` | fad2f5db | test_rad_dhj_ck_cpu (rewritten gate) |
| kappa_R tabulated from the ck table + continuum at start-up | `rad_kappa_src = table` | 6271d193 | same |
| exact face-normal gradient on the cubed sphere | `rad_cs_exact` (default true) | ebd57244 | test_rad_cs_raddiff_cpu (cs_test iprob 15) |
| AUSM+-up cut-offs and wall-HLLC hybrid | `hydro/ausm_mcut, ausm_mcut_p, ausm_wall_hllc` | f6f0d6f4 | column tests (memory: lowmach-solver-audit) |
| velocity seed for the column | `problem/vpert, vpert_grid` (wb_column) | 2f612c96 | -- |
| abort on non-finite state | `time/nan_check_cycles` (default 100) | d3d74f2b | manual (column that dies at cycle 5) |

The dhj inputs `deep_hot_jupiter_rt_eos` and `_blowup` carry ALL of the above by default
(WB polytropic + rot_potential + radiative diffusion blend + table opacity; `rt_ck = true`).
The grey inputs (hydro, ideal_xe) carry WB + rot_potential only (the blend needs the ck RT).
Cost of the deep-RT package on the production inputs: +9.5 % (sp), +5.8 % (cs), dt unchanged.

## Running on viper through the maintenance (results visible only from viper, after 09-12)

| run | dir on viper | jobs | what to read |
| --- | --- | --- | --- |
| cs_mhd_prod2 | bench/cs_mhd_prod2 | 11526708 (lead, to ~rot 18) then 11526681, 11526682 | cs MHD production on the new defaults; rot 10.9 at 09:00, tracking the old run within 10-20 %. Does it pass rot 41.6? |
| cs_hyd_rs/{hllc,lhllc,ausmpup,ppmx} | bench/cs_hyd_rs | 11529253/56/59, 11529330 + two chained each | pure-hydro solver/reconstruction comparison; the jet at rot 15-20. At rot 2: horizontal KE equal to 9 %, LHLLC keeps 70x HLLC's deep radial KE (65 vs 26 m/s), AUSM+-up ~ HLLC, ppmx ~ HLLC. |
| sp_pole_bisect/{wall_r,x3shift_r} | bench/sp_pole_bisect | 11526902, 11526903 (end ~11:30 at rot ~4.9) | gate: history ME1 at rot 4.7 (dirty 1.6e34, clean 8e31) and `polerow.py` on bin 00004. wall_r clean => fefe6a17 (wall mirror) is the cause; x3shift_r reads the pole commits. |
| cs_mhd_prod_nan | bench/cs_mhd_prod_nan | 11529359 | restart from rot 40 with 0.05-rot dumps: PASSED rot 41.6 clean; restarts diverge at 5e-6 within 0.1 rot -> not bit-reproducible. |
| lowmach/isoL_N64_* | scratch (may be gone) | login-node serial | 2000-t_BV Edelmann slow-mode test, hllc/lhllc/ausmpup/variants; ~50 % at 09:00. |

`cs_mhd_prod` (the old production) died at rot 41.6 (NaN everywhere, no precursor) and was
cancelled; its rot-40 restart is the last good one.

## What to do first on orion

1. `git fetch fork && git checkout polar-average-perf && git submodule update --init --recursive`.
   Build CPU: `cmake -B build_dhj_cpu -D PROBLEM=deep_hot_jupiter_rt -D CMAKE_BUILD_TYPE=Release`.
   The ck tables are at `/orion/u/jinma/ATHENAK/athenak/data/exo_fms_ck` (not in git; see
   data/exo_fms_ck/PROVENANCE.md). The dhj inputs reference `data/exo_fms_ck` relative to the
   run directory or the repo -- use absolute paths in a bench copy.
2. `cd tst && python run_test_suite.py --test test_suite/rad/test_rad_dhj_ck_cpu.py` and
   `--test test_suite/rad/test_rad_cs_raddiff_cpu.py`. Both PASS on viper at ebd57244.
3. Any GPU build must include d3d74f2b (the NaN guard); viper's build_dhj_gpu does not.
4. Determinism test for cs MHD (the one thing the NaN hunt asked for): restart twice from one
   restart file for 200 cycles on the GPU and diff the histories bitwise. If they differ, find
   the nondeterministic kernel (candidates: the cs seam exchange, the EOS resistivity, the WB
   cache); if not, the rot-41.6 NaN was a transient fault.
5. The EOS-inversion table `eos_table.txt` used by `scripts/dhjcs.py` is produced by
   `<mhd>/eos_table_dump` (memory: eos-table-dump); regenerate it on orion before any
   temperature/pressure plot.

## Open questions, in priority order

1. Does the jet differ between hllc / lhllc / ausmpup / ppmx? (viper results after 09-12.)
2. Does the deep interior drift differently with the radiative outlet (cs_mhd_prod2 vs the old run)?
3. The sp polar-row blow-up: which commit in 2a64e2c7..18f5dd21? (round 2 on viper.)
4. Edelmann's slow decoupling mode: does it grow on 2000 t_BV with lhllc / ausmpup here?
5. The rot-41.6 NaN: determinism first, then reproduce.
   ANSWERED IN PART on orion: the CPU restart path is bit-identical, even across rank
   layouts, so the divergence is GPU-specific. See RESULTS-2026-09-07-orion.md.

## Things that bit tonight (all recorded in the memory notes)

- `pgrep -f "make -j 8"` matches the polling shell itself; wait on a log marker.
- The stock `wb_column.athinput` is SUPERADIABATIC (tgrad -0.3 vs -0.286): every solver
  "grows" KE by convection. Use tgrad -0.1 for a rest test.
- The 3D unsplit CFL limit is 1/3: CFL 0.5 kills HLLC too.
- Original AUSM+-up (shared cut-off ~M) is an explicit pressure diffusion with dt ~ f_a^p:
  unusable at low Mach explicitly; keep `ausm_mcut_p >= 0.3` (default 1).
- AthenaK did not stop on NaN (fmin drops it); it does now.
- The python `sbatch` of a directory whose input file failed to be written still submits.
- The Velasco-Romero "C < M" restriction is the anti-diffusion of a first-order-in-time
  integrator; rk2 and rk3 show no restriction for LHLLC here. Forward Euler kills HLLC too.
