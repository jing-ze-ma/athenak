---
name: implicit-transverse-raddiff-plan
description: 2026-09-15 ACCELERATION: rad_sts_perplane (fcaac111, per-plane RKL1 counts; correct, bitwise where equal, but only 1.02x because the loop still runs to s_max and skipped planes cost a register copy - restrict the kernel's i-range per substage to recover ~1.5x more) and rad_tr_tau_lo/hi (c2e5911d, smooth transverse weight in log column tau; 1/3: 1.9x, 3/10: 2.0x, 10/30: 2.3x on a 336x64x64 cost box; photosphere weight 0 for all; solutions agree to 1e-10). prod_whole RESTAGED with perplane + taper 3/10 on build_hip_pp (pinned): 8 GPUs, --time 19 h (13.9 h ideal) for 40 turnovers; 2 GPUs = 56 h. 2026-09-14 rad_blend_transverse (414b9ee4, default true = bitwise): the transverse operator's weight followed the VERTICAL tau-blend -> in EVERY production run horizontal radiative transport was active only below tau 100 (none at the photosphere; the 3.4 % dT/T there may be inflated). With false: the operator runs everywhere down to rad_gate_rho (MUST set ~1e-9 = the taper's rho_lo; ungated it dies at cycle 2, stiffness 7e9 at rho 1e-12); cost 3.1x production, 145-197 substages vs the cap 200 (raise rad_ang_maxit); photospheric Mmax 0.01 -> 0.21 with horizontal transport on (check before trusting). The PCG (implicit horizontal) is relevant again for the thin region. Test dir bench/bstar_fecz/blendtr. SPEED PACKAGE (main tree HEAD 3cf3161d, commits a26754aa/3b08a591/3cf3161d, unported): rad_sts_split (explicit part of each face conductance inside the RK stages, C_max,f = 0.5 min(V/alpha)/(nf beta_dt); L1 13-26 % better; costs 1.2-1.3x wall on uniformly stiff tests because the flux kernels re-do the opacity - make it FREE by forming F = -C_exp dx dT from the built conductance; BOX MEASURED 09-14: 2.2x SLOWER in the B-star box, same substages (max z_i -0.3 %), solution identical 5e-6 -> LEAVE OFF; ported to box branch as e0304cd2/11da763d/0c0ff997, binary build_hip_split), rad_sts_once (40 % fewer substages, 11 % wall, L1 4x WORSE -> leave off), rad_sts_margin (integer quantised, ~0). All bitwise off. UNIFIED 3-D RKL1 SWITCH DONE (rad_sts_all, main tree implicit-transverse-raddiff HEAD 271bcf6a, commits 75a3ce3c/8915265e/80129dcf/271bcf6a, NOT yet on rg-box-convection): one RKL1 loop over x1+x2+x3 replaces implicit radial + RKL1 transverse; 4.4x MORE ACCURATE than the split at the same dt, 25 % more substages, conservative 3e-16; physical x1 faces stay explicit (bottom rad_flux_inner mirrors conduction.cpp 831-835); Cartesian isotropic boxes only (NOT stretched spherical). Test test_rad_sts_all_cpu.py. Operator is exactly 2nd order (test regate 505f995b). Cost of the split operator in the B-star box 1.25x/cycle -> PCG deferred. STEP 1 DONE + WORKING: branch implicit-transverse-raddiff (main tree, HEAD cf1ff6c8, unpushed) = rg-box-convection commits bb7e05bb/be12cf30/4e535bad/61fe80cd (wt_rgbox). RKL1 STS on the energy increment, rad_implicit_ang=true replaces rad_cap_ang. BUG FOUND+FIXED be12cf30: operator-split LAG (T* from w0 at stage start vs post-radial energy, 4-7 %/stage) ran away 1.5x/cycle at the bottom row; fix = refresh T*/alpha from u0 (C_f still frozen -> Gaussian order 2.27->1.65, abs error 2-3x better; refresh C_f too = step 2). He-3 box: tracks the cap run, 1.37x cost, KEh 3-4x LOWER than with the cap (cap inflated amplitudes). GPU+MPI bitwise. Runs 09-13 night: He tr/tr10 11699957/8, B-star rt2turn_tr 11699959. ORIGINAL PLAN - implicit transverse (x2,x3) radiative-diffusion operator to replace rad_cap_ang in the FeCZ boxes. Design - operator-split task after ImplicitConduction (hydro_tasks.cpp ~191), per-layer 2D SPD solve on the energy increment, coefficients REUSED from the cap arrays cap_c2/cap_c3/capx (conduction.cpp 634/671/706), preconditioned CG with per-MeshBlock ADI preconditioner (15-25 it), dedicated 1-var MeshBoundaryValuesCC halo exchange as mhd.cpp 450 does for me0. Ship in 2 steps (STS loop first, then PCG). NOT RKG - 140-190 substages/cycle, MHD-only driver wiring. ~800-1000 lines, new file src/diffusion/conduction_transverse.cpp
metadata:
  type: project
---
Today: transverse fluxes radcond2/3 (conduction.cpp 760/802) ride the RK update; cap = wt*(capang/xm) per face (787-791);
with rad_cap_ang>0 the transverse dt terms are DROPPED (1601/1609), so the cap is the only stability. Explicit limit dt/(2 x_i),
x_i 700-1400 in the He box. Line solves alone fail: boxes split x2 AND x3 across ranks (he3_2turn nx2=nx3=104, block 52).
Unpreconditioned CG needs ~400 it (cond ~3e3). New inputs: rad_implicit_ang (default off, bitwise inert), rad_ang_tol 1e-8,
rad_ang_maxit 50, rad_ang_solver sts|pcg; fatal with rad_cap_ang>0 or cs/sp grids in v1. Tests: periodic 2D Gaussian at 100x
the explicit limit vs analytic, bitwise-off, sum V de = 0, He-3 box restart. See [[fecz-box-projects]], [[rt-thin-region-lte-prad]].
